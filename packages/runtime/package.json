{
  "name": "@prisma/runtime",
  "version": "0.0.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "pnpm exec tsc",
    "dev": "npx tsup src/index.ts --watch --format esm --dts",
    "test": "vitest run",
    "lint": "eslint 'src/**/*.ts'",
    "typecheck": "tsc -p tsconfig.build.json",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@prisma/relational-ir": "workspace:*",
    "@prisma/sql": "workspace:*",
    "typescript": "^5.6.2"
  },
  "devDependencies": {
    "@prisma/dev": "^0.11.0",
    "@types/pg": "^8.10.9",
    "pg": "^8.11.3",
    "tsup": "^8.2.4",
    "typescript": "^5.6.2",
    "vitest": "^2.1.1"
  }
}
