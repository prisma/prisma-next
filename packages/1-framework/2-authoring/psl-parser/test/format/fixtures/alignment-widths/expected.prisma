model A {
  shortName Int @id
  x         Int
}

model B {
  id            Int @id
  verboseColumn Int
}

model Widths {
  id      Int                  @id @default(autoincrement())
  related SomeVeryLongTypeName
  name    String
}

enum Role {
  USER
  ADMINISTRATOR
  GUEST
}
