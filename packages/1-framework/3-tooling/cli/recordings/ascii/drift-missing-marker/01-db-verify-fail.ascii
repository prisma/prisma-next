>






























────────────────────────────────────────────────────────────────────────────────
> # Verify fails — no marker, db init never run [empty database, contract emitted]
>





























────────────────────────────────────────────────────────────────────────────────
> # Verify fails — no marker, db init never run [empty database, contract emitted]
>





























────────────────────────────────────────────────────────────────────────────────
> # Verify fails — no marker, db init never run [empty database, contract emitted]
> prisma-next db verify





























────────────────────────────────────────────────────────────────────────────────
> # Verify fails — no marker, db init never run [empty database, contract emitted]
> prisma-next db verify





























────────────────────────────────────────────────────────────────────────────────
> # Verify fails — no marker, db init never run [empty database, contract emitted]
> prisma-next db verify





























────────────────────────────────────────────────────────────────────────────────
> # Verify fails — no marker, db init never run [empty database, contract emitted]
> prisma-next db verify
prisma-next db verify → Check whether the database has been signed with your contract
│
│ config:               prisma-next.config.ts
│ contract:             contract.json
│
│ Read more             https://pris.ly/db-verify
└

│
■  ✖ Database not signed (PN-RTM-3001)
│    Why: No database signature (marker) found
│    Fix: Run `prisma-next db sign --db <url>` to sign the database
>
















────────────────────────────────────────────────────────────────────────────────
