>






























────────────────────────────────────────────────────────────────────────────────
> # Recovery fails — cannot re-add NOT NULL column without default [email column missing, contract expects it
]
>




























────────────────────────────────────────────────────────────────────────────────
> # Recovery fails — cannot re-add NOT NULL column without default [email column missing, contract expects it
]
>




























────────────────────────────────────────────────────────────────────────────────
> # Recovery fails — cannot re-add NOT NULL column without default [email column missing, contract expects it
]
> prisma-next db update -y




























────────────────────────────────────────────────────────────────────────────────
> # Recovery fails — cannot re-add NOT NULL column without default [email column missing, contract expects it
]
> prisma-next db update -y




























────────────────────────────────────────────────────────────────────────────────
> # Recovery fails — cannot re-add NOT NULL column without default [email column missing, contract expects it
]
> prisma-next db update -y




























────────────────────────────────────────────────────────────────────────────────
> # Recovery fails — cannot re-add NOT NULL column without default [email column missing, contract expects it
]
> prisma-next db update -y
prisma-next db update → Update your database schema to match your contract
│
│ config:               prisma-next.config.ts
│ contract:             contract.json
│
│ Read more             https://pris.ly/db-update
└

│
■  ✖ Database schema does not satisfy contract (4 failures) (PN-RTM-3020)
│    Why: The resulting database schema does not satisfy the destination contract.
│    Fix: Inspect the reported conflict, reconcile schema drift if needed, then re-run `prisma-next db update
`
│    Issues (showing 1 of 1):
│      - [missing_column] Column "user"."email" is missing from database
>












────────────────────────────────────────────────────────────────────────────────
