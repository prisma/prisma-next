>






























────────────────────────────────────────────────────────────────────────────────
> # Verify catches drift — structural schema check detects missing column [manual DDL dropped email column, m
arker untouched]
>




























────────────────────────────────────────────────────────────────────────────────
> # Verify catches drift — structural schema check detects missing column [manual DDL dropped email column, m
arker untouched]
>




























────────────────────────────────────────────────────────────────────────────────
> # Verify catches drift — structural schema check detects missing column [manual DDL dropped email column, m
arker untouched]
> prisma-next db verify




























────────────────────────────────────────────────────────────────────────────────
> # Verify catches drift — structural schema check detects missing column [manual DDL dropped email column, m
arker untouched]
> prisma-next db verify




























────────────────────────────────────────────────────────────────────────────────
> # Verify catches drift — structural schema check detects missing column [manual DDL dropped email column, m
arker untouched]
> prisma-next db verify




























────────────────────────────────────────────────────────────────────────────────
> # Verify catches drift — structural schema check detects missing column [manual DDL dropped email column, m
arker untouched]
> prisma-next db verify
prisma-next db verify → Check whether the database signature and live schema match your contract
│
│ config:               prisma-next.config.ts
│ contract:             contract.json
│ mode:                 full (marker + schema)
│
│ Read more             https://pris.ly/db-verify
└

│
│  ✖ contract
│  └─ ✖ table user
│     ├─ ✖ columns
│     │  ├─ ✖ email: missing (Column "email" is missing)
│     │  └─ ✔ id: int4 (pg/int4@1) (not nullable)
│     └─ ✔ primary key: id
│
│  ✖ Database schema does not satisfy contract (4 failures) (PN-SCHEMA-0001)
>









────────────────────────────────────────────────────────────────────────────────
