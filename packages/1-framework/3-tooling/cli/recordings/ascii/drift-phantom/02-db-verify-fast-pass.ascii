>






























────────────────────────────────────────────────────────────────────────────────
> # Marker-only verification still passes — hash unchanged [email column dropped, marker row untouched]
>





























────────────────────────────────────────────────────────────────────────────────
> # Marker-only verification still passes — hash unchanged [email column dropped, marker row untouched]
>





























────────────────────────────────────────────────────────────────────────────────
> # Marker-only verification still passes — hash unchanged [email column dropped, marker row untouched]
> prisma-next db verify --fast





























────────────────────────────────────────────────────────────────────────────────
> # Marker-only verification still passes — hash unchanged [email column dropped, marker row untouched]
> prisma-next db verify --fast





























────────────────────────────────────────────────────────────────────────────────
> # Marker-only verification still passes — hash unchanged [email column dropped, marker row untouched]
> prisma-next db verify --fast





























────────────────────────────────────────────────────────────────────────────────
> # Marker-only verification still passes — hash unchanged [email column dropped, marker row untouched]
> prisma-next db verify --fast
prisma-next db verify → Check whether the database signature and live schema match your contract
│
│ config:               prisma-next.config.ts
│ contract:             contract.json
│ mode:                 fast (marker only)
│
│ Read more             https://pris.ly/db-verify
└

│
│  ✔ Database marker matches contract
│    verification: marker only (--fast)
│    storageHash: sha256:95060eda0d8ef9b30439212a41756c15066733defdde81ee7118f955be7361d1
│    profileHash: sha256:a9868829161d6f7e3a139d8cf9f42d59e0503281d26342ddf900d9b054a51b98
│
│  ⚠ Schema verification skipped because --fast was provided. Run `prisma-next db schema-verify` to detect st
ructural drift.
>











────────────────────────────────────────────────────────────────────────────────
