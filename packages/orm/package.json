{
  "name": "@prisma/orm",
  "version": "0.1.0",
  "description": "ORM-style relation support for Prisma Next",
  "main": "dist/index.mjs",
  "types": "dist/index.d.mts",
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "typecheck": "tsc --noEmit",
    "test": "vitest",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@prisma/relational-ir": "workspace:*",
    "@prisma/sql": "workspace:*",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^24.7.2",
    "tsup": "^8.0.0",
    "typescript": "^5.9.3",
    "vitest": "^2.1.1"
  },
  "files": [
    "dist"
  ]
}
