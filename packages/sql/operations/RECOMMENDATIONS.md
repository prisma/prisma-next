# Recommendations

## Observations
- Placeholder; SQL operation manifests remain in `@prisma-next/sql-target`.
- Extension authors have no guidance on how to contribute operations.

## Suggested Actions
- Move the SQL operation manifests into this package and document the manifest format.
- Add validation tests so missing lowering templates or capability flags fail fast.

