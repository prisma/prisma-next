export * from '../types';
// Re-export contract types for backward compatibility
export type {
  DataContract,
  ContractStorage,
  StorageTable,
  StorageColumn,
} from '@prisma-next/contract/types';

