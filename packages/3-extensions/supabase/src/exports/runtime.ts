// TODO(M2): the real SupabaseRuntime with asUser()/asAnon() lands here.
// This stub exists so the ./runtime subpath resolves; it is intentionally
// empty for M1.

export type SupabaseRuntime = never;
