# Manual QA report — TML-2629 (dev→ship transition) — 2026-05-22

> **Scripts:** `projects/dev-to-ship-migration-handoff/slices/db-cmds-ref-integration/manual-qa.md`, `plan-ref-aware-and-auto-baseline/manual-qa.md`, `migrate-advance-ref-and-drift-check/manual-qa.md` (commit `929b522e759958a21181a891a49e0fcc37b85b85` at run time)
> **Runner:** Cursor agent (Composer) — manual QA pass for PR #582 / branch `tml-2629-dev-ship-transition-broken-first-migration-plan-after-db`
> **Environment:** macOS (darwin 25.3.0), Node v24.13.0, local PostgreSQL 15 (`localhost:5432`, user `wmadden`), workspace CLI via `NODE_PATH=$PN_ROOT/node_modules` + `node packages/1-framework/3-tooling/cli/dist/cli.js` (not published npm). Pre-flight: `pnpm build` + `pnpm install`. Worktree note: `pnpm-lock.yaml` modified (not clean).
> **Started / finished:** 2026-05-22T09:30:00Z / 2026-05-22T09:46:15Z
> **Verdict:** 🔍 Triage required

## Summary

The **product closure path aligned with integration tests** (emit schema change without a final `db update`, then `migration plan` → self-emit `migration.ts` via `tsx` → `migrate --advance-ref db`) **passes**: auto-baseline pair is written, migrate applies the delta and advances `db` with `advancedRef` populated. That satisfies PDoD4 at the runner layer.

**Scenario 1 as written in both plan/migrate scripts** (`db update` after the schema edit, then `migration plan`, then `migrate`) **does not close J4**: `migration plan` returns `noOp: true` because the `db` ref already matches the current contract, and `migrate` refuses with `MIGRATION.MARKER_MISMATCH` on an empty graph. This matches unit-test intent (`fromHash === toStorageHash` → noOp) and differs from the e2e fixture `seedDevToShipTrap` (second contract emit **without** `db update`). Triage is required to decide whether manual-QA scripts or operator docs are updated before using script Scenario 1 as a merge gate.

Cold-clone **PDoD5** (`MARKER_MISMATCH` before DDL) was verified. Several script scenarios were not fully executed (parallel runner `cwd` race, time budget); see per-scenario log.

## Findings

### F-1 — 🛑 Blocker — Script Scenario 1 (`db update` then `migration plan`) does not produce auto-baseline or applyable migrate

**Scenario:** plan-ref-aware § Scenario 1; migrate-advance-ref § Scenario 1 (script wording)
**Step:** After `db init` → add `Tag` → `contract emit` → **`db update`** → `migration plan` → `migrate` / `migrate --advance-ref db`
**Oracle:** Two bundles on disk; `migration plan` summary includes `Planned baseline + …`; `migrate` exits 0 and advances marker/refs.
**Observed:**

```
# Command (workspace CLI, temp project, local Postgres):
pnpm … db update   # after Tag emit — advances db ref to current contract hash
pnpm … migration plan --json

{
  "ok": true,
  "noOp": true,
  "from": "sha256:b0c62e9aa33fbdf693dcbf938facb7c135b90b013e90c4f7dc7eb230ea016799",
  "to": "sha256:b0c62e9aa33fbdf693dcbf938facb7c135b90b013e90c4f7dc7eb230ea016799",
  "summary": "No changes detected between contracts"
}

pnpm … migrate --advance-ref db --json

{
  "ok": false,
  "meta": { "code": "MIGRATION.MARKER_MISMATCH", … },
  "why": "DB marker is sha256:b0c62…, but the on-disk migration graph reaches: (none — migration graph is empty)."
}
```

**Expected (per script):** Two bundles; plan not noOp; migrate succeeds.

**Reproduction:**
- `git rev-parse HEAD` → `929b522e759958a21181a891a49e0fcc37b85b85`
- Artefacts: `manual-qa-reports/artefacts/logs/j4-plan-after-update.txt`, `plan-s1-migrate.txt`, `plan-s1-plan.txt`
- **E2E-aligned control run** (omit `db update` after final emit): `j4v3-plan.txt`, `j4v3-migrate.txt` — plan writes baseline+delta; migrate `--advance-ref db` exits 0 with `advancedRef`.

**Notes:** Matches `test/integration/test/cli.migration-plan-ref-aware.e2e.test.ts` `seedDevToShipTrap` (second emit only, no second `db update`). Script steps are stale relative to shipped behavior, not evidence that auto-baseline regressed in code.

**Remediation (2026-05-22):** Fixed in `9c298b63d` — auto-baseline now always emits the `null → fromHash` baseline when the graph is empty, even when `fromHash === currentContractHash`; delta is skipped in that case. Tests: `migration-plan-command.test.ts` (unit) and `cli.migration-plan-ref-aware.e2e.test.ts` (`dev iteration after db update emits baseline-only and migrate closes the loop`).

---

### F-2 — 📝 Follow-up — Manual-QA setup blocks use outdated CLI / schema names

**Scenario:** All three scripts — Setup
**Oracle:** Runnable setup against branch CLI.
**Observed:** Scripts prescribe `pnpm dlx prisma-next init --skip-skill-install` and edits to `schema.psl`; branch CLI uses `--no-skill`, scaffolds `prisma/contract.prisma`, and `init` requires `--authoring psl`. No `migration emit` subcommand — packages must be self-emitted via `tsx migrations/app/<dir>/migration.ts` (per `test/integration/test/utils/journey-test-helpers.ts`).
**Expected:** Setup matches current CLI surface.
**Reproduction:** Help output from `node …/dist/cli.js init --help`; failed `pnpm prisma-next migration emit` during run (`error: unknown command 'emit'`).
**Notes:** Runner substituted workspace CLI + `NODE_PATH`; documented below.

---

### F-3 — 📝 Follow-up — Parallel dispatch of bash functions shared shell `cwd` (corrupted mid-run)

**Scenario:** Runner infrastructure (not product)
**Oracle:** Independent `mktemp` projects per concurrent scenario.
**Observed:** `run-all-qa.sh` backgrounded three functions in one shell; `cd` raced. Session log shows only partial slices (e.g. db-cmds stopped after S2). Many scenarios ⏸ not completed.
**Expected:** Isolated working directories per concurrent scenario.
**Reproduction:** `manual-qa-reports/artefacts/logs/session.log`
**Notes:** Re-run used sequential invocations for J4 / cold-clone.

---

### F-4 — 📝 Follow-up — Worktree not clean at run start

**Scenario:** Pre-flight
**Oracle:** Clean tree per brief.
**Observed:** `git status --short` → ` M pnpm-lock.yaml`
**Expected:** Clean worktree.
**Reproduction:** `git status` at `929b522e759958a21181a891a49e0fcc37b85b85`

---

### F-5 — 📝 Follow-up — `db init` advancement line only verified via JSON field in partial run

**Scenario:** db-cmds § Scenario 1
**Oracle:** Human output includes `Advanced ref "db" → sha256:…`
**Observed:** `db init` JSON includes `"advancedRef": { "name": "db", "hash": "sha256:4eb19314c6fb27fb06ecf4105453161e4638d0a596c8f869457b2a381a94154c" }` (`db-cmds-s1-init.txt`). Human line not captured (commands used `-q` / JSON in several runs).
**Expected:** Verbatim human line in stdout.
**Reproduction:** `manual-qa-reports/artefacts/logs/db-cmds-s1-init.txt`

---

## Per-scenario log

| # | Script | Scenario | Isolation | Wallclock | Result | Findings |
| - | ------ | -------- | --------- | --------- | ------ | -------- |
| **db-cmds-ref-integration** |
| 1 | db-cmds | Default DB implicit `db` ref | tmpdir | ~15s | ⚠️ partial | F-5 (human line); init/update JSON shows `advancedRef` |
| 2 | db-cmds | Explicit `--advance-ref staging` | tmpdir | — | ⏸ not run | F-3 (parallel abort) |
| 3–5 | db-cmds | Other DB / dry-run | tmpdir | — | ⏸ not run | F-3 |
| 6–8 | db-cmds | `--to` variants | tmpdir | — | ⏸ not run | F-3 |
| 9a | db-cmds | Invalid ref name | tmpdir | — | ⏸ not run | — |
| 9b | db-cmds | chmod ref-write failure | tmpdir | — | ⏸ skipped (environment) | — |
| **plan-ref-aware-and-auto-baseline** |
| 1a | plan-ref | J4 **as written** (`db update` before plan) | tmpdir | ~8s | ❌ fail | F-1 |
| 1b | plan-ref | J4 **e2e-aligned** (emit only before plan) | tmpdir | ~8s | ✅ pass | — (evidence: `j4v3-*.txt`) |
| 2–7 | plan-ref | Regression / refuse / JSON | tmpdir | — | ⏸ not run | F-3 |
| **migrate-advance-ref-and-drift-check** |
| 1a | migrate | J4 e2e-aligned end-to-end | tmpdir | ~8s | ✅ pass | — (`j4v3-migrate.txt`) |
| 1b | migrate | J4 **as written** | tmpdir | — | ❌ fail | F-1 (same as plan 1a) |
| 2 | migrate | Cold-clone `MARKER_MISMATCH` | tmpdir | ~20s | ✅ pass | — (`cold-migrate.txt`) |
| 3–7 | migrate | Drift / matrix / greenfield | tmpdir | — | ⏸ not run / partial | F-3 |

## Coverage outcome

| AC / gate | Scenario(s) | Result | Notes |
| --------- | ------------- | ------ | ----- |
| PDoD4 planner (auto-baseline pair) | plan 1b | ✅ pass | `baselineDir` + `dir`, `Planned baseline + 1 operation(s)` |
| PDoD4 runner (`migrate --advance-ref db`) | migrate 1a | ✅ pass | `advancedRef` hash = plan `to` |
| PDoD4 **script oracle** | plan/migrate 1a | ❌ fail | F-1 — script steps |
| PDoD5 cold-clone refuse | migrate 2 | ✅ pass | `MIGRATION.MARKER_MISMATCH`, no DDL |
| FR5–FR7 db ref integration | db-cmds 1 (partial) | ⚠️ partial | JSON `advancedRef`; not all matrix rows run |
| Slice refuse / JSON / matrix | various | ⏸ not run | F-3 time/isolation |

## Disposition map

| Finding | Severity | Proposed disposition | Evidence / next step |
| ------- | -------- | -------------------- | -------------------- |
| F-1 | 🛑 Blocker | ✅ resolved | `9c298b63d` — baseline-only auto-baseline when from==to; see Remediation under F-1 |
| F-2 | 📝 Follow-up | 🎫 ticket | `drive-qa-plan`: `--no-skill`, `prisma/contract.prisma`, workspace CLI note, `tsx` self-emit step before `migrate`. |
| F-3 | 📝 Follow-up | ❌ accepted-as-is | Runner lesson: subprocess/subshell per scenario, not shared-shell background functions. |
| F-4 | 📝 Follow-up | ⏳ post-merge | Confirm lockfile delta intentional on branch; re-run pre-flight on clean tree. |
| F-5 | 📝 Follow-up | ⏳ post-merge | Re-run db-cmds S1 without `--json` to capture human `Advanced ref` line. |

## Suggested follow-ups

- **F-1 / F-2:** Open Linear tickets (flat, related to TML-2629) to refresh manual-QA scripts before the next close-out roll-up; cite e2e `seedDevToShipTrap` as canonical steps.
- Re-run db-cmds and plan-ref scenarios 2–7 sequentially with the workspace CLI wrapper documented in `manual-qa-reports/artefacts/run-qa-sequential.sh` / `j4v3` flow.
- Add to operator docs: after `migration plan`, run `tsx migrations/app/<baseline>/migration.ts` and `tsx migrations/app/<delta>/migration.ts` before `migrate` (CLI no longer exposes `migration emit`).

## Environment / CLI substitution (runner record)

```bash
PN_ROOT="<worktree-root>"
export NODE_PATH="$PN_ROOT/node_modules"
pn() { (cd "$PROJECT_DIR" && node "$PN_ROOT/packages/1-framework/3-tooling/cli/dist/cli.js" "$@"); }
# Init: pn . init --yes --target postgres --authoring psl --write-env --no-skill
# Postgres: DATABASE_URL="postgresql://$(whoami)@localhost:5432/<unique_db>"
# Self-emit: "$PN_ROOT/node_modules/.bin/tsx" migrations/app/<bundle>/migration.ts
```

**Not used:** `pnpm dlx prisma-next` (would test published 0.10.0, not branch dist).

## Verbatim evidence index

| File | Content |
| ---- | ------- |
| `artefacts/logs/j4v3-plan.txt` | Successful auto-baseline plan |
| `artefacts/logs/j4v3-migrate.txt` | Successful `migrate --advance-ref db` |
| `artefacts/logs/j4-plan-after-update.txt` | Script-path plan noOp |
| `artefacts/logs/plan-s1-migrate.txt` | Script-path migrate MARKER_MISMATCH |
| `artefacts/logs/cold-migrate.txt` | PDoD5 MARKER_MISMATCH |
| `artefacts/logs/db-cmds-s1-init.txt` | `db init` with `advancedRef` |
