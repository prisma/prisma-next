# Brief: D7 R3 (resumed) — F4

- **F4 (low / process):** example test docblock + test name describe the two-view FK traversal as an "ORM join through the aggregate client" — contradicts server.ts comment + README. Fix the two strings; no behaviour change.

Gates: example lint + test run.
