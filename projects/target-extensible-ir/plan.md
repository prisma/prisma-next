# Project Plan

## Summary

The project ships in eight milestones sequenced foundation → consumers → mechanism → exemplars → docs. The opening foundation milestone introduces the framework SPI interfaces (`ContractSerializer<TContract>`, `SchemaVerifier<TContract, TSchema>`), the framework `SchemaNodeBase` abstract class, the `Storage` interface with `namespaces` shape, and the per-SPI family abstract bases. No new `Target<TContract, TSchema>` aggregator is introduced; the existing target descriptor pattern (`SqlControlTargetDescriptor`, etc.) grows two new named properties next to `migrations`. Mongo lands as the first consumer of the foundation (rather than as a parallel stream) so SPI shape disagreements surface against a second family before SQL targets commit to the SPI shape. M2.5 severs the inverted Mongo family→target dependency edge so SPI bases land at the family layer (matching SQL) before M3. Postgres + SQLite land together (M3) because they share family-level abstract bases — flipping one without the other would leave the family bases in a dual-shape state. **M3.5 lands the target-extensible authoring mechanism — a new `entities` namespace on `AuthoringContributions` plus the family-agnostic lift of `composed-authoring-helpers.ts` — so the two structural exemplars can contribute their entity kinds via the proven pack-bag-driven extension surface (rather than via hand-edited family-layer construction sites).** The two structural exemplars (enums, then namespace) build on the mechanism; the namespace exemplar splits in two — M5a introduces the namespace concept, multi-tenancy, and the authoring-DSL surface; M5b adds cross-namespace FKs as the load-bearing user-facing capability. Documentation lands at close-out.

The user-facing capabilities this project delivers are: multi-schema Postgres contracts with namespace declarations in PSL (top-level `namespace { … }` blocks; namespaces do not recursively nest) and the TS builder; cross-namespace FK references within a contract space via dot-qualified type references in `@relation` (the FL-02 fix that unblocks Supabase's `auth.users` story); connection-bound multi-tenancy via explicit `namespace unbound { … }` PSL opt-in (lowered by the Postgres interpreter to the IR `__unbound__` slot, resolved at connection time via `search_path`). The IR refactor that underwrites these capabilities is invisible to users but is what makes the follow-up Supabase project (RLS policies, `supabase()` runtime facade, `auth.users` queryable surface) a series of focused feature PRs rather than another foundational reshape.

**PR2 forward concerns (out of project scope; flagged for follow-up projects):**
- **Target-contributed top-level PSL blocks.** The framework PSL parser stays hardcoded for the small set of top-level keywords (`model`, `enum`, `type`, `types`, `namespace`). Downstream work (RLS, roles, custom types) will hit this gap. A follow-up project adds a target-contributed top-level PSL block registry and migrates `enum` from framework-parser to Postgres-pack contribution as the proof-of-concept. `namespace { … }` stays framework-parsed because it's a framework concern. Sequenced after this project's PR2 merges — does not block PR2.
- **`@hint(was: 'oldSlot')` mechanism for rebind intent.** When a contract changes a model's namespace declaration (or a table's name), the planner cannot distinguish a deliberate rebind from a delete-and-recreate. PR2 ships drop+create on any slot mismatch with no informational diagnostic (every slot mismatch looks identical to the planner; a diagnostic would either fire spuriously or require the same impossible inference it's trying to flag). The `@hint` mechanism referenced in ADR 001 is the durable solution; it solves table renames and namespace rebinds with one syntax. Future project.

**Spec:** [`projects/target-extensible-ir/spec.md`](spec.md)
**Linear (project):** [Target-Extensible IR + Namespaces](https://linear.app/prisma-company/issue/TML-2459) (originating ticket TML-2459, completed; per-milestone tickets TML-2468 through TML-2474)
**Linear (PR umbrellas):** [TML-2520 — PR2 follow-up (M5a + M5b + namespace ADRs)](https://linear.app/prisma-company/issue/TML-2520)

## Delivery sequencing — split into two PRs

The project ships across **two PRs** to keep each PR's review burden manageable and to isolate risk between the proven mechanism (M1-M4) and the heavier greenfield namespace work (M5a + M5b). The project itself stays intact at `projects/target-extensible-ir/` across both PRs; only the delivery is split.

- **PR1 — `target-extensible-ir` branch (current).** Ships M1 + M2 + M2.5 + M3 + M3.5 + M4 + M6 + **M7 (registry-driven hydration + entityTypes rename — PR1 review fixes)** + **M8 (PR1 review-findings sweep — principal-engineer findings + GH-thread comments)** + **M9 (PR1 architectural-honesty close — slot widening + cast drop + discriminator rename + abstract collapse + cheap adjacencies)**. Closes the polymorphic IR + extensible authoring (entityTypes) mechanism + first real consumer (Postgres enum exemplar) + the documentation sweep covering the IR convention + entityTypes mechanism. Decision recorded at the M5a-pause point: M5a's pre-flight reconnaissance found the namespace work was substantial enough that bundling it into one PR with M1-M4 would double the diff and put the proven mechanism at risk if M5a hit a deeper architectural issue. The PR1 narrative stands on its own: *"Contract IR + Schema IR are now extensible per-target via 3-layer polymorphic class hierarchies; contract authoring is extensible per-target via the registry-driven entityTypes mechanism; one full exemplar (enum) demonstrates the round-trip end-to-end with the polymorphic IR class first-class at every layer (no `as unknown` casts at the slot boundary); SQL stack honours the framework `Storage` interface; example apps consume the canonical façade surface."*
- **PR2 — namespace follow-up branch (umbrella ticket TML-2520).** Branches off the merged PR1 base. Ships M5a + M5b + namespace-specific ADRs. Consumes the proven M1-M4 mechanism without depending on further changes to it. Reconnaissance at `wip/m5a-reconnaissance.md` is portable to the PR2 branch (PSL consumer cascade audit, storage-keying inventory, M2 Mongo namespace template reference).

The project's close-out (delete `projects/target-extensible-ir/`, promote remaining ADRs to `docs/`, strip references) happens **after PR2 merges**, not after PR1. PR1 lands the M6 docs that cover the M1-M4 surface (IR convention + entities mechanism); PR2's docs sweep adds the namespace ADRs alongside its implementation. See § Close-out for the full sequence.

## Cross-project dependencies

This project must land **after** the Contract Spaces work because both projects contend for the same files (test fixtures, Mongo IR, SQLite planner). The strict-precedence dependencies, in order:

1. **[TML-2457 — APP_SPACE_ID coupling audit](https://linear.app/prisma-company/issue/TML-2457).** Touches 294 sites across 75 files, mostly tests constructing contracts. This project's FR8/AC12 migration touches the same lines. Doing TML-2457 first means each test file has one stable contract construction site for this project to migrate, not two.
2. **[TML-2463 — SQLite multi-space planner upgrade](https://linear.app/prisma-company/issue/TML-2463).** Conflicts on SQLite planner files this project will reshape. Doing TML-2463 first means it builds against today's flat-data IR (well-understood); this project's foundation step then absorbs the SQLite IR-shape flip alongside Postgres. Independent of TML-2408 — can run in parallel with it.
3. **[TML-2408 — Port contract spaces to Mongo family](https://linear.app/prisma-company/issue/TML-2408).** Direct collision with this project's Mongo migration step (M2). Doing TML-2408 first means this project absorbs one well-understood migration (Mongo aggregate-aware infrastructure → 3-layer IR) instead of two simultaneous reshapings. Independent of TML-2463 — can run in parallel with it. **Sequencing constraint to flag in TML-2408's plan:** Mongo must land fully aggregate-native in TML-2408 (not in a half-migrated transition window); otherwise this project's Mongo step deals with mixed-state Mongo code.

Independent / non-blocking:

- **[TML-2458 — Remove vestigial `MigrationMetadata` fields](https://linear.app/prisma-company/issue/TML-2458).** Tiny, scoped to one type. No file conflict. Cheap to land before this project so we don't migrate fields that are about to be deleted, but it can land at any time.
- **[TML-2464 — Strip single/multi-space branching](https://linear.app/prisma-company/issue/TML-2464).** Blocked by TML-2463 + TML-2408 + this project (Linear `blockedBy` set 2026-05-14). Paused on user instruction until this project lands. Its AC #7 (rename `schemaVerifyAgainstSchema → verifySchema`, compose `introspect + verifySchema`, remove async `schemaVerify`) is absorbed into M2.5 — see `wip/unattended-decisions.md § 12` and `specs/mongo-control-adapter-spi.spec.md § 0`. TML-2464 resumes against ACs #1-6 + #8 (multi-space stripping, `Aggregate` qualifier removal) when this project merges.

Resulting global sequence: TML-2458 (any time) → TML-2457 → TML-2463 ∥ TML-2408 → **this project (M1 → M2 → M2.5 → M3 → M3.5 → M4 → M5a → M5b → M6)** → TML-2464.

## Milestones

The eight milestones below correspond to the eight PRs (M1, M2, M2.5, M3, M3.5, M4, M5a, M5b, M6). Each milestone is a single PR. **M3 is not splittable into Postgres-then-SQLite:** the two SQL targets share family-level abstract bases (`SqlTable`, `SqlColumn`, `SqlForeignKey`, …), and lifting one to the class-hierarchy shape without the other would leave the family bases in a dual-shape state — incompatible with NFR1 ("no dual-shape transition window"). The Postgres + SQLite lifts therefore land in one PR; SQLite's share of the work is mechanical within that scope.

### M1 — Foundation (framework interfaces + family abstract bases)

**Goal:** declare the SPI interfaces and per-SPI family abstract bases. No targets touched yet; the framework + family layers compile but have no consumers. This is the largest single design surface and lands before any consumer commits to the shape.

**Tasks:**

- [ ] Declare framework types in `1-framework/`:
  - `interface SchemaNode { readonly kind: string }` — bare alphabet, no `accept`/`freeze` methods.
  - `abstract class SchemaNodeBase implements SchemaNode` — centralised `freeze()` helper for subclasses (mirrors today's frozen-AST pattern in `OpFactoryCall` and `MongoSchemaNode`). Subclasses call `this.freeze()` in their constructors.
  - `interface Namespace extends SchemaNode { id: string; … }`.
  - `abstract class NamespaceBase implements Namespace`.
  - `interface Storage { readonly namespaces: Record<string, Namespace> }` — the framework-level promise that every IR carries namespaces (enforces FR15 at the type level).
  - `interface SchemaVerifier<TContract, TSchema>` and `interface ContractSerializer<TContract>` (with `deserializeContract(json: unknown): TContract` and `serializeContract(contract: TContract): JsonObject`).
  - `createIdentityContractSerializer<TContract>()` helper for tests that don't exercise serialization.
  - **No new `Target<TContract, TSchema>` aggregator interface.** SPI aggregation lives on the existing `*ControlTargetDescriptor` / `*RuntimeTargetDescriptor` types (in `framework-components/control/control-instances.ts`); those grow new typed properties for `contractSerializer` and `schemaVerifier` as part of the descriptor type definitions.
- [ ] Declare SQL family abstract bases in `2-sql/`: `SqlNode extends SchemaNodeBase`, `SqlContractSerializerBase`, `SqlSchemaVerifierBase`, plus the IR-node bases (`SqlTable`, `SqlColumn`, `SqlForeignKey`, `SqlIndex`, `SqlPrimaryKey`, `SqlUnique`) and `abstract class SqlStorage implements Storage`.
- [ ] Declare Mongo family abstract bases in `2-mongo-family/`: `MongoContractSerializerBase`, `MongoSchemaVerifierBase`, `abstract class MongoStorage implements Storage`. Lift today's `MongoSchemaNode` to extend `SchemaNodeBase` (no behavioural change at this point).
- [ ] Family-shared utility helpers (`verifyCommonSqlSchema`, structural-validation helpers) co-land where the abstract bases call into them.
- [ ] Draft the ADRs the foundation calls for under `projects/target-extensible-ir/specs/`, captured against the foundation they describe; drafts get refined throughout M2–M5b as the convention is exercised, and are promoted to `docs/architecture docs/adrs/` at close-out. AC10 names the two that must exist by close-out: (a) the **3-layer polymorphic IR convention** ADR, and (b) the **architectural principles underwriting it** ADR (FR25). M1 drafts both — and any further ADRs the implementer surfaces as load-bearing while drafting (e.g. if the SPI aggregation strategy or the `__unspecified__` model warrants its own ADR rather than living inside one of the two named drafts). (Convention is to draft ADRs at close-out at a minimum; drafting earlier here is cheap insurance against losing context.)
- [ ] `pnpm lint:deps` passes; layering is enforced.

**Validation:** the framework + family compile in isolation. No consumers exist; correctness is verified by typecheck + layering rules, not behavioural tests.

### M2 — Mongo migration (first consumer of the foundation)

**Goal:** Mongo becomes the first family to commit to the foundation's SPI shape. SPI shape disagreements surface here, in a PR scoped to one family, where they can be resolved without simultaneously breaking SQL.

**Round structure.** M2 lands across two implementer rounds (orchestrator-decided during M2 R1 reconnaissance; logged in `wip/unattended-decisions.md § 4`):

- **M2 R1**: M1 placement-fix prerequisite + SPI shells + Mongo namespace concretion + Mongo target descriptor extension + Mongo's share of the `validateContract` migration. The implementer's pre-implementation reconnaissance also surfaced an M1 placement defect (Mongo family bases were placed in the wrong package layer such that target classes cannot extend them); the fix lands as a labeled prerequisite commit at the start of M2 R1 work.
- **M2 R2**: Full Contract IR class flip per FR18 — the **storage-shape** hierarchy (`MongoStorageCollection` interface → `MongoCollection` concrete class extending `SchemaNodeBase`; same swap for `MongoStorageIndex` → `MongoIndex`, `MongoStorageValidator` → `MongoValidator`, `MongoStorageCollectionOptions` → `MongoCollectionOptions`) plus options bags (`MongoIndexOptions`, `MongoCollationOptions`, etc.) all extending `SchemaNodeBase` per FR18's "no half-flat / half-AST state". Authoring `type =` shapes (currently named `MongoIndex` etc.) get a disambiguating `*Input` suffix to free the unprefixed names for the IR hierarchy. Hydration walker in `parseMongoContractStructure` constructs class instances from arktype-validated JSON. Cascade across foundation + authoring (TS + PSL) + query-ast + mongo-target + mongo-adapter. **Per-node IR classes are concrete-only (no abstract+target split)** — Mongo has a single target today (`target-mongo`), so the family-base+target-concrete split collapses to a concrete family-class per the "single-target families collapse the abstract-family bar" pattern; matches the existing Mongo Schema IR convention per spec line 387. When a second Mongo target lands the lift to abstract+target is mechanical and consumer-transparent. Orchestrator-decided in M2 R2 prep after the implementer surfaced the authoring-vs-storage shape distinction; rationale at `wip/unattended-decisions.md § 7` (storage-shape vs authoring-shape) and `§ 10` (single-target collapse, amending § 7's per-node abstract+target wording). Plan's prior wording ("MongoIndex from `type =` to AST class") was shorthand; spec line 387 + 477 (FR18) drive the interpretation; spec line 419's mapping table is generic illustration of the multi-target shape (applies to SQL in M3, not Mongo today).

**Tasks:**

- [ ] **Prerequisite (M2 R1):** Move the three Mongo M1 SPI bases (`MongoContractSerializerBase`, `MongoSchemaVerifierBase`, abstract `MongoStorage`) from `packages/2-mongo-family/9-family/src/core/ir/` to `packages/2-mongo-family/1-foundation/mongo-contract/src/` so `@prisma-next/target-mongo` can extend them without producing a circular workspace dependency. M1 R1/R2 review missed the layering check (`@prisma-next/family-mongo` already depends on `@prisma-next/target-mongo`); see orchestrator note in `code-review.md` and `wip/unattended-decisions.md § 2`. Asymmetric with SQL: `@prisma-next/family-sql` does NOT depend on the SQL targets, so SQL needs no parallel placement fix in M3.
- [ ] Introduce `MongoTargetContractSerializer extends MongoContractSerializerBase` and `MongoTargetSchemaVerifier extends MongoSchemaVerifierBase` (concrete SPI implementers). The verifier base body follows the **pragmatic recipe** (orchestrator decision; `wip/unattended-decisions.md § 3`): the family base provides the iteration scaffolding (walk namespaces, walk collections, dispatch protected per-element hooks, assemble the result envelope) and the target overrides hooks (`verifyCollection`, `verifyIndex`, etc.) carrying the per-element diff/canonicalize logic. _M2.5 R1 update: `contractToMongoSchemaIR` / `canonicalizeSchemasForVerification` / `diffMongoSchemas` lifted to family-mongo with the verifier shell rather than left at target — see § Open items._
- [ ] **(M2 R2)** Flip Mongo's storage-shape Contract IR to AST classes per FR18: `MongoStorageCollection` interface → `MongoCollection` class hierarchy (family base + `MongoTargetCollection extends MongoCollection`); same swap for index/validator/options shapes; options bags (`MongoIndexOptions`, `MongoCollationOptions`) all extend `SchemaNodeBase` ("no half-flat / half-AST state"); authoring `type =` shapes get disambiguating `*Input` suffix; hydration walker in `parseMongoContractStructure`. See round-structure note above for the full scope.
- [ ] **(Post-M2 R2 — rebase onto PR 494)** PR 494 ports the SQL contract-spaces mechanism to the Mongo family (per-space marker ledger, multi-space runner with per-space verify projection, framework `projectSchemaToSpace` / `extractStorageElementNames` generalisation to duck-type `collections` alongside `tables`, synthetic Mongo fixture + e2e). It is review-ready with green CI as of M2 R2 start; the orchestrator's order recommendation is PR 494 lands first and M2 R2 rebases onto it (rationale: PR 494 is review-ready; the conflict surface is small and additive on both sides; rebase direction is natural since our class-IR lift naturally absorbs the new contract-space consumers as part of the M2 R2 consumer cascade; cost-of-delay asymmetry favours not holding a green-CI PR for a not-yet-PR'd refactor). Three textually-conflicting files (additive edits in non-overlapping sections — `packages/2-mongo-family/9-family/src/core/control-instance.ts`, `packages/2-mongo-family/9-family/src/exports/control.ts`, `packages/3-mongo-target/1-mongo-target/src/exports/control.ts`) plus semantic-but-not-textual interactions (PR 494's per-space verify projection re-wraps the pruned shape as `MongoSchemaIR` — needs to consume the new `MongoCollection` class hierarchy; PR 494's `mongo-runner.ts` consumer of the marker-ledger flow needs to consume our hydration-walker output, not raw JSON). Rebase commit message: `rebase(target-extensible-ir): pick up TML-2408 contract-spaces-mongo-port`. After rebase, re-run M2 R2 validation gates on the rebased state. PR 494's new per-space-runner code is another consumer of the existing Mongo descriptor seam (the `mongoTargetDescriptor` aggregator at `family-mongo`); this deepens the seam-asymmetry-vs-SQL recorded in § Open items but does not change M2 R2's scope.
- [ ] **Mongo namespace concretion.** Declare `class MongoTargetDatabase extends NamespaceBase` (Namespace = the connection's `db` field) and the singleton subclass `class MongoTargetUnspecifiedDatabase extends MongoTargetDatabase` with `id = '__unspecified__'`. The singleton overrides the qualifier-emission methods to elide the database name from any rendered output. The Mongo storage gets `namespaces: { __unspecified__: MongoTargetDatabase.unspecified, … }`. **Implementing Mongo namespace semantics in M2 (rather than in M5a) keeps all families aligned on the same Storage shape from the first consumer commit; bolting it on later would require reshaping `MongoStorage` after consumers had begun depending on its shape.**
- [ ] Extend the Mongo target descriptor type with `contractSerializer` + `schemaVerifier` named properties; the runtime Mongo descriptor composes the two implementers.
- [ ] Migrate Mongo's framework-internal call sites to `descriptor.contractSerializer.deserializeContract(json)` (Mongo-side share of the FR8 migration).
- [ ] Mongo unit + integration tests pass (`mongodb-memory-server`).
- [ ] If the SPI shape needs to flex to accommodate Mongo, the change lands in M1's interfaces — this is the cheapest moment to find such a need.

**Validation gate** (must all pass for SATISFIED):

- `pnpm typecheck` (workspace-wide; catches consumer breakage from any rename or SPI-base lowering).
- `pnpm lint:deps` (no new layering violations).
- `pnpm --filter '@prisma-next/mongo*' --filter '@prisma-next/family-mongo*' --filter '@prisma-next/mongo-target*' test` (Mongo unit tests across foundation, family, and target packages).
- `pnpm test:integration` (the only path that exercises `mongodb-memory-server`).
- Cross-package grep guard: after the IR class flip, grep the workspace for the deleted-or-renamed type names (`MongoIndex`, `MongoIndexOptions`, `MongoCollationOptions`, …) and confirm no consumer outside the Mongo packages still references the old shape.

(Workspace-wide `pnpm test:packages` is intentionally not in the gate. The pre-existing fragility recorded under § Open items doesn't touch Mongo, but `pnpm test:packages` would run those failures and produce noise the implementer would have to manually triage every round. The Mongo-scoped `--filter` set is the surface M2 actually changes.)

**Other validation:** Family/target boundary observable in code (see m1 R2 watch-points in `learnings.md` — particularly the lowering of `verifyCommonMongoSchema` + `parseMongoContractStructure` from abstract to concrete at the family layer).

### M2.5 — Mirror SQL's adapter-SPI dispatch pattern in Mongo

**Goal:** restore cross-target consistency (Principle 2 from the architectural-principles ADR) before SQL exercises the same SPI surface in M3. Today, Mongo's family→target dep edge is inverted (`@prisma-next/family-mongo` depends on `@prisma-next/target-mongo`, `@prisma-next/adapter-mongo`, and `@prisma-next/driver-mongo`). The inversion forced M2 R1 to place Mongo's SPI bases at `mongo-contract` (foundation) as a workaround. M3 will land SQL's SPI bases at `family-sql` (family layer) — without M2.5, the project ships with a Mongo-at-foundation / SQL-at-family asymmetry that future readers trip over and that any future Mongo target (Atlas, multi-tenant) inherits.

The SQL pattern (verified on disk in pre-M2.5-R1 reconnaissance, recorded in `wip/unattended-decisions.md § 11`): `family-sql` declares a `SqlControlAdapter` SPI interface; the per-target adapters (`PostgresControlAdapter`, etc.) implement the SPI with method bodies that execute queries directly via `driver.query(...)` — the adapter is the natural home because it has both the connection (`driver`) and the dialect-specific query vocabulary. `family-sql/control-instance.ts` dispatches via `getControlAdapter().readMarker(driver, APP_SPACE_ID)` rather than importing target/adapter symbols directly. The result: `family-sql` carries zero `target-* / adapter-* / driver-*` runtime deps. M2.5 mirrors this in Mongo.

**Original "relocation only" framing (corrected pre-R1).** M2.5 was originally specified as a flat file relocation of `mongo-target-descriptor.ts` from family to target. Pre-R1 reconnaissance found that the dep severance the original spec promised could not be reached by the file move alone — `family-mongo/control-instance.ts` carries seven additional runtime imports of target/adapter symbols (marker ops, schema verifier, operation formatter, target contract type, introspection helper) that the descriptor file relocation does not touch. The corrected spec rewrites M2.5 as the architecturally correct shape (SPI dispatch); the descriptor relocation falls out as one of nine orchestrated changes. See `wip/unattended-decisions.md § 11` for the full reasoning trail and the user-validated call to land the corrected scope.

**Sequencing.** Runs AFTER the post-M2 R2 PR 494 rebase (so the multi-space runner code added by PR 494 to `mongo-target-descriptor.ts` is in our tree before we move the file) and BEFORE M3 (so SQL's SPI base placement is symmetric with Mongo's from the start, and so the lessons from this Mongo dispatch refactor inform M3's SPI surface where applicable).

**Absorbed work.** This milestone subsumes the work originally filed as [TML-2505](https://linear.app/prisma-company/issue/TML-2505/relocate-mongo-target-descriptorts-into-target-mongo-fix-mongo-vs-sql) (`projects/relocate-mongo-target-descriptor/`). TML-2505 is closed as duplicate of TML-2459.

**Detailed task spec:** [`projects/target-extensible-ir/specs/mongo-control-adapter-spi.spec.md`](specs/mongo-control-adapter-spi.spec.md). The task list below summarises the milestone's ten orchestrated changes; the spec carries file-by-file detail and acceptance criteria.

**TML-2464 AC #7 absorbed into M2.5 (recorded in `wip/unattended-decisions.md § 12`).** TML-2464 is paused on user instruction until this project lands. Three of its commits (cross-family rename `schemaVerifyAgainstSchema → verifySchema`, compose `introspect + verifySchema` at call sites, async `schemaVerify` removal) are folded into M2.5 as a § 0 prerequisite step — the renames + composition cherry-pick cleanly; the async removal naturally absorbs into the SPI dispatch refactor of `family-mongo/control-instance.ts`. M2.5 ships against the new `verifySchema` naming. TML-2464 resumes against a smaller scope (its remaining ACs #1-6 + #8) when this project merges.

**Tasks:**

- [ ] **Absorb TML-2464 AC #7** (renames + async-`schemaVerify` removal). Implementer can cherry-pick TML-2464 commits `b4548c1b3` (rename) and `e6c9d1563` (compose at call sites) onto our base; both auto-merge. Drop the `projects/migration-runner-target-layer/{spec,plan}.md` additions from those commits — they belong to TML-2464. The third commit `d0712a45e` (async removal) conflicts in `family-mongo/control-instance.ts` against M2 R1's helpers and is folded into the SPI dispatch refactor (task 5 below); the SQL family's async-`schemaVerify` removal is a small mechanical pass that can land as a sibling commit.
- [ ] **Define `MongoControlAdapter` SPI** at `packages/2-mongo-family/9-family/src/core/control-adapter.ts` (mirrors `SqlControlAdapter`). Carries `readMarker`, `readAllMarkers`, `initMarker`, `updateMarker`, `writeLedgerEntry`, `introspectSchema`. Re-export from `packages/2-mongo-family/9-family/src/exports/control-adapter.ts`.
- [ ] **Move `marker-ledger.ts`** from `packages/3-mongo-target/1-mongo-target/src/core/` → `packages/3-mongo-target/2-mongo-adapter/src/core/`. The file's imports are pure wire-level (zero target-mongo internals), so the move is mechanical. Companion test moves with it.
- [ ] **Implement `MongoControlAdapter`** at `mongo-adapter` (class `MongoControlAdapterImpl` or factory equivalent). Wraps `Db`, forwards SPI calls to the relocated marker functions and the existing `introspect-schema.ts`.
- [ ] **Relocate `verifyMongoSchema`** (verifier shell) from `target-mongo/schema-verify` → `family-mongo/schema-verify` (mirrors `verifySqlSchema` placement). Per-element diff/canonicalize helpers (`contractToMongoSchemaIR`, `canonicalizeSchemasForVerification`, `diffMongoSchemas`) lift alongside the verifier — they only depend on foundation packages and lifting them removes a target → family → target import-shape hop. Relocate `formatMongoOperations` to `family-mongo/operation-preview.ts` as a free function (mirrors SQL's `sqlOperationsToPreview`).
- [ ] **Refactor `family-mongo/control-instance.ts`** to dispatch every target/adapter call via the `MongoControlAdapter` SPI surfaced by the control stack. Match SQL's `getControlAdapter()` helper pattern (structured error if adapter is missing or doesn't implement the SPI shape). Replace `MongoTargetContract` with the SPI's contract-shape generic. As part of this refactor, delete the async `schemaVerify` method (per TML-2464 AC #7 absorbed in task 0) and rewrite its one internal use case to compose `getControlAdapter().introspectSchema(driver)` + `verifySchema(...)`.
- [ ] **Relocate the three Mongo SPI bases** from foundation (`mongo-contract`) back up to family (`family-mongo`). Files: `mongo-contract-serializer-base.ts`, `mongo-schema-verifier-base.ts`, abstract `mongo-storage-base.ts`. The M2 R1 workaround placement is no longer needed once the dep edge is severed. Update `target-mongo`'s implementer classes to import the bases from `@prisma-next/family-mongo/control` (or wherever the family barrel surfaces them).
- [ ] **Relocate `mongo-target-descriptor.ts`** from `packages/2-mongo-family/9-family/src/core/` → `packages/3-mongo-target/1-mongo-target/src/core/control-target.ts`. The original spec's headline change; now mechanical because the previous tasks already remove the family-layer reasons it lived there. New home imports `MongoControlFamilyInstance` from `@prisma-next/family-mongo/control` (upward direction).
- [ ] **Sever the family→target/adapter/driver dep edges** in `packages/2-mongo-family/9-family/package.json`: drop `@prisma-next/target-mongo`, `@prisma-next/adapter-mongo`, `@prisma-next/driver-mongo`. Verify with `cat packages/2-mongo-family/9-family/package.json | rg "target-mongo|adapter-mongo|driver-mongo"` returning zero hits. Run `pnpm install` to update the lockfile.
- [ ] **Update consumers** for direct deep imports into any of the moved files; prefer routing through public package surfaces. Family's public API (`@prisma-next/family-mongo/control`) still exposes `mongoTargetDescriptor` and `MongoControlTargetDescriptor` (re-exported from target-mongo) — no public-API change.
- [ ] **Update subsystem 10 doc** (`docs/architecture docs/subsystems/10. MongoDB Family.md`) for any "where things live" references that name the moved files. Add a short paragraph naming the `MongoControlAdapter` SPI.

**Validation gate** (must all pass for SATISFIED):

- `pnpm typecheck` (workspace-wide).
- `pnpm lint:deps` (zero new violations; the family→target/adapter/driver violations should disappear; the architecturally-correct symmetric-with-SQL state appears).
- `pnpm --filter '@prisma-next/mongo*' --filter '@prisma-next/family-mongo*' --filter '@prisma-next/mongo-target*' test`.
- `pnpm test:integration` (multi-space runner + `db-sign` + `db-verify` + `db-update` + the contract-spaces-mongo-port surface from PR 494). The documented PGlite shutdown-race flake set (see `plan.md § Open items` trajectory note) is accepted — isolation re-runs of any non-PGlite failure are required before declaring done.

**Acceptance:** detailed criteria at [`specs/mongo-control-adapter-spi.spec.md § Acceptance criteria`](specs/mongo-control-adapter-spi.spec.md#acceptance-criteria). The orchestrator's M2.5 close-out also marks TML-2505 as RESOLVED (duplicate of TML-2459) and removes the "Mongo descriptor-seam asymmetry vs SQL" entry from `plan.md § Open items`.

### M3 — Postgres SPI shells + `validateContract` migration

**Goal:** Postgres adopts the foundation. The `validateContract` → SPI migration lands here, including the test-cost share (FR8 / AC12).

**Round split (M2 precedent — see `wip/unattended-decisions.md § 4` and `§ 13`).** M3's task surface is too large for a single round (~248 SQL-IR-consumer files for the class flip + ~111 `validateContract` call sites + descriptor wiring + SQLite mirror; realistic at 5-10 days of focused implementer work). Split into four rounds with intent-validation between each:

- **R1 — emitter-side serializer audit (done; commit `b8ba9c0cb`).** Outcome (a) per `wip/unattended-decisions.md § 5`. `canonicalizeContractToObject` accepts an optional `CanonicalSerializeContract` hook threaded from the descriptor at the CLI; `MongoTargetContractSerializer` overrides `serializeContract`; `MongoTargetStorage` drops its `Object.defineProperty(..., enumerable: false)` workaround. Convention recorded at `learnings.md` so M5a (when SQL targets gain runtime-only `namespaces`) follows the SPI-override pattern.
- **R2 — SPI implementer shells + descriptor wiring against existing flat IR (done; commits `f2c9fd5b1` Postgres + `9a70429b0` SQLite, plus orchestrator cleanup `106482a76`).** Tasks 2, 3 + SQLite mirror. The SPI bases tolerate any `TContract`, so the implementer classes stood up against today's flat-data Postgres + SQLite IR. `SqlControlTargetDescriptor` gained a `TContract extends Contract<SqlStorage> = Contract<SqlStorage>` generic plus `contractSerializer` + `schemaVerifier` named properties; existing call sites compile unchanged. The R2 typecheck gate surfaced two pre-existing escapees from prior rounds (M2.5's vestigial `family` parameter on `createMongoRunnerDeps`; M3 R1's contravariant-parameter mismatch at the side-by-side-contracts integration test boundary) which the orchestrator absorbed in the cleanup commit rather than reopen the implementer.
- **R3a — IR class flip + hydration walker (done; commits `3b5a477a6` → `a8e794e31`, plus orchestrator cleanup `[next commit]` dropping family-side IR placeholder stubs flagged by the R3a reviewer).** SQL Contract IR (`SqlStorage`, `StorageTable`, `StorageColumn`, `PrimaryKey`, `UniqueConstraint`, `Index`, `ForeignKey`, `ForeignKeyReferences`, `StorageTypeInstance`) and SQL Schema IR (`SqlSchemaIR`, `SqlTableIR`, `SqlColumnIR`, `SqlForeignKeyIR`, `SqlUniqueIR`, `SqlIndexIR`, `PrimaryKey`) flipped from flat-data interfaces to family-shared concrete classes extending `SchemaNodeBase` via family bases `SqlNode` / `SqlSchemaIRNode`. `SqlContractSerializerBase` now ships the family-shared hydration walker; per-target Postgres + SQLite serializers collapse to empty class declarations. Decision 16 (`SchemaNodeBase.kind` becomes optional at the framework level) collapsed the workspace cascade from ~200-500 mechanical edit sites to a handful of test rebases. M1's unearned `SqlTable` abstract `name` / `namespaceId` field declarations dropped per Decision 15. The 8 unused family-side IR placeholder stubs (`SqlNode`/`SqlTable`/`SqlColumn`/etc. at `family-sql/ir/`) deleted per Decision 14's "abstract earns its existence by ≥2 consumers" rule — they get reintroduced in M4/M5a if the abstract+target split actually arrives. Round shape: 7 commits, no rollbacks, workspace typecheck + SQL stack package tests green throughout.
- **R3b — `validateContract` migration + AC12 close-out (done; commits `65d9cfab6` → `5ba7e4a98`, 9 commits).** ~50+ consumer call sites migrated to `descriptor.contractSerializer.deserializeContract(json)` across packages, integration/e2e tests, and demo examples. SQL `validateContract` removed from `@prisma-next/sql-contract/validate` public barrel. Two foundation-level barrel exports (framework `validateContract`, foundation `validateMongoContract`) **retained** as layering-allowed structural-validation primitives — parallel to the existing `validateSqlContractFully` pattern; lower-layer non-family packages (e.g. `mongo-query-builder` at layer 5) need access to the structural-validation primitive without depending on family layer. AC12 satisfied for the user-facing shape. R3b's call-site sweep surfaced one tracked regression (codec-aware default-decoding; see § Open items). Reframed from the original "R4" naming — R3a covered tasks 4 + 5 + SQLite mirror; R3b absorbs the original R4 scope. Originally planned R3 + R4 collapses to R3a + R3b.

**Tasks:**

- [ ] **M3 entry — emitter-side serializer audit (orchestrator-decided in M2 R1; see `wip/unattended-decisions.md § 5`).** Audit `canonicalizeContractToObject` (and any sibling emitter walk over `contract.storage`) and lift the storage-canonicalization path to consume `descriptor.contractSerializer.serializeContract(contract)` instead of walking the in-memory storage directly with `Object.entries`. The current Mongo `MongoTargetStorage` workaround uses `Object.defineProperty(..., enumerable: false)` to hide the runtime-only `namespaces` field from the emitter walk; that workaround multiplies per class-internal field as the IR class hierarchy grows. The architecturally correct home for "strip runtime-only class API fields before canonicalization" is the SPI itself. If the audit shows the emitter cannot reach the SPI at its layer (e.g. it operates on contract-types-level shapes before the descriptor is bound), document why and accept the symptom-fix pattern across SQL targets too — but the audit must be performed and the decision recorded. Acceptance: either (a) `canonicalizeContractToObject` calls `serializeContract` and `MongoTargetStorage` drops its `defineProperty` workaround; or (b) a new ADR appendix at `projects/target-extensible-ir/specs/3-layer-polymorphic-ir-convention.md` documents why the symptom-fix pattern is the correct architectural choice for emit-side canonicalization.
- [ ] Introduce `PostgresContractSerializer extends SqlContractSerializerBase` and `PostgresSchemaVerifier extends SqlSchemaVerifierBase`.
- [ ] Extend `SqlControlTargetDescriptor` (and any sibling descriptor types) with `contractSerializer` + `schemaVerifier` named properties; the runtime Postgres descriptor (`postgresControlTargetDescriptor` in `packages/3-targets/3-targets/postgres/src/exports/control.ts`) composes the two implementers next to its existing `migrations` property.
- [ ] Lift today's family-shared flat-data SQL Contract IR to family-shared concrete classes (`SqlStorage`, `SqlTable`, `SqlColumn`, `SqlForeignKey`, `SqlIndex`, `SqlPrimaryKey`, `SqlUnique`) extending `SchemaNodeBase`; constructors call `freezeNode(this)` per Decision 8. Per-target subclasses (`PostgresEnumType`, per-target `namespaces` storage) deferred to M4 / M5a per Decision 14 (the abstract+target split is unearned today because Postgres + SQLite share 100% of the IR shape).
- [ ] Lift SQL Schema IR (introspection-side) to the same family-shared class-hierarchy shape.
- [ ] Migrate every `validateContract` call site to `descriptor.contractSerializer.deserializeContract(json)`. Remove the standalone `validateContract` function from public exports. The user-facing facade (`postgres<Contract>(...)`) wraps the SPI call so end-users do not see it.
- [ ] Migrate test suites: tests that exercise serialization import a real target descriptor; tests that don't use `createIdentityContractSerializer<TContract>()`.
- [ ] (SQLite-specific work in R3 collapses to "SQLite consumes the family-shared concrete classes alongside Postgres" — no SQLite-side concretion needed today per Decision 14.) The SQLite descriptor's two new SPI properties already landed in R2 (`9a70429b0`). SQLite's `validateContract` call sites migrate alongside Postgres in R4.
- [ ] Postgres + SQLite unit + integration tests pass (PGlite).
- [ ] AC12 verified by inspection of migrated call sites and absence of `validateContract` from public exports.

**Validation:** existing Postgres + SQLite test suites green; round-trip property tests confirm fidelity (AC8).

### M3.5 — Target-extensible authoring (entity contributions)

**Status: done** (R1 commits `d2a729e65` → `154d5fbfb`, R1b fix commit `89c29b1ae`). FR8a-FR8d and AC12a's mechanism-level subclauses satisfied; named-exemplar subclauses defer to M4 + M5a per Decision 17. Decision 17 boundary preserved (Mongo built-in contribution-ification deferred to a tracked open item). Spec amended for descriptor's shipped `args?` shape vs the originally-drafted `schema` shape (rationale in commit `154d5fbfb`); ADR draft refreshed. R1 surfaced one pack-bag variance regression (FR8b — `AuthoringEntityDescriptor` defaulted to contravariant-blocking `Input = unknown`) caught by the reviewer; R1b fixed with `Input = never` defaults at the contravariant factory parameter position + dispatch-site cast at `instantiateAuthoringEntity`. Workspace typecheck + lint:deps + filtered authoring + Mongo regression + filtered SQL gates all green.

**Goal:** make the contract builders target-extensible so the structural exemplars (M4 enum, M5a/b namespace) and any future target-specific entity (RLS policies, roles, views — out of scope for this project but the mechanism's first real consumers) can be contributed by target packs via the existing pack-bag-driven extension surface, rather than via hand-edited family-layer construction sites. Closes the gap diagnosed at M2 R2 close-out: the IR is target-extensible (M2 R2) but the builders that construct it are not.

**Background.** The existing `composed-authoring-helpers.ts` scaffolding (`packages/2-sql/2-authoring/contract-ts/src/composed-authoring-helpers.ts`) already carries pack-bag-driven type narrowing for **fields**, **type constructors**, codec types, query operation types, and SQL index types via the `MergeExtensionXxx<Packs>` + `UnionToIntersection` template, surfacing them as a typed `helpers` parameter to `defineContract`'s factory. This milestone extends that template to a new **`entities`** namespace on `AuthoringContributions` for top-level contract-authoring-domain kinds (the kinds packs contribute — models, namespaces, enums, future RLS policies, roles, views) — distinct from the application-domain instances users author against them (their `User`, `Invoice`, `Product`). No novel type-system machinery; the contribution shape mirrors the existing codec-descriptor pattern (`codecDescriptors[].factory` on pack refs) — a descriptor with a schema (PSL/TS lowest common denominator) plus an output mechanism that is either a declarative template (preferred where expressible) or a factory function (when computation can't be templated, as for IR-class instances that carry methods, freeze semantics, and per-class identity).

**Sequencing.** Runs AFTER M3 (Postgres SPI shells; the SQL-side SPIs need to be in tree before the mechanism wires construction through them) and BEFORE M4 (the enum exemplar is the first consumer of the new `entities` namespace).

**Tasks:**

- [ ] **Add `entities` namespace to `AuthoringContributions`** in `packages/1-framework/1-core/framework-components/src/shared/framework-authoring.ts`. The descriptor shape: `{ kind: 'entity', schema, output: { template } | { factory } }` where `template` follows the existing `AuthoringTemplateValue` shape (preferred where expressible) and `factory: (input) => SchemaNode` constructs an IR-class instance from validated input (used for any contribution whose output is a class instance, which is most or all contributions in this namespace today). Mirror the existing `field` / `type` namespace shape for collision detection (`isAuthoringEntityDescriptor`, recursive `mergeAuthoringNamespaces` walker).
- [ ] **Lift `composed-authoring-helpers.ts` to a family-agnostic location.** The current file lives at `packages/2-sql/2-authoring/contract-ts/src/composed-authoring-helpers.ts` with SQL-flavoured type parameters (`TargetPackRef<'sql', string>`, etc.). Move the family-agnostic merge / instantiation scaffolding to a shared location (likely `@prisma-next/contract-authoring`) so Mongo can mirror; SQL-specific helper composition stays where it is, importing the lifted scaffolding. Verify both contract-builders (`packages/2-sql/2-authoring/contract-ts` and `packages/2-mongo-family/2-authoring/contract-ts`) consume the lifted module without circular deps.
- [ ] **Extend `ComposedAuthoringHelpers<Family, Target, ExtensionPacks>` to merge entity contributions across packs.** Apply the same `MergeExtensionEntityNamespaces<ExtensionPacks>` + `UnionToIntersection` template used today for `field` / `type`. Surfaces contributed entities at `helpers.entities.<entityName>(...)` (top-level placement; collisions detected at compose time per the existing `assertNoCrossRegistryCollisions` discipline).
- [ ] **Wire the SQL contract-builder.** `defineContract`'s factory already receives `helpers: ComposedAuthoringHelpers<Family, Target, ExtensionPacks>`; the new `helpers.entities` surface becomes available without changing the call signature. Update `contract-lowering.ts` / `build-contract.ts` to consume entity-contribution outputs alongside the existing model / type / field paths.
- [ ] **Wire the Mongo contract-builder (thin plumbing only — Decision 17).** `packages/2-mongo-family/2-authoring/contract-ts/src/contract-builder.ts` adopts the lifted scaffolding and exposes `helpers.entities` to its factory shape so future Mongo packs can contribute entities. **Mongo built-in contribution-ification deferred** — the existing hand-imported `MongoCollection` / `MongoIndex` / `MongoCollectionOptions` construction sites in `buildCollections` / `toStorageIndex` / `toStorageCollectionOptions` stay as-is; restructuring them to dispatch through pack-contributed factories is a separable consumer-cascade refactor that does not block M4 / M5a (the M3.5 mechanism's primary downstream consumers). Tracked in § Open items.
- [ ] **Pack-side wiring exemplar.** Demonstrate one in-tree pack contributing an entity through the new mechanism end-to-end (likely a Postgres-pack contribution serving as the rehearsal for M4 enum). Acceptance: user code authoring against a pack that ships entity contributions sees the entity helper at `helpers.entities.<name>` with full type narrowing, and the contributed call site lowers to the correct IR-class instance via the pack-supplied factory.
- [ ] **Spec amendment + ADR draft.** This milestone's spec amendment (under `## Authoring surface` in spec.md) documents the unified contribution shape and the `entities` namespace. The ADR draft (`projects/target-extensible-ir/specs/target-extensible-authoring.md`) captures the rejected alternatives (declarative-only-for-entities; builder-emits-JSON; mixin / class-extension; sub-builder dispatch; per-target builder packages). Both land as part of M3.5 work.

**Out of scope for this milestone.**

- **Sub-builder / mid-chain extension** (e.g. extending `ScalarFieldBuilder` so packs can contribute `.encrypted()` at chain positions). Different type-system shape; would be the *attribute* contribution surface in the entity / attribute / relationship vocabulary; deferred until a use case demands it.
- **Backport of the descriptor + factory shape to `field` and `type` namespaces.** Mechanical follow-up tracked separately as [TML-2513](https://linear.app/prisma-company/issue/TML-2513).
- **Postgres-domain entities** (RLS policies, roles, views). Real-world consumers of this mechanism, but explicitly out of scope per spec; ship in a follow-on project on the proven mechanism.

**Validation gate** (must all pass for SATISFIED):

- `pnpm typecheck` (workspace-wide; the lift of `composed-authoring-helpers.ts` is a heavy-generics relocation and must not regress type narrowing for any in-tree consumer).
- `pnpm lint:deps` (no new layering violations; the lifted scaffolding lives at family-agnostic layer without circular deps).
- Filtered tests on the SQL + Mongo contract-builder packages: `pnpm --filter '@prisma-next/sql-contract-ts' --filter '@prisma-next/mongo-contract-ts' --filter '@prisma-next/contract-authoring' test`.
- The pack-side wiring exemplar's tests pass: type-level narrowing works (the entity helper appears at `helpers.entities.<name>` with the contributed signature), runtime construction works (the call lowers to an IR-class instance via the supplied factory), round-trip works (the constructed instance serializes back to canonical JSON via the existing `ContractSerializer` SPI).

**Acceptance:** spec.md § "Authoring surface" describes the unified contribution shape and the `entities` namespace; the lifted scaffolding lives at family-agnostic layer; both family contract-builders consume it; one in-tree pack contributes an entity via the new mechanism with full type narrowing and runtime correctness.

### M4 — Enum exemplar

**Status: done** (R1a commits `3cd51acff` → `fb72efe5f`; R1b commits `cbc415ba7`, `818993d81`, `00dbbd732`, `396b167a8`; R1c cleanup commits `ae84dcb01`, `f14a1bfbe`, `c4458221e`). AC1 (verifier+planner walk `SqlEnumType` natively) and AC2 (codec-hook glue removed; mechanism-consumption check holds via PSL-runtime diagnostic per the wording amendment at `a75d8e2b2`) both satisfied. Reviewer report SATISFIED with three non-blockers, all addressed: AC2 wording amended; `enumChangeCallStrategy` collapsed into `nativeEnumPlanCallStrategy` (smell removed); `contract-ts` README doc-rot fixed. Strategy collapse surfaced a load-bearing architectural insight on per-emission `recipe`-vs-`dep` flag dispatch (recorded in `learnings.md`). Decision 18 Option B (polymorphic `storage.types`) verified on disk — only the one enum-bearing parity fixture (`core-surface`) gained envelope keys; codec-typed entries' envelopes byte-identical to pre-M4. Decision 19 (TS surface `type.enum` → `entities.enum`) verified on disk in production code + docs.

**Goal:** prove the new IR pattern works end-to-end on a real existing concept by lifting enums from codec-hook glue into first-class IR nodes. **First real consumer of the M3.5 mechanism**: the Postgres pack contributes the enum entity via `helpers.entities.enum(...)` rather than via a hand-edited family-layer factory site.

**Storage shape — Decision 18 (Option B, polymorphic `storage.types`).** `Contract.storage.types[name]` becomes a polymorphic value: `StorageTypeInstance | PostgresEnumType`. Enum entries carry literal `kind: 'sql-enum-type'`; codec-typed entries (pgvector, decimal, varchar) inherit the family base's non-enumerable `kind` per Decision 16 — JSON envelope unchanged for those entries. The hydration walker dispatches on shape/kind. Verifier + planner walk `SqlEnumType` instances natively; codec-typed entries continue through codec-hook dispatch unchanged. Rationale + alternatives + reversibility recorded in `wip/unattended-decisions.md § 18`.

**Schema IR scope — Decision 18 (Contract IR only).** Schema IR enum representation stays annotation-shaped (`schema.annotations.pg.storageTypes`). The new `SqlSchemaVerifierBase` enum walk carries a small bridging adapter from the annotation shape to a normalised view for the diff. Schema IR enum lift deferred (tracked open item below). Introspector also stays codec-hook-shaped in M4; lifts when Schema IR enum representation lifts.

**Tasks:**

- [ ] Declare `abstract class SqlEnumType extends SqlNode` (family layer). First family-side abstract IR class earned by polymorphic-dispatch consumers (verifier walks enum types separately).
- [ ] Declare `class PostgresEnumType extends SqlEnumType` (target layer); Postgres-specific `CREATE TYPE` rendering, native-type-name resolution; `kind = 'sql-enum-type'` literal for the polymorphic dispatch surface.
- [ ] Contribute the enum entity through the M3.5 `entities` namespace on the Postgres **target** pack ref (descriptor + factory co-located with the IR class at `packages/3-targets/3-targets/postgres/src/core/authoring.ts`; registered via `packages/3-targets/3-targets/postgres/src/core/descriptor-meta.ts`). The enum concept is target-specific (`CREATE TYPE … AS ENUM` is Postgres syntax; SQLite emulates via CHECK constraints) — adapters lower, targets carry concepts. The factory constructs `PostgresEnumType` instances; the descriptor lowers existing enum syntax through the new path. Validation: removing the contribution causes the existing enum tests to fail with the expected "no entity helper named `enum`" type error. *Spec-vs-code drift correction (PR1 review B3): an earlier plan revision named the adapter pack ref; the architecturally-correct site is the target pack and the implementation lives there.*
- [ ] Hydration walker: when deserializing `Contract.storage.types[name]`, dispatch on shape/kind (literal `kind: 'sql-enum-type'` → `PostgresEnumType` instance via the pack factory; otherwise → existing `StorageTypeInstance` codec-typed path). Per-target serializer's `serializeContract` covers the inverse on the way out.
- [ ] Verifier walks `SqlEnumType` instances natively via the per-SPI verifier with a small bridging adapter to the existing annotation-shape Schema IR. `pgEnumControlHooks.verifyType` is deleted.
- [ ] Planner consumes `SqlEnumType` IR nodes natively via the existing `Op` builder functions in `packages/3-targets/3-targets/postgres/src/core/migrations/operations/enums.ts` (these stay as plain `Op` builders — promoting them to `OpFactoryCall` AST classes per ADR 195 is a separable refactor not in M4 scope). `pgEnumControlHooks.planTypeOperations` is deleted; pure helpers (pg_enum array parsing, `buildCreateEnumOperation`, `buildAddValueOperations`, `buildRecreateEnumOperation`) lift verbatim into the planner integration site.
- [ ] Authoring DSL preserved: existing PSL `enum Status { … }` and TS builder enum syntax both lower internally to `helpers.entities.enum(...)`; user-facing surface unchanged.
- [ ] `pgEnumControlHooks` registration is deleted from `packages/3-targets/6-adapters/postgres/src/core/descriptor-meta.ts`. The introspector (`introspectTypes`) stays in M4 — it remains codec-hook-shaped because Schema IR enum representation stays annotation-shaped (Decision 18 Q4).
- [ ] AC2 verified: enum tests pass without semantic change; codec-hook glue visibly removed.

**Validation gate** (must all pass for SATISFIED):

- `pnpm lint:deps` (no new layering violations).
- `pnpm typecheck` workspace-wide (standing implementer gate per the M3.5 R1b learning).
- Filtered Postgres + SQL test gate: `pnpm --filter '@prisma-next/target-postgres' --filter '@prisma-next/adapter-postgres' --filter '@prisma-next/family-sql' --filter '@prisma-next/sql*' test`. (Note: package is `adapter-postgres`, not `extension-postgres`.)
- Mongo regression sweep: `pnpm -F './packages/2-mongo-family/**' -F './packages/3-mongo-target/**' test`.
- Filtered SQLite test gate: `pnpm --filter '@prisma-next/target-sqlite' test` (verify SQLite enum-emulation via CHECK constraints stays untouched).
- Integration tests: `pnpm test:integration` (PGlite-flake re-runs allowed per documented pattern).
- AC2 grep guard: `rg "pgEnumControlHooks|PG_ENUM_CODEC_ID" packages/3-targets/6-adapters/postgres/src/core/descriptor-meta.ts` returns zero hits (or only deleted-import lines). The `verify-sql-schema.ts` enum-branch grep guard is satisfied today (zero hits) and needs no follow-up.
- Mechanism-consumption check: removing the `entities.enum` contribution from the Postgres pack causes the PSL interpreter to emit one missing-helper diagnostic per enum declaration (paraphrasing: "Enum \"X\" requires the active target pack to contribute an entities.enum helper"). Verify by `git stash` of the contribution + running the PSL parity case. The original wording promised a TypeScript "no entity helper named `enum`" type error; that variant doesn't fire in-tree because no TS consumer types directly against `helpers.entities.enum` (PSL is the only consumer and dispatches via string lookup through `instantiateAuthoringEntity`). The PSL-runtime diagnostic is the load-bearing signal; PSL parity tests carry the regression net.

### M5a — Namespace exemplar (new concept) + authoring DSL

**Status: deferred to PR2 ([TML-2520](https://linear.app/prisma-company/issue/TML-2520) / TML-2472 M5a).** See § Delivery sequencing at the top of this plan for rationale. Scope, tasks, and validation gates below remain canonical for the PR2 implementer.

**Goal:** introduce the higher-risk new framework-level concept on a foundation that is now well-exercised, and ship the authoring-DSL surface that makes namespaces usable in PSL and the TS builder. **Second consumer of the M3.5 mechanism**: each target pack with native namespacing (`postgres`, `mongo`) contributes its Namespace concretion as an entity via `helpers.namespace(...)` (TS); the framework parser ships the `namespace { … }` PSL block (target-agnostic syntax) and each family's existing PSL interpreter handles target-specific lowering. The greenfield concept exercises the mechanism's "introduce a new framework-level kind" path (where M4 exercises the "lift existing target-specific glue" path).

**Pre-implementation reconnaissance (recorded during PR2 shaping).** A consumer-cascade probe verified the plan is implementable on the existing substrate. Findings:

- **Framework parser** at [`packages/1-framework/2-authoring/psl-parser/src/parser.ts`](../../packages/1-framework/2-authoring/psl-parser/src/parser.ts) is line-regex-based, hardcoded for top-level keywords (`model`, `enum`, `type`, `types`). New top-level keywords require editing the framework parser; no pack-contributed top-level block mechanism exists today. `namespace { … }` joins the hardcoded set in PR2 — a framework concern, not a per-target one. (Target-contributed top-level PSL blocks are out of scope; see Non-goals.)
- **Dotted field types** are partially supported today. The type-constructor regex (`parser.ts:494`) already accepts dot-separated identifiers; the bare-type-fallback regex (`parser.ts:668`) does not. One-regex extension makes `auth.User` parseable. `PslField.typeName` already stores `string` (dot-joined when constructed via the type-constructor path), so the AST shape is unchanged.
- **PSL printer** at `packages/1-framework/2-authoring/psl-printer/src/` emits `field.typeName` verbatim — dotted typeNames round-trip without changes (verified at `serialize-print-document.ts:135`). The printer needs one new emission case for `PslNamespace` blocks (mirrors the existing `PslEnum` block emission), estimated at ~40–60 LOC across `ast-to-print-document.ts` and `serialize-print-document.ts`.
- **M3.5 entity-contribution mechanism** is end-to-end-verified by [`packages/2-sql/2-authoring/contract-ts/test/entities-namespace.exemplar.test.ts`](../../packages/2-sql/2-authoring/contract-ts/test/entities-namespace.exemplar.test.ts). PR2 contributes `helpers.namespace(...)` via the Postgres pack's `entityTypes.namespace` descriptor — structurally identical to how M4 contributed `helpers.enum(...)`. No new contribution-surface infrastructure required.
- **Per-family PSL interpreter dispatch surfaces** are at `packages/2-sql/2-authoring/contract-psl/src/interpreter.ts` (SQL family — handles Postgres and SQLite; per-target branching reads from the existing `entityContext`) and `packages/2-mongo-family/2-authoring/contract-psl/src/psl-helpers.ts` (Mongo family). Both already have access to the active pack via `entityContext`; per-target namespace-lowering dispatch is implemented within the interpreter body, not via new SPI surface.
- **Mongo namespace concretion** at [`packages/3-mongo-target/1-mongo-target/src/core/mongo-target-database.ts`](../../packages/3-mongo-target/1-mongo-target/src/core/mongo-target-database.ts) already implements the right structural shape: `MongoTargetDatabase` carries `qualifier()` / `qualifyCollection()` methods, and `MongoTargetUnspecifiedDatabase` is a singleton subclass that overrides both to elide the prefix. M5a's Mongo work is rename + docstring refresh — no behavioural changes.
- **SQL family placeholder** `SqlUnspecifiedNamespace` at [`packages/2-sql/1-core/contract/src/ir/sql-unspecified-namespace.ts`](../../packages/2-sql/1-core/contract/src/ir/sql-unspecified-namespace.ts) is a family-shared singleton from PR1 M9; obsoleted by per-target concretions (`PostgresUnboundSchema`, `SqliteUnboundDatabase`). M5a removes it (or repurposes its file location for `SqlUnboundNamespace` as a typed slot if the per-target concretions want a family-shared abstract).
- **`sqlSchemaIrToPslAst` synthesizer** at [`packages/2-sql/9-family/src/core/psl-contract-infer/sql-schema-ir-to-psl-ast.ts`](../../packages/2-sql/9-family/src/core/psl-contract-infer/sql-schema-ir-to-psl-ast.ts) currently produces a flat AST from a flat `SqlSchemaIR.tables`. Under Reading D, both the IR (FR15 nested map) and the AST (FR16a `namespaces`-only shape) become nested; the synthesizer must group inferred tables by namespace coordinate and emit `namespace { … }` blocks for non-`public` schemas. The synthesizer change lands in M5b (alongside the cross-namespace FK work that also touches the synthesizer's relation-rendering path).

The probe surfaces no architectural surprises. The plan as written is implementable on the existing substrate with the per-task work itemised below.

**Tasks:**

- [ ] **Task 1 — Mechanical rename `__unspecified__` → `__unbound__` at the IR layer (PR2 prerequisite, single focused commit).** Renames per FR16d: framework constant `UNSPECIFIED_NAMESPACE_ID` → `UNBOUND_NAMESPACE_ID`; literal `'__unspecified__'` → `'__unbound__'` at every IR call site and JSON envelope key; class `SqlUnspecifiedNamespace` → `SqlUnboundNamespace` (the family-layer placeholder from PR1 M9); class `MongoTargetUnspecifiedDatabase` → `MongoTargetUnboundDatabase` (already on disk from M2); ADR drafts under `projects/target-extensible-ir/specs/` (`3-layer-polymorphic-ir-convention.md`, `architectural-principles.md`) and any other in-tree documentation that names the literal. **Do not rename the PSL AST identifier `__unspecified__`** — that constant names the framework parser's syntactic-absence bucket and is a different concept (see FR14, FR16a). Workspace `pnpm typecheck` + `pnpm test:packages` must pass after the rename commit; subsequent tasks build on the renamed surface.
- [ ] **Task 2 — Pre-flight PSL consumer cascade audit.** Before lowering the PSL grammar, enumerate everywhere the existing flat-model AST (`PslDocumentAst.models`, `PslDocumentAst.enums`, …) is consumed downstream — emitter passes, formatter, code-completion, validation, doc tooling, `sqlSchemaIrToPslAst` synthesis path. Confirm which consumers can absorb the collapse-into-namespaces shape (FR16a Reading D) and which need to be taught namespace-awareness. The audit's output is a small table that informs the rest of the milestone's task scope — and prevents shipping a grammar feature that silently breaks downstream tooling.
- [ ] **Framework AST shape (Reading D, FR16a):** `PslDocumentAst.namespaces: readonly PslNamespace[]` becomes the only models container; `PslDocumentAst.models` / `.enums` / `.compositeTypes` / `.types` slots collapse into the nested-by-namespace shape. Add `PslNamespace { kind: 'namespace', name: string, models: …, enums: …, span }`. Top-level (no-block) declarations are collected by the parser into an implicit `PslNamespace { name: '__unspecified__', … }` entry (the parser's framework constant for the syntactic-absence bucket). Reserved-identifier policy: the framework parser does **not** reserve `unbound` — reservation is per-target interpreter (see FR16c).
- [ ] **Framework parser:** add the top-level `namespace <name> { … }` block parser (mirrors `parseModelBlock`); extend the simple-type-fallback regex (`parseField` at `packages/1-framework/2-authoring/psl-parser/src/parser.ts:668`) from `^([A-Za-z_]\w*)$` to `^([A-Za-z_]\w*(?:\.[A-Za-z_]\w*)*)$` so bare dotted typeNames like `auth.User` parse (the type-constructor path already accepts dots; this brings the bare-type fallback in line — see FR16b). Namespace blocks reopen-merge (multiple blocks for the same name produce one AST entry); recursive nesting is a parse error.
- [ ] **Framework printer:** add `PslNamespace` block emission (mirrors `PslEnum` block emission in `packages/1-framework/2-authoring/psl-printer/src/ast-to-print-document.ts` and `serialize-print-document.ts`). The printer already emits `field.typeName` verbatim so dotted typeNames round-trip without additional changes.
- [ ] **Target concretions:** declare `class PostgresSchema extends NamespaceBase` (target layer); search-path semantics, owner role, SQL rendering as `"<schema>"`. Declare `class PostgresUnboundSchema extends PostgresSchema` (singleton, `readonly id = UNBOUND_NAMESPACE_ID as const`); overrides qualifier-emission methods to elide the `"<schema>".` prefix. Stable static reference `PostgresSchema.unbound`. Same singleton pattern for SQLite, but SQLite's interpreter rejects all explicit namespace blocks — only the implicit `__unspecified__` AST entry reaches the SQLite interpreter, which lowers it to the singleton.
- [ ] **Postgres storage** gains `namespaces` keyed by namespace id (nested-map storage shape per FR15: `tables: Record<NamespaceId, Record<TableName, StorageTable>>`, and analogously for types / functions). Default population: `{ [UNBOUND_NAMESPACE_ID]: PostgresSchema.unbound, ['public']: <PostgresSchema 'public'> }` — the `public` schema is materialised because the Postgres interpreter lowers the implicit `__unspecified__` AST entry to it (FR16, FR16c). The Mongo equivalent landed in M2; M5a renames the Mongo placeholder class per Task 1 but adds no new Mongo behaviour.
- [ ] **Re-key every storage object** (table, enum, function, …) by `(namespace.id, name)` rather than `name` alone. Verifier walks two parallel trees of namespace-scoped objects. (FKs are still single-namespace at this milestone — M5b adds the cross-namespace FK shape.)
- [ ] **Per-target PSL → IR interpreter dispatch (FR16c).** Each family's existing PSL interpreter (`packages/2-sql/2-authoring/contract-psl/` for SQL; `packages/2-mongo-family/2-authoring/contract-psl/` for Mongo) gains namespace-aware lowering per the dispatch table in FR16c. Postgres: implicit `__unspecified__` → IR `public`; explicit `unbound` → IR `__unbound__`; named → IR named schemas; user-declared namespaces named `unbound` are rejected (the identifier is reserved by Postgres's interpreter for the late-binding sentinel). SQLite + Mongo: accept only the implicit `__unspecified__` AST entry; reject all explicit `namespace { … }` blocks with a diagnostic naming the target and pointing at the offending block span. Reopen-merge across implicit + explicit slots (FR16c).
- [ ] **Authoring DSL — TS builder:** top-level `namespaces` declaration list + per-model `namespace` field; defaulting to the implicit `__unspecified__` AST bucket when omitted (FR16a). Defensive validation rejects user-declared namespaces named `__unbound__` (the IR sentinel) and (Postgres only) `unbound` (the PSL keyword reserved by Postgres's interpreter).
- [ ] **Multi-tenancy via `namespace unbound { … }`** end-to-end via connection `search_path` (AC6). Single-tenant Postgres contracts (top-level models, no `namespace { … }` block) continue to map to the IR `public` slot and emit qualified DDL — verified by ensuring existing single-namespace tests continue to pass without modification.
- [ ] **Existing single-namespace contracts migrate mechanically** (FR16): Postgres contracts with top-level models continue to work; the Postgres interpreter lowers the implicit `__unspecified__` AST entry to the IR `public` slot (qualified DDL — slight cosmetic change from today's unqualified DDL, but semantically equivalent in single-tenant environments). SQLite contracts continue to work via the singleton slot. Mongo contracts continue to work via the connection-`db` slot. No PSL rewrite required for any existing contract.

**Validation:** ACs 4a, 5, 6, 6a, 6b pass. Multi-namespace + multi-tenancy integration tests green. AC4 (cross-namespace FK demonstration) is *not* yet green at this milestone — it lands in M5b.

### M5b — Cross-namespace FK references + ForeignKey IR restructuring

**Status: deferred to PR2 ([TML-2520](https://linear.app/prisma-company/issue/TML-2520) / TML-2473).** See § Delivery sequencing for rationale.

**Goal:** complete the namespace story by adding cross-namespace FK references within a single contract space — the FL-02 fix that unblocks Supabase's `auth.users` story. Concurrent FK IR restructuring (source-vs-target separation; `ForeignKeyReferences` → `ForeignKeyReference` rename) lands in the same milestone because the cross-namespace work touches the same IR surface.

**Tasks:**

- [ ] **ForeignKey IR restructuring (FR16b).** Rename `ForeignKeyReferences` (plural) → `ForeignKeyReference` (singular) — each FK has exactly one reference target, the plural was misleading. Rename `ForeignKey.references` (plural) → `ForeignKey.reference` (singular). Restructure into clean source-vs-target shape: `ForeignKey { source: ForeignKeySource, target: ForeignKeyTarget, … }` where `ForeignKeySource = { columns: string[] }` and `ForeignKeyTarget = { namespaceId: string, table: string, columns: string[] }`. FK-level metadata (`constraint`, `index`, `name`, `onDelete`, `onUpdate`) stays at the `ForeignKey` level. Mechanical rename + restructure; full test suite + lint passes after the commit.
- [ ] **Cross-namespace FK target carries a namespace coordinate** (the source side does not — the source is always the table the FK is declared on, which already has its namespace from the storage shape). `ForeignKeyTarget.namespaceId` populated for named-schema targets; populated with `UNBOUND_NAMESPACE_ID` for late-bound targets (deliberate user opt-in via PSL `unbound.<Model>`). **Design discipline (FR16b extension-point note):** do not fuse `namespaceId` and `table` into a single composite key; keep them addressable separately so cross-*contract-space* refs can extend the shape additively to `(spaceId, namespaceId, table)` in a follow-up project without restructuring the IR.
- [ ] **Verifier** dispatches on `(namespace.id, name)` for both ends of an FK; walks across namespaces correctly. For the IR `__unbound__` slot on either end, the verifier resolves to `current_schema()` (Postgres) at entry and then proceeds with concrete-keyed dispatch — diagnostics surface the resolved schema name, not the sentinel.
- [ ] **Planner-DDL builder** emits qualified `REFERENCES "<schema>"."<table>"("<col>")` clauses for named-schema targets and unqualified `REFERENCES "<table>"("<col>")` clauses for the IR `__unbound__` target (search_path resolves at migration time). Symmetric with `__unbound__` table-creation DDL.
- [ ] **Authoring DSL — TS builder:** existing FK call sites (`constraints.foreignKey(cols.x, OtherModel.refs.y, …)` in the SQL-block constraints DSL and `rel.belongsTo(OtherModel, …)` in the relations DSL) lower to cross-namespace IR automatically when `OtherModel`'s namespace differs from the referencing model's — the model handle carries the namespace coordinate, no new syntax (FR16b).
- [ ] **Authoring DSL — PSL:** dot-qualified type references in the existing `@relation` mechanism (`user auth.User @relation(fields: [userId], references: [id])`). No new attribute; the namespace coordinate is carried by the **type position**, not in the `references:` list. The framework parser changes (M5a) already accept dotted field types; M5b adds the per-target interpreter logic that resolves `auth.User` → namespace `auth`, model `User` and populates the FK target accordingly.
- [ ] **`sqlSchemaIrToPslAst` synthesizer:** when emitting a PSL field for an inferred relation, qualify `typeName` with the `<namespace>.` prefix when the FK target table lives in a different namespace from the declaring model. Emit `namespace { … }` blocks at the document level for non-default schemas. Default schema (`public`) emission preserves today's top-level shape (no `namespace public { … }` wrapper) for round-trip-friendly inferred output.
- [ ] **Integration test (PGlite):** a Postgres contract with `public.profiles.user_id REFERENCES auth.users(id)` migrates, emits, and verifies end-to-end (FL-02 scenario).

**Validation:** AC4 (cross-namespace FK) passes. FL-02 acceptance demonstrated via PGlite integration test. The Supabase `auth.users` reference shape is now expressible in Prisma Next contracts. The `ForeignKey` IR shape is the post-rename / post-restructure shape across all tests, fixtures, and downstream consumers.

### M6 — Documentation + ADR drafts (PR1 scope)

**Goal:** lift the IR convention + entities mechanism out of the project workspace into durable repo docs so they outlive PR1's branch and are reviewable as part of PR1 (rather than waiting for the project's eventual close-out after PR2).

**Scope split between PR1 + PR2.** M6 in PR1 covers everything that's anchored on the M1-M4 + M3.5 surface: the IR convention (3-layer polymorphic class hierarchy), the entities mechanism, the architectural principles ("framework provides affordances; targets implement specifics"), the storage shape decisions (Decision 18 polymorphic `storage.types`), and the TS-surface evolution to the canonical `helpers.entities.<name>(...)` shape (Decision 19). **Namespace-specific ADRs and any namespace-driven subsystem-doc updates land in PR2's docs sweep**, not here.

**Tasks (PR1):**

- [ ] Update `AGENTS.md` / `CLAUDE.md` Golden Rule on "Interface-Based Design" to split along the service-vs-AST/IR axis described in the spec § "Codifying the convention" (FR21). Service interfaces (factory-pattern) vs AST/IR class hierarchies (3-layer polymorphic) are now distinct conventions; the rule needs both.
- [ ] Add the AST/IR class-hierarchy section to `docs/reference/typescript-patterns.md` as a sibling to "Interface-Based Design with Factory Functions" (FR22). Include the `freezeNode(this)` convention (Decision 8), the `kind` discriminator strategy (Decisions 15/16: optional framework-level + per-leaf enumerable when polymorphic dispatch earns it), and the SPI dispatch shape (`hydrateEnumType`-style hooks; per-target subclass override).
- [ ] Update `docs/Architecture Overview.md` § "Guiding Principles" to surface "framework provides affordances; targets implement specifics" and "familiar with one target, fluent in another" (FR23). The polymorphic IR + entities mechanism are the two structural reifications of these principles; cite both.
- [ ] Update affected subsystem docs to reflect the class-hierarchy IR shape (FR24): **Data Contract** (polymorphic `storage.types`; per-target IR subclasses; serializer SPI `hydrateEnumType` hook), **Contract Emitter & Types** (per-target `serializeContract` owns the on-disk JSON envelope; runtime-only fields stay enumerable on instances + are elided by the serializer), **Adapters & Targets** (the SPI dispatch surface; `helpers.entities.<name>(...)` contribution path; codec-hook removal pattern). Namespace-related subsystem updates wait for PR2.
- [ ] Promote ADR drafts under `projects/target-extensible-ir/specs/` to "ready to merge" state, but **do not move them to `docs/architecture docs/adrs/`** — that's the project close-out step after PR2. ADRs anchored on PR1's surface that need to exist by PR1's M6 close-out (per AC10 + FR25):
  - 3-layer polymorphic IR convention (M1 draft);
  - Architectural principles underwriting it (M1 draft);
  - Target-extensible authoring / `entities` contributions (M3.5 draft);
  - Storage shape for first-class IR vs codec-typed entries — Decision 18 Option B (M4-derived; new ADR if not yet drafted).
- [ ] Audit the spec.md for forward-references to namespaces / M5a / M5b that PR1 readers shouldn't take as "this PR ships that"; either soften the wording or add a "shipped in PR2" callout. The spec stays canonical for both PRs (per § Delivery sequencing); the audit is just to keep PR1 reviewers from looking for the namespace work in this PR's diff.

**Validation:** docs reviewable as diffs; ADR drafts ready for promotion at the eventual project close-out (post-PR2).

### M7 — PR1 review fixes (registry-driven hydration + entityTypes rename)

**Goal:** close two architectural defects surfaced by the PR1 review (architect SDR § 3.3 + § 3.1; principal-engineer F06 + F09 framings) before PR1 merges. The defects: (1) `SqlContractSerializerBase` carries privileged knowledge of enums (imports `SqlEnumType`, dispatches on `kind === 'sql-enum-type'`, declares a per-kind `hydrateEnumType` SPI hook with a throwing default) — a duck-typed family-layer special-case that bypasses the M3.5 entities mechanism the project shipped to eliminate exactly this smell; (2) the contribution-surface naming `AuthoringContributions.entities` overloads the word "entity" in three meanings (visualisation discriminant; contribution-kind namespace; application-domain instance) — fresh contributors reach first for the application-domain meaning. **Both fixes land in PR1; the PR cannot tell two contradictory stories about how target-specific entities enter the system.**

**Tasks:**

- [ ] **R1 — doc-only round (in-session, before code):** update spec FR8a wording to describe the registry-driven deserialization shape; add new FR8e ("the family-layer deserializer dispatches through a contribution-driven registry; no per-kind hooks on family bases"); rename `AuthoringContributions.entities` → `AuthoringContributions.entityTypes` throughout the spec; clarify the asymmetric naming convention (helpers surface stays `helpers.entities.<name>(input)`); update the four ADR drafts (`target-extensible-authoring.md`, `polymorphic-storage-types.md`, `3-layer-polymorphic-ir-convention.md`); update plan M3.5 + M4 task wording where it references `entities` at the contribution surface; update PR description (`wip/pr-499-body.md`); commit.
- [ ] **R2 — mechanical rename `entities` → `entityTypes` at contribution surface (code).** Touches: framework-authoring type definitions (`AuthoringContributions.entities` → `entityTypes`; `AuthoringEntityNamespace` → `AuthoringEntityTypeNamespace`; `AuthoringEntityDescriptor` → `AuthoringEntityTypeDescriptor`; `AuthoringEntityFactoryOutput` → `AuthoringEntityTypeFactoryOutput`; `instantiateAuthoringEntity` → `instantiateAuthoringEntityType`; `isAuthoringEntityDescriptor` → `isAuthoringEntityTypeDescriptor`); both family contract-builders that consume `AssembledAuthoringContributions.entityTypes`; the lifted scaffolding; Postgres pack literal (`postgresAuthoringEntities` → `postgresAuthoringEntityTypes`); fixture `entities: {}` → `entityTypes: {}`; synthetic exemplar test imports/usages. **Helpers surface stays `helpers.entities.<name>(input)`** — the user constructs entities; they don't construct entity types. The composed-helpers code bridges contribution `entityTypes` → user-facing `entities` (asymmetric mapping; one place in code). Mechanical change; passes typecheck and full test suite; commit.
- [ ] **R3 — registry-driven deserialization (substantive refactor).** Add explicit `discriminator: string` field to `AuthoringEntityTypeDescriptor` (declared at the contribution site; reads alongside `kind: 'entity'` and `output`). Deletes `SqlContractSerializerBase.hydrateEnumType` (with throwing default) and the per-kind dispatch in `hydrateStorageTypeEntry`; deletes the `SqlEnumType` import from the family base. Replaces with a generic registry-dispatch shape: the family base accepts a `ReadonlyMap<string, EntityHydrationFactory>` in its constructor and `hydrateStorageTypeEntry` looks up the entry's `kind` discriminator in the registry and calls the contributed factory. The Postgres descriptor builds the registry from the resolved `postgresAuthoringEntityTypes` contributions (same factories that handle authoring-time construction; same shape) and passes it to the `PostgresContractSerializer` constructor. Deletes `PostgresContractSerializer.hydrateEnumType` override (no longer exists). Adds discriminator `'sql-enum-type'` to the Postgres `entityTypes.enum` descriptor. Validates: full typecheck + lint:deps + workspace test suite; new test exercises the registry-dispatch path explicitly (a synthetic "fake-entity" pack contribution registers a discriminator, the deserializer routes through it). Commit.
- [ ] **R4 — review artefact refresh.** Update `projects/target-extensible-ir/reviews/pr-499/{system-design-review,code-review,walkthrough}.md` to mark B1's typology hole + F06's type-erasure cast as addressed (or downgrade to "follow-up only" if the registry-driven shape doesn't fully eliminate them). Update PR description to mention M7 in the "How it fits together" sequencing.

**Validation:** workspace `pnpm typecheck`, `pnpm lint:deps`, `pnpm test:packages`, and the integration test suite all pass. Reviewer round confirms the family base no longer imports any per-kind IR class and no `hydrateXxx` per-kind hooks remain on the family base. The exemplar-test still demonstrates type narrowing + runtime construction + JSON-cleanliness; the new registry-dispatch test demonstrates the family base routing to a contributed factory by discriminator.

**Round structure note.** R1 (docs) executes in-session by the orchestrator; R2 + R3 (code) delegate to an implementer subagent (one round, two commits inside it for review-clarity). R4 (review-artefact refresh) executes in-session.

### M8 — PR1 review findings sweep (principal-engineer findings + GH-thread comments)

**Goal:** close every PR1 review finding that has not already been folded into M7. PR1 cannot ship with the architecturally-flagged issues (asymmetric `Storage` adoption; `SchemaNode` naming; CLI per-target branching; example apps reaching past the façade; dead `validateContract` exports; transient project IDs in source) still on disk. Each finding has a concrete close; the milestone is mechanical-and-tactical, not architectural.

**Sources:**
- Principal-engineer pass: [`projects/target-extensible-ir/reviews/pr-499/code-review.md`](reviews/pr-499/code-review.md) §§ F01–F12.
- Architect pass: [`projects/target-extensible-ir/reviews/pr-499/system-design-review.md`](reviews/pr-499/system-design-review.md) §§ B1–B3, 3.1–3.6.
- GitHub PR #499 comments (13 inline threads opened by the user; all on PR1 surfaces).

**Round structure.** Three implementer rounds (R1–R3 below). R0 (this commit) updates plan + spec docs ahead of code work. R4 closes by refreshing the review artefacts and resolving GH threads.

#### R1 — mechanical / low-process fixes (one commit)

Single focused commit: `chore(target-extensible-ir): PR1 review-finding mechanical fixes (F03, F05, F07, F10–F12, transient IDs, sentinel const, SerializeContract rename)`.

- **F03** — `MongoTargetStorage`: replace `Object.freeze(this)` with `freezeNode(this)` ([`packages/3-mongo-target/1-mongo-target/src/core/mongo-target-storage.ts`](../../packages/3-mongo-target/1-mongo-target/src/core/mongo-target-storage.ts)). Audit `rg "Object\.freeze\(this\)" packages/` after the fix to confirm no other `SchemaNodeBase`-/`IRNodeBase`-derived class drifted.
- **F05a** — amend [`packages/2-sql/1-core/contract/src/ir/sql-enum-type.ts (L44–L47)`](../../packages/2-sql/1-core/contract/src/ir/sql-enum-type.ts) docstring to acknowledge per-target additions ("concrete subclasses may add additional enumerable own properties (e.g. `PostgresEnumType.codecId`) when the persisted envelope demands them").
- **F05b** — add convention-naming comment inside `SqlContractSerializerBase.serializeContract` default ("Targets that ship enumerable runtime-only fields must override this method (mirroring `MongoTargetContractSerializer.serializeContract`)").
- **F07** — add inline `as unknown as JsonObject` justification at `MongoTargetContractSerializer.serializeContract` line 58 (mirror M2 R2 F3 closure pattern).
- **F10** — add inline justification at `PostgresEnumType` line 61's `as unknown as TValues` cast.
- **F11** — defensive assertion at `nativeEnumPlanCallStrategy` boundary so the "introduce + rebuild different enums in one plan" failure surfaces as a planner-time error rather than a downstream PG runtime error.
- **F12** — refresh the `postgresPlannerStrategies` array description block at lines 673–683 to name the collapsed-strategy shape (`enumChangeCallStrategy` is gone).
- **GH#10** — drop transient project IDs (M4, M5a) from [`packages/1-framework/1-core/framework-components/src/shared/framework-authoring.ts (L103–L107)`](../../packages/1-framework/1-core/framework-components/src/shared/framework-authoring.ts) — replace with the durable shape description (per `.cursor/rules/doc-maintenance.mdc`).
- **GH#7** — encode `'__unspecified__'` as an exported const at the framework Namespace seam ([`packages/1-framework/1-core/framework-components/src/ir/namespace.ts`](../../packages/1-framework/1-core/framework-components/src/ir/namespace.ts)). Suggested name: `UNSPECIFIED_NAMESPACE_ID`. Switch all literal usages (Mongo target singletons + tests; future Postgres singleton in PR2) to import the const. Minimum viable: export the const + switch the in-tree singleton constructors + the framework Namespace docstring; tests can stay literal until they need refactoring for other reasons.
- **GH#4** — rename `CanonicalSerializeContract` → `SerializeContract` workspace-wide. The `Canonical` prefix is misleading: this hook is the per-target JSON-conversion seam, not a canonicalization routine. Touches `packages/1-framework/0-foundation/contract/src/canonicalization.ts` (and rename `canonicalization.ts` → `serialize-contract.ts` if the file becomes single-purpose after F08); the type definition; the `EmitOptions.serializeContract` binding; the contract-emit CLI op import.
- **GH#5** — review `canonicalizeContractToObject`'s public export. If only `canonicalizeContract` (the JSON-stringifying variant) is used outside the file, drop the `to-object` export; if both are needed externally, document the export-pair with a one-line justifying comment.

**Validation:** workspace `pnpm typecheck`, `pnpm lint:deps`, `pnpm test:packages` — all green.

#### R2 — `SchemaNode` → `IRNode` rename + Storage interface participation

Single focused commit: `refactor(framework): rename SchemaNode → IRNode; Storage extends IRNode (workspace-wide)`.

- **GH#8** — `SchemaNode` and `SchemaNodeBase` are the common root for *both* Contract IR and Schema IR class hierarchies. The "Schema" prefix is misleading — fresh readers infer Schema-IR-only. Rename:
  - `interface SchemaNode` → `interface IRNode`
  - `abstract class SchemaNodeBase` → `abstract class IRNodeBase`
  - `freezeNode<T extends SchemaNode>` → `freezeNode<T extends IRNode>` (the helper name stays — it operates on IR nodes regardless of root naming)
  - File rename: `schema-node.ts` → `ir-node.ts`
  - Workspace-wide cascade: every `extends SchemaNodeBase` becomes `extends IRNodeBase`; every `import { SchemaNode, SchemaNodeBase, freezeNode }` updates; every docstring referencing `SchemaNodeBase` (notably `learnings.md`, `AGENTS.md`, the four ADR drafts, the M6 `typescript-patterns.md` and `Architecture Overview.md` updates) updates.
- **GH#9** — `interface Storage` does not extend the IR node alphabet today, but `MongoStorageBase implements Storage` and `MongoStorageBase extends IRNodeBase` — the structural dual already holds. Tighten: declare `interface Storage extends IRNode { readonly namespaces: Readonly<Record<string, Namespace>> }`. Storage is then a typed-IR-node interface, not an unprefixed structural promise; the dispatch surface (a SchemaVerifier walks `IRNode`-typed slots) gains correct typing.

**Validation:** workspace `pnpm typecheck`, `pnpm lint:deps`, `pnpm test:packages`, `pnpm test:integration` — all green. Build affected packages so downstream `.d.mts` declarations refresh. The cascade is mechanical but workspace-wide.

#### R3 — SQL `Storage` participation + CLI SPI completeness + façade hygiene + dead-export cleanup

Multi-commit round (the items below are independent; commit each separately for review-clarity). The implementer chooses the order; suggested grouping:

**Commit A — SqlStorage implements Storage with `__unspecified__` stub (F09, partial close of architect B1).**
- Introduce `class SqlUnspecifiedNamespace extends NamespaceBase` (singleton family-layer placeholder) at [`packages/2-sql/1-core/contract/src/ir/`](../../packages/2-sql/1-core/contract/src/ir/). PR2's M5a swaps it for `PostgresSchema.unspecified` / `SqliteUnspecifiedDatabase.instance`.
- `class SqlStorage extends SqlNode implements Storage` — declare `readonly namespaces: Readonly<Record<string, Namespace>>` defaulted to `{ [UNSPECIFIED_NAMESPACE_ID]: SqlUnspecifiedNamespace.instance }`.
- Update arktype storage validators + every SQL test fixture / construction site that builds an `SqlStorage`-shaped object literal. Per architect F09 estimate: 30–60 file fixtures + arktype updates, bounded.
- `architect SDR § B1` is closed at the structural level: framework `Storage` interface now honoured by both families. PR2 still owns the per-target namespace concretion (`PostgresSchema` / `SqliteUnspecifiedDatabase`) but the seam is "fill in the per-target concretion" rather than "introduce namespaces."

**Commit B — CLI drops the optional/unknown SPI cast; SQL targets land their `serializeContract` SPI fully (GH#11, GH#12, GH#13).**
- The CLI emit operation at [`packages/1-framework/3-tooling/cli/src/control-api/operations/contract-emit.ts (L237–L257)`](../../packages/1-framework/3-tooling/cli/src/control-api/operations/contract-emit.ts) currently does `(config.target as { contractSerializer?: { ... } }).contractSerializer` — defensive against the SPI being absent. Both SQL targets ship `contractSerializer` on their descriptors today (`PostgresContractSerializer` / `SqliteContractSerializer`); the cast and the optional-chain are dead. Drop them; reach the SPI through the typed descriptor surface.
- `EmitOptions.serializeContract` becomes required at [`packages/1-framework/3-tooling/emitter/src/emit-types.ts`](../../packages/1-framework/3-tooling/emitter/src/emit-types.ts). The "test fixtures pass minimal options" workaround (the docstring at line 19 names this) is no longer load-bearing if every target ships the SPI. Update test call sites to thread the serializer (or import a real target descriptor's serializer directly — there is no in-tree fixture serializer to maintain).
- Refresh comments at the CLI emit op to remove "as the family adopts the SPI" wording — both families are adopted now.

**Commit C — `createIdentityContractSerializer` removed from production exports (GH#6).**
- The helper is exported from `@prisma-next/framework-components/control` but is unused in production today (`rg createIdentityContractSerializer` returns only the export point + the file that defines it). Delete the function + remove the export entry. If a test in tree relied on it (re-grep against `test/`), inline the trivial identity shape into that test or delete the test if it covers SPI-shape sanity that's no longer needed.

**Commit D — Standalone `validateContract` deleted; READMEs migrated to the SPI surface (F08).**
- Delete `packages/2-sql/1-core/contract/src/validate.ts`'s `validateContract` function (keep `decodeContractDefaults` if it remains a building block; otherwise delete). Keep the framework-level twin at `packages/1-framework/0-foundation/contract/src/validate-contract.ts` (retained per M3 R3b SATISFIED note as a layering-allowed structural-validation primitive).
- Delete `validate.test.ts` if it covers nothing else; otherwise prune to the surviving primitives.
- Remove `"./validate": "./dist/validate.mjs"` from `packages/2-sql/1-core/contract/package.json` `exports`.
- Update both READMEs ([`packages/2-sql/2-authoring/contract-ts/README.md` (L28, L32, L222, L225, L261)](../../packages/2-sql/2-authoring/contract-ts/README.md) and [`packages/2-sql/1-core/contract/README.md` (L120)](../../packages/2-sql/1-core/contract/README.md)) to point at the per-target descriptor pattern (`descriptor.contractSerializer.deserializeContract(json)` from `@prisma-next/target-postgres/control` etc.).
- Spec AC12's literal "removed from the codebase" clause now passes strict reading.

**Commit E — Mongo example apps stop reaching past the façade (GH#1, GH#2, GH#3).**
- The `mongo<TContract>(...)` façade at [`packages/3-extensions/mongo/src/runtime/mongo.ts`](../../packages/3-extensions/mongo/src/runtime/mongo.ts) already wraps `MongoContractSerializer().deserializeContract(...)` and preserves `TContract` through its generic. Example apps should not reach into `@prisma-next/family-mongo/ir` directly.
- The two example apps that violate this — [`examples/mongo-demo/src/db.ts`](../../examples/mongo-demo/src/db.ts) and [`examples/retail-store/src/db.ts`](../../examples/retail-store/src/db.ts) — also carry `as Contract` casts (GH#2, GH#3) that exist *because* they reached past the façade (the façade's `TContract` generic prevents the cast at the API surface).
- The example apps install telemetry middleware on a hand-rolled stack — check whether the `mongo<TContract>(...)` façade accepts middleware. If yes, rewrite the examples to use the façade. If no, extend the façade with `middleware?: ReadonlyArray<Middleware>` (mirroring the Postgres façade's middleware support — verify pattern); then rewrite the examples.
- After rewrite: `as Contract` casts are gone from the examples; `MongoContractSerializer` import is gone; the demo apps demonstrate the canonical user-facing surface.

**Commit F — `introspectPostgresEnumTypes` return type narrows to the annotation shape (F02).**
- [`packages/3-targets/6-adapters/postgres/src/core/enum-control-hooks.ts (L108–L130)`](../../packages/3-targets/6-adapters/postgres/src/core/enum-control-hooks.ts) declares `Promise<Record<string, StorageTypeInstance>>` but constructs plain object literals. The class-instance signature lies. Replace with a dedicated annotation-shape type (e.g. `PostgresStorageTypeAnnotation = { codecId: string; nativeType: string; typeParams: { values: readonly string[] } }`) declared at the Schema IR annotation seam. Schema IR enum representation lift (open item) is unaffected — the annotation shape is the right surface today.

**Validation per commit:** typecheck, lint:deps, packages tests for the touched surface; full workspace test suite at the end of R3.

#### R4 — review-artefact refresh + GH thread closure (in-session, orchestrator-owned)

- Update [`projects/target-extensible-ir/reviews/pr-499/code-review.md`](reviews/pr-499/code-review.md) to mark F03, F05, F07, F10, F11, F12 as RESOLVED with commit SHAs; refresh F02, F08, F09 verdicts (PASS); refresh AC12 to PASS strict; refresh AC9/AC10 if doc surfaces moved.
- Update [`projects/target-extensible-ir/reviews/pr-499/system-design-review.md`](reviews/pr-499/system-design-review.md) §§ B1, B2, B3, 3.1, 3.3 to reflect M7+M8 closures.
- Refresh [`projects/target-extensible-ir/reviews/pr-499/walkthrough.md`](reviews/pr-499/walkthrough.md) to mention M7 + M8 closure in the sequencing.
- Reply to each of the 13 GH PR threads with the resolution commit SHA + brief closure note; resolve the thread via `gh api`.
- Update PR #499 description to mention M7 + M8 in the "How it fits together" sequencing.

**Findings explicitly carried as open items, not closed in M8:**
- **F01 — codec-aware default-decoding regression.** Estimated touch surface: SPI shape + framework `validateContract` + test surface. Larger than the other findings; properly belongs in a focused follow-up where the SPI evolution can be reasoned about end-to-end. Stays on the open-items list.
- **F04 — `MongoTargetUnspecifiedDatabase` frozen-by-base trap.** Documentation-only fix is in the implementer's hands during R3 Commit A (close to the SQL singleton work; same singleton-subclass shape). If the implementer judges the doc-only fix is enough, fold into Commit A; if the structural fix (move `freezeNode` to a leaf-class `seal()`) is preferred, surface as an escalation.
- **Architect § 3.5 (abstract-earns-existence ADR-body codification).** The two clauses of the rule ("≥2 sibling targets OR a single polymorphic-dispatch consumer") need to land in the Decision body of one of the convention/principles ADRs, not only in Open questions. Folded into the ADR draft updates as part of M8 R4 (orchestrator-owned doc edits).
- **Architect § 3.6 (`*Base` suffix collision-vs-convention).** Cleared when the legacy `type MongoStorage` data shape is removed; not load-bearing in M8.
- **`EntityHelperFunction<Descriptor>` literal-tuple non-propagation.** Open item; same surface as M7 F06; tracked separately. **PR1 routing (post-M9):** routed to the `postgres-enum-finishing` project (`projects/postgres-enum-finishing/spec.md` Axis A) — the literal-tuple propagation work is the foundation that user-facing enum DX (typed declaration, defaults, query input/output, runtime introspection) builds on. Out of M9 scope; M9 closes the structural lie at the contribution surface (drops the `as unknown as StorageTypeInstance` cast) without sharpening the helper signature.
- **PGlite shutdown-race flakiness.** Pre-existing; not exacerbated by PR1.
- **R1d projector regression — caught + fixed at PR1 pre-merge gate (commit `f23665db0`).** `deriveAnnotations` in `packages/2-sql/9-family/src/core/migrations/contract-to-schema-ir.ts` post-R1d wrote the raw `PostgresEnumStorageEntry` shape (top-level `values`) into `schema.annotations.pg.storageTypes` for enum entries, breaking the documented invariant in `enum-planning.ts § readExistingEnumValues` that every entry — whether produced by the adapter introspector or the projector — carries the codec-typed triple `{codecId, nativeType, typeParams: {values}}`. The planner consequently treated existing enums as new and emitted a bare `CreateEnumTypeCall` instead of the full rebuild recipe (`createEnumType(temp) → alterColumnType → dropEnumType(orig) → renameType` plus the `DataTransformCall` placeholder when a value is dropped). Surface that flushed it out: deterministic failure of `test/integration/test/cli-journeys/data-transform-enum-rebuild.e2e.test.ts`. Fix normalises the postgres-enum variant back to the codec-typed shape inside the projector; codec-instance entries unchanged. Test now passes; integration-suite flake count down from 15 → 10 (remaining 10 are PGlite/CLI fragility, same envelope as the existing § Open items entries).

### M9 — PR1 architectural-honesty close (slot widening + cast drop + discriminator rename + abstract collapse + cheap adjacencies)

**Status:** SATISFIED (M9 R1 + R2 closed; all eight findings F13–F20 resolved; AC2 PASS at the strict bar; PR1 pre-merge gate passed after one regression catch + fix — see § Open items entry "R1d projector regression" / commit `f23665db0`).

**Goal:** close M4's stated objective ("enums are first-class IR nodes, not codec types") at the type system, not just at the runtime instance layer. Today's `PostgresEnumType` is constructed correctly but immediately cast to `StorageTypeInstance` at the contribution surface (`packages/3-targets/3-targets/postgres/src/core/authoring.ts:45`) — the slot type `SqlStorage.types: Readonly<Record<string, StorageTypeInstance>>` doesn't accept polymorphic IR; the cast is the architectural lie M4 was supposed to remove. M9 widens the slot to polymorphic `StorageType`, drops the cast, renames `'sql-enum-type'` → `'postgres-enum'` (the literal encodes the family altitude; per Decision 18 enum is target-only), and collapses the unearned `SqlEnumType` family abstract into `PostgresEnumType` (no second concrete subclass exists). Six cheap adjacencies that touch the same IR-layer files ride along: F15 dead-code delete, F18 helpers shape flatten, F19 Mongo storage hoist, IRNode renames, F17 freeze-trap docs, F13/F14/F16 doc comments. **All eight findings (F13–F20) close in M9; PR1 cannot ship with the architectural lie still on disk.**

**Sources:** [`projects/target-extensible-ir/reviews/pr-499/round-2/code-review.md`](reviews/pr-499/round-2/code-review.md) §§ F13–F20; orchestrator/user decisions in this conversation; cross-references to architect SDR § B1a / § 3.1 / § 4.2 (where M9 contradicts a "CLOSED — deliberate" verdict).

**Round structure.** Two implementer rounds — split because R1 is load-bearing and large (~30-40 file touch surface; the slot-widening cascade), R2 is lighter and rides on R1 ("the slot accepts polymorphic IR; here are the adjacencies"). Each round commits per task as named below; explicit staging only.

**R1 — F20 close (one task per commit, four commits, sequenced).** Land the slot widening + cast drop + rename + abstract collapse as four reviewable commits in order. R1 cannot land out of order (each commit's typecheck depends on the prior).

- [x] **R1a — Promote framework `StorageType` abstract; widen `SqlStorage.types` slot to polymorphic.** Add `interface StorageType extends IRNode { readonly kind: string }` at framework. `StorageTypeInstance` becomes a structural sub-interface (the legacy codec-triple shape; plain object literal, JSON-clean). Add `kind: 'codec-instance'` to plain-object codec triples at every construction site + every JSON envelope. `SqlStorage.types` becomes `Readonly<Record<string, StorageTypeInstance | PostgresEnumStorageEntry>>` — a closed union at the SQL family slot, accepted as honest pragmatic close (avoids excess-property-check errors on existing `SqlStorage`-shaped fixture literals while letting framework-level `StorageType` remain truly polymorphic; structurally analogous to F13's family-layer arktype-validator constraint). Routed to open item below for widening to bare `StorageType` at the same trigger as F13. Update arktype `StorageTypeEntrySchema` to admit either codec-instance OR `'sql-enum-type'` (the rename to `'postgres-enum'` lands in R1c, not here — keep R1a focused on slot widening). Test fixtures + on-disk migration JSONs need the new `kind: 'codec-instance'` field on codec entries (mirroring M8 R3 Commit A's namespace cascade — same pattern). Touch surface: ~15-25 files. Commit: `refactor(framework,sql): widen StorageType slot to polymorphic; codec triples carry kind: 'codec-instance'`. **Landed:** commit `b7bf3d33a`.

  **Do NOT also:** refactor codec discovery (`codecRefForStorageColumn`, `pgCodecs`); add new entity types; refactor planner-strategies or verifier internals; edit `PgEnumCodec` to be value-aware (that's `postgres-enum-finishing` project work). If a downstream consumer breaks (verifier, planner, runtime), update the consumer to match the new slot type — do not narrow the slot type back.

- [x] **R1b — Drop the `as unknown as StorageTypeInstance` cast at `authoring.ts:45`.** Falls out of R1a — the slot now accepts `PostgresEnumType` natively. Factory return type is the structural `PostgresEnumStorageEntry` interface (the SQL-domain alphabet entry that `PostgresEnumType` satisfies) — chosen over `PostgresEnumType<TName, TValues>` for portability so SQL-domain consumers do not import `target-postgres` (matches the same constraint that drove the D6 cross-domain pushback in R1d). Touch surface: ~3-5 files (the factory at `packages/3-targets/3-targets/postgres/src/core/authoring.ts`; any consumer that referenced the cast-erased type). Commit: `refactor(target-postgres): drop StorageTypeInstance cast at enum contribution surface`. **Landed:** commit `847fc0cd8`.

  **Do NOT also:** sharpen `EntityHelperFunction<Descriptor>` literal-tuple propagation. That's separate and lives in the `postgres-enum-finishing` project (`projects/postgres-enum-finishing/spec.md` Axis A); M9 stops at "the cast is gone; the slot accepts the IR class natively."

- [x] **R1c — Rename `'sql-enum-type'` → `'postgres-enum'` workspace-wide.** The discriminator literal encodes the family altitude (`sql-`) on a target-only IR node (Postgres only; per Decision 18). Rename across: `PostgresEnumType.kind` literal; `postgresAuthoringEntityTypes.enum.discriminator`; `buildPostgresEntityTypeRegistry` registry key; the arktype `SqlEnumTypeSchema` literal value (renamed `SqlEnumTypeSchema` → `PostgresEnumTypeSchema`; the schema stays at the family layer per F13's deferred lift — see Open items); parity-fixture hashes; on-disk migration JSON for cipherstash + test fixtures. Touch surface: ~10 files + cascade. Commit: `refactor(target-postgres): rename 'sql-enum-type' → 'postgres-enum' (target-altitude discriminator)`. **Landed:** commit `7b835c8ee` (with infra-prereq `a68db53f2` adding `migrations/**` to turbo build inputs so migration JSON edits invalidate the build cache).

  **Do NOT also:** rename other `'sql-*'` discriminators not directly related to enums. None exist in tree today; if you find one, surface as escalation rather than acting on it.

- [x] **R1d — Collapse `SqlEnumType` family abstract into `PostgresEnumType`.** Per Decision 18 (enum is target-only). Delete `packages/2-sql/1-core/contract/src/ir/sql-enum-type.ts`; `PostgresEnumType extends SqlNode` directly (or whatever the post-IRNode-rename family base is). **Dispatch site rule (D6 cross-domain pushback, accepted):** sites in the SQL family that need to recognize the enum entry **must not** use `instanceof PostgresEnumType` — `architecture.config.json:612` constrains the SQL family to import only from framework, not from `target-postgres`. Use the structural type guard `isPostgresEnumStorageEntry(value)` declared at `packages/2-sql/1-core/contract/src/ir/postgres-enum-storage-entry.ts` instead (the structural interface lives at the family layer; both `PostgresEnumType` and any future on-disk hydration of the same shape satisfy it). Sites internal to `target-postgres` may use `instanceof PostgresEnumType` (no layering issue). Verify with grep: `rg "SqlEnumType" packages/` returns zero hits post-commit. Touch surface: ~5-8 files. Commit: `refactor(target-postgres): collapse SqlEnumType family abstract into PostgresEnumType (Decision 18: target-only enum)`. **Landed:** commit `fa9134130`.

  **Do NOT also:** refactor verifier or planner internals beyond the `instanceof` switch; refactor adjacent IR classes; lift any other family abstract.

**R1 validation gate** (run once at end of R1d):

- `pnpm typecheck` (workspace-wide).
- `pnpm lint:deps`.
- `pnpm test:packages` (workspace-wide; the slot widening cascades into many fixtures).
- `pnpm test:integration` (the JSON envelope cascade in R1a needs to round-trip cleanly through PGlite).
- Grep guards (must all be zero hits): `rg "SqlEnumType" packages/`; `rg "'sql-enum-type'" packages/`; `rg "as unknown as StorageTypeInstance" packages/`.
- Transient-ID scan per the implementer agent's standard checklist.

**R2 — Cheap adjacencies (one task per commit, six commits, independent).** Order is implementer's choice; each commit independent of the others. R2 lands after R1 SATISFIED.

- [x] **R2a — F15: delete dead `kind === 'sql-enum-type'` runtime bridges.** Both branches are dead in tree (canonical path always pipes raw JSON through `SqlStorage` constructor, which throws on raw `'sql-enum-type'` per F09; bypass path also gated by the same constructor). Delete: `packages/2-sql/4-lanes/relational-core/src/codec-ref-for-column.ts` lines 34-51 (the `if (kind === 'sql-enum-type')` branch + its docstring); `packages/2-sql/5-runtime/src/sql-context.ts` `readEnumViewIfApplicable` lines 224-258 (the entire `if (kind === 'postgres-enum')` synthesis branch — discriminator is already renamed by R1c). Touch surface: ~50 lines deleted across two files. Verify: tests at the two runtime sites should not assert the now-deleted branches; if they do (test was asserting unreached code), surface as escalation. Commit: `refactor(sql-relational-core,sql-runtime): delete dead 'postgres-enum' bridges (canonical path throws upstream)`. **Landed:** commit `db2e6d758`. **As-built:** the plan body's `lines 34-51` / `lines 224-258` delete targets no longer exist — R1d's structural unification (`isPostgresEnumStorageEntry` type guard) already collapsed those synthesis branches before R2a started. R2a landed as docstring tightening that names the canonical-only invariant explicitly (raw JSON never reaches these sites; `SqlStorage.normaliseTypeEntry` throws upstream per F09). F15's intent is closed at the doc level.

  **Do NOT also:** refactor sibling functions in those two files; reshape `codecRefForStorageColumn`'s return shape; reshape `sql-context`'s view-synthesis API.

- [x] **R2b — F18: flatten pack-contributed entityTypes onto top-level helpers shape.** Built-in top-level entities (`model`, `rel`) sit at top level; pack-contributed top-level entities (`enum` today; `namespace`/`policy`/`role`/`view` in PR2 + beyond) live behind `helpers.entities.<name>` — flatten them to top level alongside `model`/`rel`. The contribution data structure `AuthoringContributions.entityTypes` is unchanged — only the user-facing helpers shape changes. Touch sites: SQL `composed-authoring-helpers.ts` (`ComposedAuthoringHelpers` type at lines ~132-155 + `createComposedAuthoringHelpers` runtime at lines ~249-283); Mongo `contract-builder.ts` mirror; the entities-namespace exemplar test at `packages/2-sql/2-authoring/contract-ts/test/entities-namespace.exemplar.test.ts` (rewrite `({ entities, model }) => entities.demoEntity(...)` to `({ demoEntity, model }) => demoEntity(...)`); the contract-ts README usage example; the spec FR8b paragraph (now needs to name top-level placement as the principle, not the asymmetry as deliberate); ADR draft `target-extensible-authoring.md` Decision body. Touch surface: ~6-10 files. Commit: `refactor(framework,sql,mongo): flatten pack-contributed entityTypes onto top-level helpers shape (built-in/contributed symmetry)`. **Landed:** commit `d4cdda060`. **As-built:** spec FR8b paragraph + ADR draft touches deferred per Decision 21 — orchestrator owns those (post-R2 SATISFIED orchestrator sweep). R2b touched the contract-ts README usage example as planned.

  **Do NOT also:** change the contribution data structure shape (`AuthoringContributions.entityTypes` stays — that's the contribution surface, not the user surface); rename `field` or `type` namespaces (they remain legitimate inner-value namespaces); refactor `composeEntityNamespace` / collision detection (those still apply to contribution data structure).

- [x] **R2c — F19: hoist `MongoTargetStorage` → `MongoStorage` at family layer.** Move `mongo-target-storage.ts` from `packages/3-mongo-target/1-mongo-target/src/core/` to `packages/2-mongo-family/1-foundation/mongo-contract/src/ir/`. Rename `class MongoTargetStorage` → `class MongoStorage`. Collapse the abstract `MongoStorageBase` into the new concrete `MongoStorage` (the only consumer was the now-hoisted target class). Update import paths in `target-mongo` consumers (~6-8 sites). The `MongoTargetUnspecifiedDatabase` placeholder stays at target layer for this commit (separate question; do not bundle). The `MongoTargetContractSerializer.serializeContract` override stays as-is (the JSON-envelope-asymmetry is F16, separate seam). Touch surface: ~10-15 files. Commit: `refactor(mongo-family,mongo-target): hoist MongoTargetStorage to family layer as MongoStorage (cross-family placement symmetry)`. **Landed:** commit `deafdee92`. **As-built:** the plan body didn't anticipate the `type MongoStorage` ↔ `class MongoStorage` collision (SQL has no equivalent — `SqlStorage` is the class; no data-shape alias). Resolution: rename data-shape alias `MongoStorage` → `MongoStorageShape`; the class takes `MongoStorage`. Two call sites + one contract-builder generic constraint switched. The persisting data-shape/class duality is a Mongo-only artifact (analogous to F16); collapsing it is out of scope for M9 ("Do NOT also: refactor unrelated Mongo IR classes").

  **Do NOT also:** change the `serializeContract` override behavior; refactor unrelated Mongo IR classes; hoist `MongoTargetUnspecifiedDatabase` (separate concern).

- [x] **R2d — IRNode disambiguating renames.** Three independent renames, each mechanical (grep-driven): `SchemaNodeKind` → `SchemaViewNodeKind` (the SchemaView AST kind union — name reflects SchemaView's identity); `MongoSchemaNode` → `MongoSchemaIRNode` (Mongo Schema IR root node); `SqlSchemaNode` → `SqlSchemaIRNode` (SQL Schema IR root node — mirrors Mongo). Each rename ~5-8 files. Touch surface: ~15-25 files total. Commit: `refactor: rename SchemaView/Mongo/Sql schema-IR root node kinds for disambiguation`. **Landed:** commit `23c5f7a0e`. **As-built:** plan named three renames; only two were live work — `SqlSchemaNode` → `SqlSchemaIRNode` was already in tree pre-M9 (`rg "SqlSchemaNode\b" packages/` returned 0 hits pre-R2d). SchemaView + Mongo renames landed; `AnyMongoSchemaNode` type alias remains (distinct union-type alias enumerating concrete node classes; not a reference to the old abstract). See § Open items for an optional consistency rename to `AnyMongoSchemaIRNode`.

  **Do NOT also:** rename other `*Node` types not in this list (`IRNode`, `IRNodeBase`, `SchemaView`, `OpFactoryCall`, etc. all stay — the IRNode rename happened in M8 R2). If you find a `*Node` that seems related and isn't on this list, surface as escalation.

- [x] **R2e — F17: freeze-trap warning comments at non-leaf-freeze sites.** Two file edits, ~5 lines added each. (1) `packages/3-mongo-target/1-mongo-target/src/core/mongo-target-database.ts` — add a comment on `class MongoTargetDatabase` warning "Constructor calls `freezeNode(this)` at the end. Direct subclasses MUST NOT add instance fields — the freeze runs in this base constructor and any subclass field assignment will silently fail in non-strict mode or throw in strict mode. To add instance fields to a subclass, lift this `freezeNode` to leaf-class constructors." (2) `packages/2-sql/1-core/contract/src/ir/sql-unspecified-namespace.ts` — add a parallel comment naming the inverse pattern (leaf calls `Object.defineProperty(this, 'kind', ...)` after `super()` works only because `NamespaceBase` does NOT freeze; subclasses that add instance fields will still hit the same trap if a future framework change lifts freeze to `NamespaceBase`). Touch surface: 2 files. Commit: `docs(mongo-target,sql-contract): warn about freeze-trap on subclass instance fields`. **Landed:** commit `571ed542e`. **As-built:** no staleness; landed exactly as planned.

  **Do NOT also:** lift `freezeNode` to a leaf-class `seal()` hook (out of scope; routed to follow-up); refactor freeze convention; touch other IR class freeze sites.

- [x] **R2f — F13 + F14 + F16 doc-only comment additions naming the open seams.** Three file edits, ~3-5 lines each. (1) `packages/2-sql/1-core/contract/src/validators.ts` near `StorageTypeEntrySchema` (lines ~97-104) — add a comment: "Family-layer arktype validation enumerates known polymorphic shapes. Pack-contributed entity types ship a parallel arktype schema entry here when they introduce a new persisted shape; the registry-driven hydration seam at `SqlContractSerializerBase.hydrateStorageTypeEntry` is open, but the family-layer structural validator is closed by design. Future open: lift to `unknown` fallback + move structural diagnostics to the registry-dispatch site, when a second polymorphic kind earns it." (2) `packages/3-targets/3-targets/postgres/src/core/postgres-contract-serializer.ts` near `buildPostgresEntityTypeRegistry` (lines ~11-33) — add a comment: "Registry built from this target pack's literal `postgresAuthoringEntityTypes`. Extension-pack-contributed entity types do not reach this registry today; the surface is honest for in-tree consumers (Postgres pack only). Future open: lift the registry build to descriptor-composition time, threading the composed `AuthoringContributions.entityTypes` from extension packs, when a real extension pack ships a round-trip-needing entity type." (3) `packages/1-framework/1-core/framework-components/src/ir/storage.ts` JSDoc on `Storage` interface — add a paragraph: "Whether the namespace map appears in the persisted JSON envelope is a per-target decision made by `ContractSerializer.serializeContract`. Some targets emit a JSON-clean namespace shape that round-trips through `JSON.stringify` cleanly (SQL today via the family-layer identity serializer); others ship runtime-only fields on their namespace concretions and override `serializeContract` to strip them (Mongo). The on-disk envelope shape is target-owned, not framework-promised." Touch surface: 3 files. Commit: `docs(sql-contract,target-postgres,framework): comment open seams in validator/registry/storage`. **Landed:** commit `93bbca8fa`. **As-built:** no staleness; landed exactly as planned.

  **Do NOT also:** implement the structural fixes named in those findings (validator lift; descriptor-time registry; explicit envelope test) — they are routed to follow-up open items below; M9 ships only the comments.

**R2 validation gate** (run once at end of R2f):

- `pnpm typecheck` (workspace-wide).
- `pnpm lint:deps`.
- `pnpm test:packages` (workspace-wide; R2b helpers-shape change cascades into the entities-namespace exemplar + every contract-builder consumer).
- `pnpm test:integration`.
- Grep guards (must all be zero hits): `rg "MongoTargetStorage" packages/` (R2c); `rg "MongoStorageBase" packages/` (R2c collapse); `rg "kind === 'sql-enum-type'" packages/` (R2a — and `kind === 'postgres-enum'` in `codec-ref-for-column.ts` and the deleted `sql-context.ts` branch); `rg "SchemaNodeKind" packages/` (R2d); `rg "MongoSchemaNode\b" packages/` (R2d); `rg "SqlSchemaNode\b" packages/` (R2d).
- `rg "helpers\.entities\." packages/ examples/` returns zero hits (R2b — top-level access is the new shape; `helpers.entities` namespace is gone).
- Transient-ID scan per the implementer agent's standard checklist.

**Spec / plan / ADR refresh** (orchestrator-owned, in-session, between R1 SATISFIED and R2 start):

- spec.md AC2 wording — locked to the M9 honest-IR bar.
- spec.md FR8b — name top-level placement as the helpers-shape principle (rather than the entityTypes/entities asymmetry as deliberate).
- spec.md FR9 — drop the `SqlEnumType` family-abstract phrasing; per-target IR class only.
- ADR drafts at `projects/target-extensible-ir/specs/` — refresh `target-extensible-authoring.md`, `polymorphic-storage-types.md`, `3-layer-polymorphic-ir-convention.md` for: (a) helpers-shape top-level principle (R2b); (b) abstract-earns-existence applied to `SqlEnumType` collapse (R1d); (c) polymorphic slot type (R1a); (d) target-altitude discriminators (R1c).

**Cross-cutting scope guards (bind every commit in this milestone):**

- **Implementer must not address application-facing enum DX.** Literal-tuple propagation, enum-value defaults, codec value-awareness, `db.enums.<Name>` runtime surface — all routed to the `postgres-enum-finishing` project at [`projects/postgres-enum-finishing/spec.md`](../../postgres-enum-finishing/spec.md). M9 ships an architecturally honest enum exemplar; `postgres-enum-finishing` ships an idiomatically usable one. If tempted by adjacent improvements, surface as escalation.
- **Implementer must not refactor the planner, the verifier, or the codec subsystem.** These are downstream consumers of the IR slot; they inherit the new types, but their internal shape stays. If a downstream consumer breaks, fix the consumer to match the new slot type — do not narrow the slot type back.
- **Implementer must not invent new entity types or new authoring contributions.** M9 keeps the contribution data structure shape-stable.
- **Implementer must commit each task as named (one commit per `Rxy` bullet).** No "while I'm here" merges across tasks. R1's four commits are sequential; R2's six commits are independent (any order).
- **Implementer must not touch any of the items under § Open items.** Pre-existing carry-overs (F01 codec-aware default-decoding; AC3 strict-bar gap; M5a/M5b namespace exemplar; PGlite flake) are NOT M9 scope.

**Out-of-scope follow-ups M9 routes to open items** (orchestrator records below; implementer does not act on them):

- F13 structural fix — lift `StorageTypeEntrySchema` to `unknown` fallback + move structural diagnostics to the registry-dispatch site. Tied to the first non-enum entityType that ships through the registry (PR2's namespace work or a post-PR2 target-only entity).
- F14 structural fix — lift `buildPostgresEntityTypeRegistry` to descriptor-composition time so extension-pack-contributed entity types round-trip through the registry. Tied to the first extension pack that ships an entity contribution.
- F16 structural close — extend per-target `ContractSerializer` integration test surface to assert which envelope shape each target persists. Tied to PR2's per-target namespace lift.
- F17 structural fix — move freeze convention to a leaf-class `seal()` hook so subclasses can add instance fields without the trap. Touches framework + family bases + ~12 IR class files; out of PR1 scope.



> Items surfaced during execution that are out of scope for this PR but worth tracking. Reviewed at close-out.

- ~~**Lift Mongo schema-side helpers to the family layer (post-Supabase, future project).**~~ _Resolved in M2.5 R1._ The three target-side helpers (`contractToMongoSchemaIR`, `canonicalizeSchemasForVerification`, `diffMongoSchemas`) lifted to family-mongo alongside the `verifyMongoSchema` verifier shell. The lift was structurally trivial (helpers depend only on foundation packages) and removed a target → family → target import-shape hop that would otherwise persist post-M2.5. The future-project framing carried over from M2's verifier-base lowering decision; M2.5's pre-R1 reconnaissance found the lift was no longer load-bearing on a second Mongo target — it was load-bearing on the family-layer SPI dispatch shape itself.
- **Mongo descriptor-seam asymmetry vs SQL → absorbed into M2.5; scope expanded pre-R1.** Originally filed as a follow-up under TML-2505 + sibling project `projects/relocate-mongo-target-descriptor/`; promoted to in-scope for this project (`wip/unattended-decisions.md § 9`). Pre-M2.5-R1 reconnaissance found the original "relocate one file" framing could not satisfy its own dep-severance AC because `family-mongo/control-instance.ts` carries seven additional runtime imports of target/adapter symbols that the descriptor file relocation does not touch. Spec rewritten around the architecturally correct shape — mirror SQL's adapter-SPI dispatch pattern. Detailed task spec at [`specs/mongo-control-adapter-spi.spec.md`](specs/mongo-control-adapter-spi.spec.md) (renamed from `relocate-mongo-target-descriptor.spec.md`). TML-2505 marked duplicate of TML-2459. The original "fix it later" framing is preserved in `wip/unattended-decisions.md § 2`; the M2.5 absorption is recorded in `§ 9`; the scope correction (SPI dispatch shape) is recorded in `§ 11`.
- **`EntityHelperFunction<Descriptor>` literal-tuple non-propagation (surfaced at M4 R1a).** The current `EntityHelperFunction<Descriptor>` reads the factory's declarative input/output types, which bind to the descriptor-level generic defaults (`Input = never`, `Output = unknown` per the M3.5 R1b variance flip). As a consequence, `entities.enum({ values: [...] as const })`'s literal tuple does not propagate into the inferred contract type — the contract sees the widened `string[]` shape. Sharpening `EntityHelperFunction` to forward descriptor-level generics (so a pack literal's `as const` literal-tuple input flows through to the inferred entity type) is a separable cross-family entities-mechanism refinement; it benefits every consumer of `helpers.entities.<name>(...)` and earns its own focused round when a downstream consumer surfaces a sharp need. Estimated 0.5-1 implementer-day; mechanism-only refactor scoped to `framework-authoring.ts`. Suggested follow-up: focused Linear ticket post-project (or absorbed alongside the Mongo built-in contribution-ification follow-up).
- **Schema IR enum representation lift deferred from M4 (Decision 18 Q4).** M4 lifts Contract IR enums to first-class IR (polymorphic `storage.types[name]` carrying `PostgresEnumType` instances). Schema IR enum representation stays annotation-shaped (`schema.annotations.pg.storageTypes`); the new verifier walk carries a small bridging adapter from the annotation shape to a normalised view. The introspector (`pgEnumControlHooks.introspectTypes`) stays codec-hook-shaped for the same reason. Lifts when Schema IR gains polymorphic-dispatch consumers (multiple kinds of entity carrying their own metadata: enums + roles + RLS policies + views) — coherent unit of work for a focused follow-up. Estimated 1-2 implementer-days when picked up; mechanism is established by M4. Suggested follow-up: focused Linear ticket post-project, or fold into a future Postgres-domain-entities project (RLS + roles + views).
- **Mongo built-in contribution-ification deferred from M3.5 (Decision 17).** The M2 R2 close-out flagged the load-bearing gap that Mongo's `buildCollections` / `toStorageIndex` / `toStorageCollectionOptions` paths construct IR-class instances via direct `new MongoX(...)` calls rather than dispatching through pack-contributed factories. M3.5 R1 ships the mechanism and threads `helpers.entities` through the Mongo contract-builder factory signature so future pack-contributed entities work, but the consumer-cascade refactor of the existing built-in construction sites (which would have the Mongo family-pack ship `entities.collection` / `entities.index` / `entities.collectionOptions` factories + restructure `model()` lowering to dispatch through them) is deferred. The mechanism is in tree; closing this gap is mechanical when a downstream milestone earns it (M4 enum exemplar lands against the mechanism without depending on this; M5a namespace exemplar similar). Suggested follow-up routing: M3.5 R2 if the orchestrator decides the gap should close before M5a; or a focused Linear ticket if it sequences naturally after M5a / the broader project. Estimated 1-2 implementer-days when picked up.
- **Codec-aware default-decoding regression (introduced in M3, surfaced by M3 R3b).** The pre-M3 SQL `validateContract(value, codecLookup)` ran `decodeContractDefaults` after structural validation so `column.default.value` of literal columns was passed through `codec.decodeJson` before the contract reached consumers. The M3 SPI shape (`deserializeContract(json: unknown): TContract`) doesn't accept a `codecLookup`, so `SqlContractSerializerBase` skips the decoding step. Every consumer that previously got decoded literal defaults now gets the raw JSON. Surface: limited but real — jsonb literal defaults + any column with non-trivial JSON-encoded default values. The dead-code `validate.test.ts` codec-decoding tests at `packages/2-sql/1-core/contract/test/validate.test.ts` (lines ~696–737) still pass because they test the legacy standalone wrapper directly, but no production runtime exercises that path anymore. M3 R3b's call-site sweep surfaced this; integration tests didn't catch it. **Three follow-up options:** (a) thread `CodecLookup` through the SPI signature; (b) introduce a separate post-deserialization `decodeContractDefaults(contract, codecLookup)` hook the family-level `ControlFamilyInstance.validateContract` composes after the SPI deserializer; (c) accept the regression and drop the dead `validate.ts` + tests. Recommendation: (b) — keeps SPI shape clean, restores production behavior, naturally addressable in M3.5 (when the entity-contribution mechanism makes contract construction more pluggable) or as a focused follow-up. Filed as a tracked open item rather than blocking M3 close-out because no integration test catches it and no consumer has reported it; the dead-code tests provide a regression net for whoever fixes it.
- **F13 structural fix — `StorageTypeEntrySchema` enumerates known polymorphic shapes.** Surfaced in PR1 round-2 PE pass. M9 R2f ships a doc comment naming the constraint; the structural fix (lift to `unknown` fallback + move structural diagnostics to the registry-dispatch site at `SqlContractSerializerBase.hydrateStorageTypeEntry`) is tied to the first non-enum entityType that ships through the registry (PR2's namespace work or a post-PR2 target-only entity). Estimated 0.5 implementer-day when picked up; mechanism-only refactor scoped to `validators.ts` + `sql-contract-serializer-base.ts`.
- **D1 / F20 partial — `SqlStorage.types` slot type is a closed union (`StorageTypeInstance | PostgresEnumStorageEntry`), not bare framework `StorageType`.** Surfaced in PR1 round-2 reviewer's M9 R1 verdict (variant (c) "split"). M9 R1a widened the framework `StorageType` interface to a truly polymorphic abstract (any `kind: string`) but the SQL-family slot narrows to a closed union for typed-fixture excess-property-check ergonomics — structurally analogous to F13's family-layer arktype-validator constraint, displaced from the validator into the TS slot type. The widening of the slot to bare `StorageType` (so SQL-family code accepts any pack-contributed `StorageType` polymorphically without the closed union) lands at the **same trigger as F13** — first non-enum entityType in the storage-types slot (PR2 namespace work or post-PR2 target-only entity). Pair with F13 for a single coherent unwidening pass. Estimated 0.5 implementer-day on top of F13; the two changes ship together.
- **F14 structural fix — `buildPostgresEntityTypeRegistry` reads target-pack literal, not composed contributions.** Surfaced in PR1 round-2 PE pass. M9 R2f ships a doc comment naming the constraint; the structural fix (lift the registry build to descriptor-composition time so the framework knows the full composed `AuthoringContributions.entityTypes` from extension packs) is tied to the first extension pack that ships an entity contribution that needs JSON round-trip hydration. Estimated 1 implementer-day when picked up; touches `~6-10` files at the descriptor-composition seam.
- **F16 structural close — explicit per-target envelope-shape integration test.** Surfaced in PR1 round-2 PE pass. M9 R2f ships a JSDoc on the framework `Storage` interface naming the asymmetry; PR2's per-target namespace lift will reshape what each target's `serializeContract` strips, at which point an explicit integration test asserting envelope shape per target prevents future shape regressions. Tied to PR2 M5a (`PostgresSchema` / `SqliteUnspecifiedDatabase`).
- **F17 structural fix — leaf-class `seal()` hook to eliminate the freeze-trap.** Surfaced in PR1 round-2 PE pass. M9 R2e ships warning comments at the two non-leaf-freeze sites; the structural fix (move the `freezeNode(this)` convention from base-class constructors to a `seal()` hook each leaf class calls explicitly at the end of its constructor) eliminates the trap entirely. Touches framework + family bases + ~12 IR class files. Out of PR1 scope; routed to a focused follow-up. Estimated 1-2 implementer-days when picked up; mechanism-shape change with workspace-wide ripple.
- **`AnyMongoSchemaNode` naming consistency.** Surfaced in PR1 round-2 R2 review (R2d). The `AnyMongoSchemaNode` type alias (a union enumerating concrete Mongo schema-IR node classes) was not part of R2d's three-rename pass — it's a distinct identifier, not a reference to the old abstract class. Renaming it to `AnyMongoSchemaIRNode` for consistency with `MongoSchemaIRNode` is trivial (2 files, mechanical) and a naming preference, not a correctness fix. Routed to follow-up rather than M9 to keep R2d's commit honest to its named scope.
- **Pre-existing test fragility surfaced during M1 R1 review.** Running `pnpm test:packages` against the M1 HEAD surfaced ~9 files / ~12 tests / ~8 unhandled errors across `@prisma-next/adapter-postgres` (Postgres connection flakiness in integration tests) and `@prisma-next/cli` (mock-setup issues). None touch M1-modified files; failures vary across runs (different packages failing in different runs), confirming flakiness rather than regression. Out of M1's `typecheck + lint:deps` validation gate. The Postgres adapter integration-test flakiness will likely resurface and may block M3 R1 if it persists; revisit at M3 entry. **Trajectory note (M2 R2 closure round, 2026-05-13):** the PGlite shutdown-race flake set affected 22 integration files in the M2 R2 closure round's full-suite run vs 7 files in the prior M2 R2 run, all 22 producing the canonical `Error: Connection terminated unexpectedly` in `pg/lib/client.js:180:73` and all isolation re-runs returning green. The flake is contention-dependent and the breadth widened across the worktree's other concurrent activity. M3 entry orchestrator should consider fast-tracking mitigation (Linear ticket; PGlite vs real-Postgres choice; per-test isolation pattern) rather than waiting for the flake to actually block a SATISFIED gate. No Linear tickets filed yet (per orchestrator/user decision E2 in m1 R1 triage).

## Close-out (required) — happens after PR2 merges

> The project's close-out is **not** PR1's responsibility. PR1 ships M1-M4 + M3.5 + M2.5 + M6 (PR1-scoped docs) and leaves the project artifacts at `projects/target-extensible-ir/` intact for PR2 to consume. Close-out below is the post-PR2 step.

- [ ] Verify all acceptance criteria in [`projects/target-extensible-ir/spec.md`](spec.md). PR1 closes ACs 1, 2, 7, 8, 9, 10, 11, 12 (mechanism + first exemplar + IR convention docs). PR2 closes ACs 4, 4a, 5, 6 (namespace + cross-namespace FK + multi-tenancy).
- [ ] Promote ADR drafts (3-layer IR convention; architectural principles; target-extensible authoring / `entities` contributions; storage shape Decision 18; namespace + cross-namespace FK from PR2; any other drafts surfaced during execution) from `projects/target-extensible-ir/specs/` to `docs/architecture docs/adrs/`.
- [ ] Confirm `AGENTS.md` / `CLAUDE.md` Golden Rule is updated and `docs/reference/typescript-patterns.md` carries the new AST/IR section. (PR1 lands the M1-M4 surface; PR2's docs sweep adds namespace-specific updates if any.)
- [ ] Confirm `docs/Architecture Overview.md` § "Guiding Principles" surfaces "framework provides affordances; targets implement specifics" and "familiar with one target, fluent in another".
- [ ] Confirm subsystem docs in `docs/architecture docs/subsystems/` (Data Contract, Contract Emitter & Types, Adapters & Targets) reflect the class-hierarchy IR shape AND the namespace shape.
- [ ] Migrate any other long-lived docs into `docs/`.
- [ ] Strip repo-wide references to `projects/target-extensible-ir/**` (replace with canonical `docs/` links or remove).
- [ ] Delete `projects/target-extensible-ir/`.
