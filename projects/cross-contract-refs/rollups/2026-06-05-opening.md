# Project health rollup: cross-contract-refs (TML-2500)

**Cadence:** session-bookend (opening rollup)
**Date:** 2026-06-05

## Progress

- **Slices delivered:** 0 / 4 — none
- **Slices in flight:** none
- **Slices not started:** M1 (Foundation — IR carrier + aggregate-load checks), M2 (Authoring surfaces — TS brands + PSL grammar), M3 (Planner + verifier integration), M4 (Documentation + close-out)
- **Direct changes:** none
- **Project-DoD coverage (AC → slice map):**
  - AC1 (TS cross-contract FK lowers to `source:'space'`) → M2
  - AC2 (PSL `supabase:auth.User` round-trip) → M2
  - AC3 (`__unspecified__` target → unqualified `REFERENCES`) → M2 (carrier) + M3 (DDL)
  - AC4 (`onDelete:'cascade'` no diagnostic) → M2
  - AC5 (missing-pack fail-fast, TS + PSL) → M2
  - AC6 (primitive collision + cycle rejection at load) → M1
  - AC7 (PGlite e2e: cross-schema FK in `pg_constraint`, verifier clean) → M3
  - AC8 (round-trip property test, mixed `local`/`space` carriers) → M1
  - AC9 (TML-2459 local FK tests still green) → M3 (regression check; touched throughout)
  - AC10 (`pnpm lint:deps` green) → M1 (and every slice)
  - Walking-skeleton DoD (`Profile.userId → auth.User.id` in `examples/supabase`) → M4 / M3

## Drift signals

- **[informational]** Freshness risk: spec + plan were authored when blockers **TML-2459** (target-extensible IR) and **TML-2493** (control-policy) were *future*; both have since **landed on main**. The plan's M3 control-policy-shim mitigation is now unnecessary (dispatch tables shipped). **Action:** M1's first dispatch is a *reconciliation pass* — verify the current FK-carrier IR, the `extensionPacks` contract-aggregate construction, the M5b `source:'local'` carrier shape, and the control-policy planner/verifier dispatch against the plan's assumptions before writing code. Not drift — a known, mitigable starting condition.
- **[informational]** `examples/supabase` walking skeleton is stood up by the *sibling* project `extension-supabase` M1 (lane 1), not this project. This project's walking-skeleton DoD (the `Profile → auth.User` FK) depends on that skeleton existing. **Action:** confirm skeleton availability before M3/M4 e2e wiring; if absent, this project's hermetic e2e uses a synthetic two-contract fixture (the plan already specifies this for M2/M3) and the example-app wiring defers cleanly.

## Throughput

- **Dispatches/day:** n/a (fresh start)
- **Median dispatch wallclock:** n/a
- **Median rounds-to-satisfied:** n/a

## Calibration

- **Size prediction accuracy:** n/a (no dispatches yet)
- **Retro-trigger frequency:** n/a
- **Spike-driven re-plans:** none

## Recommended next pick

1. **M1 — Foundation (IR carrier + aggregate-load checks).** It is the bottom of the stack: M2 (authoring) needs the carrier shape to lower into; M3 (planner/verifier) needs both. Unblocks everything else. Per the health pick-next heuristics this is both "unblocks others first" and "risk-reduction first" — the reconciliation pass at its head falsifies the riskiest assumption (that the plan still matches the landed substrate) before any fan-out.

_Project is **not blocked** — DoR is met: Linear Project "Supabase Integration" exists, TML-2500 is In Progress, the scaffold `projects/cross-contract-refs/` exists with mature spec + plan. The per-issue working branch `tml-2500-cross-contract-space-fk-references` will be cut from `main` at first dispatch._

## Triggers

- No scope-shift candidate.
- No retro trigger (nothing delivered yet).
- No project-DoD coverage gap — all 10 ACs + the walking-skeleton DoD map onto M1–M4.
- **Proceed to `drive-build-workflow` on M1**, opening with the reconciliation dispatch.
