# Brief: S6-D2 — failure state lives on ManagedProject (PR #974 review response)

## Task

Four items, one lifecycle, `language-server/src/server.ts` (+ tests):

1. **Operator directive (the design change): dissolve `configFailureUris` into entry
   state.** Restructure `ManagedProject` so the published-failure marker lives on the
   entry itself:
   - `loading { load, lastGood }` (unchanged),
   - `loaded { project, configFailed?: true }` — the retention case (last-good
     serving + failure diagnostic published),
   - **new** `failed {}` (or carrying the error if useful) — replaces
     delete-plus-parallel-map on first-load failure: the entry stays, recording that
     a diagnostic is published for this config.
   Publish/clear decisions read and write entry state; the parallel map is deleted.
   Lifecycle consequences to preserve exactly: first-load failure still leaves
   documents unmanaged and awaiters rejecting (today's funnel behavior — only the
   bookkeeping changes); `dropProjectWithoutManagedDocuments` deletes the entry and
   clears the marker if the state says one is published; a successful load replaces
   any state and clears if needed; `hasManagedDocuments`-gated deletion keeps
   working. Watch the resolve-paths that treat "entry exists" as "project available"
   (`resolveProject` etc.) — a `failed` entry must not be mistaken for a loadable
   project; it should behave like absence for project resolution (trigger a fresh
   load attempt), while remembering the published marker.
2. **Operator question, answered in code**: rewrite the `isCurrentLoad` comment in
   the failure handler to explain *why* a load can be non-current despite queued
   execution: `startProjectLoad` synchronously replaces the entry with the new load
   promise, so a superseded in-flight load's continuation still fires after
   replacement — the guard protects side-effects (map writes, publishes), not
   execution order.
3. **CodeRabbit majors (both accepted):**
   - Failure branch parity with success branch: when `isCurrentLoad` and
     `hasManagedDocuments(configPath)` is **false**, do not restore `lastGood` (nor
     retain a `failed` entry) — delete the entry (the marker-clear on
     no-managed-docs should follow the same rule as `dropProjectWithoutManagedDocuments`).
     Test: reload failure after the last document closed → no resurrection, no
     zombie diagnostic.
   - `resolveProjectIfLoadable` (and any sibling awaiter-funnel): gate
     `stopManagingProject(configPath)` on the rejected load still being current —
     a superseded load's failure must not destroy a newer entry. Test: overlapping
     loads where the old fails after a new one succeeded → newer project survives.
     (This was the recorded pre-existing hazard; the operator's review round is the
     right vehicle.)
4. **CodeRabbit nitpick**: export `CONFIG_LOAD_FAILED_CODE`; tests import it instead
   of hardcoding the literal. Also sharpen the `lastGood` doc comment per the
   rabbit's wording note (a failed first load serves no project and leaves documents
   unmanaged, while still publishing the config diagnostic).

Do NOT action the docstring-coverage warning (comment padding — standing decision).
Existing S6-D1 tests updated only where the bookkeeping shape leaks into assertions;
their behavioral pins must survive unchanged.

## Scope

**In:** `language-server/src/server.ts` + `test/server.test.ts`.
**Out:** everything else; no GitHub thread replies.

## Completed when

- [ ] `configFailureUris` gone; marker state on `ManagedProject`; all S6-D1
      behavioral pins green (possibly with reshaped bookkeeping assertions).
- [ ] Both rabbit-major tests added and green; constant exported and imported.
- [ ] Gates: `pnpm --filter @prisma-next/language-server test` + typecheck + lint,
      `pnpm typecheck`, `pnpm lint:framework-vocabulary`, `pnpm lint:deps`.
      Zero casts.

## Operational metadata

- **Time-box:** 75 min. Halt: the `failed` entry state conflicts with a resolve-path
  invariant that can't be preserved without widening scope (surface the fork);
  any S6-D1 behavioral pin must weaken to pass (surface, don't weaken).
