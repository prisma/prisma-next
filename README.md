# Prisma Next

> [!IMPORTANT]
> **This repository has moved.** Prisma Next development continues in [**prisma/prisma**](https://github.com/prisma/prisma) under the name **Prisma ORM v8**. That repository preserves this one's full git history and tags. This repository is archived and read-only.
>
> - Code, issues, and pull requests → [prisma/prisma](https://github.com/prisma/prisma)
> - npm → the `@prisma-next/*` and `prisma-next` packages are deprecated; Prisma ORM v8 ships as the [`prisma`](https://www.npmjs.com/package/prisma) package — install with `npm install prisma@next`
> - Get started → `npm create prisma@next`
