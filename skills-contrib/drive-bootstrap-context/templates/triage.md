# Drive `triage` context

> Read by `drive-triage-work` and `drive-start-workflow` before they start. Capture project-specific facts the generic skills can't know. Update when a drive run surfaces something the next run should inherit.

**Skills served:** `drive-triage-work`, `drive-start-workflow`

## Sizing anchors (calibration for this repo)

<!-- Reference tasks that calibrate "what's a direct change vs orphan slice vs in-project slice in THIS repo." Populated by retros + operator calibration; treat as living. Use a table: Task | Verdict | Why. -->

## Ticket-shape patterns

<!-- Patterns that have a known verdict in this repo. Use a table: Pattern | Verdict | Notes. -->

## Linear-sync conventions

<!-- Linear team key, project for orphan-slice "umbrella" (if any), issue-id prefix, branch-name convention, PR-title convention to enable auto-close. -->

## Promote / demote ceremony notes

<!-- Project-specific overrides to the promote/demote ceremony — e.g. which Linear states to use, naming conventions for the renamed parent ticket. -->

## Failure modes (catalogue; populated by retros)

<!-- When triage misroutes work, the retro lands here. Each entry: pattern → consequence → mitigation. Empty at seeding; populated by drive-run-retro. -->
