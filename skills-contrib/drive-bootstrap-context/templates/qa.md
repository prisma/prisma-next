# Drive `qa` context

> Read by `drive-qa-plan` and `drive-qa-run` before they start. Capture project-specific facts the generic skills can't know. Update when a drive run surfaces something the next run should inherit.

**Skills served:** `drive-qa-plan`, `drive-qa-run`

## Audiences & substrates

<!-- Who are the consumers of this project's surface, and what example/test substrates exercise each one? (E.g., "users of @prisma-next/postgres via the @prisma-next/upgrade-skill, substrate `examples/`; extension authors via @prisma-next/extension-upgrade-skill, substrate `packages/3-extensions/`.") -->

## Known gate gaps

<!-- Which CI gates are structural rather than semantic, and how manual QA must cover for them. (E.g., "`pnpm check:upgrade-coverage` passes if the version directory exists — it does not enforce a per-PR semantic entry; manual QA must independently verify each affected audience has an actionable entry.") -->

## Fixture catalogue

<!-- Where canonical fixtures and demo projects live. Which one to exercise for which scenario shape. -->

## Known constraints & gaps

<!-- Things QA scripts in this project have historically gotten wrong. -->

## References

<!-- Links to relevant authoring skills, gate scripts, exemplar QA scripts. -->
