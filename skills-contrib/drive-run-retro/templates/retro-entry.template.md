## YYYY-MM-DD — <one-line summary>

**Trigger:** _<event that triggered the retro>_

**What happened:** _<1–2 sentences; the observable event>_

**Root cause:** _<the upstream failure point, not the proximate one — which gate didn't fire, which pattern wasn't in the catalogue, which assumption was wrong>_

**Landing surface(s):**

- _Canonical: `drive-<skill>/SKILL.md` § <section> — <change summary>._
- _Project-context: `drive/<category>/README.md` § <section> — <change summary>._
- _ADR: `docs/architecture docs/adrs/ADR <N> — <title>`._
