// Faithful translation of prisma/prisma@a6d0155
// packages/client/tests/functional/enums (postgres matrix entry).
//
// Upstream schema:
//   model User { id String @id @default(cuid()); plan Plan }
//   enum Plan { FREE PAID CUSTOM }

enum Plan {
  @@type("pg/text@1")
  FREE   = "FREE"
  PAID   = "PAID"
  CUSTOM = "CUSTOM"
}

model User {
  id   String @id @default(cuid(2))
  plan Plan
}
