// Faithful translation of prisma/prisma@a6d0155
// packages/client/tests/functional/issues/18598-select-count-true (postgres matrix entry).
//
// Upstream schema:
//   model User { id String @id; posts Post[] }
//   model Post { id String @id; userId String; user User @relation(fields: [userId], references: [id]) }
//
// idForProvider(postgresql) = String @id
// foreignKeyForProvider(postgresql) = String

model User {
  id    String @id @default(cuid(2))
  posts Post[]
}

model Post {
  id     String @id @default(cuid(2))
  userId String
  user   User   @relation(fields: [userId], references: [id])
}
