# Failing ports — prisma/prisma

One entry per `test.fails` port: a faithful port that runs but does not pass against today's prisma-next. Format:

`` - `<ported test file>` › `<test name>` — <what it verifies> — <the gap it hits / what prisma-next does instead> ``

Every entry here corresponds one-to-one with a `test.fails` marker in the corpus.

<!-- entries appended per batch -->

- `test/ports/prisma/functional/default-selection.test.ts` › `includes scalars` — default selection returns scalar fields (id/value/otherId) — the faithful schema includes an `Enum[]` enum-list column; the postgres emitter emits an invalid `CHECK (col IN (...))` for that array column, so the contract push fails (sqlState 22P02 "malformed array literal") before any ORM op runs. (The more-faithful native-enum-array authoring pushes but then fails at query time with `type "<enum>[]" does not exist`, code 42704 — either authoring hits a real gap.)
- `test/ports/prisma/functional/default-selection.test.ts` › `does not include relations` — default selection excludes relation fields — same enum-list emitter gap.
- `test/ports/prisma/functional/default-selection.test.ts` › `includes enums` — default selection includes the enum field — same enum-list emitter gap.
- `test/ports/prisma/functional/default-selection.test.ts` › `includes lists` — default selection includes the `String[]` list field — same enum-list emitter gap (the `Enum[]` column in the same model blocks the push).
- `test/ports/prisma/functional/default-selection.test.ts` › `includes enum lists` — default selection includes the `Enum[]` enum-list field — same enum-list emitter gap.
- `test/ports/prisma/functional/enum-array.test.ts` › `can create data with an enum array` — create a record with an enum array field — text-backed enum-list column emits `CHECK (plans IN ('FREE','PAID','CUSTOM'))`, invalid for a postgres array column; push fails with sqlState 22P02 "malformed array literal". (Native-enum-array authoring pushes but fails at query time with code 42704 `type "plan[]" does not exist` — enum arrays are unsupported end-to-end today.)
- `test/ports/prisma/functional/enum-array.test.ts` › `can retrieve data with an enum array` — create then read back the enum array — same enum-list emitter gap.
- `test/integration/test/ports/prisma/functional/extended-where.test.ts` › `findMany with cursor 1 unique (PK)` — verifies `.orderBy(p=>p.id.asc()).cursor({ id: postId2 }).all()` returns 2 rows — prisma-next cursor is exclusive (keyset-after) vs Prisma inclusive; faithful `.cursor()` port returns a different result
- `test/integration/test/ports/prisma/functional/extended-where.test.ts` › `findMany with cursor 2 uniques (PK & non-PK)` — verifies `.orderBy([id.asc, title.asc]).cursor({ id, title }).all()` returns 2 rows — prisma-next cursor is exclusive (keyset-after) vs Prisma inclusive; faithful `.cursor()` port returns a different result
- `test/integration/test/ports/prisma/functional/extended-where.test.ts` › `findMany with cursor 1 unique (non-PK)` — verifies `.orderBy(p=>p.title.asc()).cursor({ title: 'Hello World 2' }).all()` returns 2 rows — prisma-next cursor is exclusive (keyset-after) vs Prisma inclusive; faithful `.cursor()` port returns a different result
- `test/integration/test/ports/prisma/functional/extended-where.test.ts` › `findFirst with cursor 1 unique (PK)` — verifies `.orderBy(p=>p.id.asc()).cursor({ id: postId2 }).first()` returns postId2 — prisma-next cursor is exclusive (keyset-after) vs Prisma inclusive; faithful `.cursor()` port returns a different result
- `test/integration/test/ports/prisma/functional/extended-where.test.ts` › `findFirst with cursor 2 uniques (PK & non-PK)` — verifies `.orderBy([id.asc, title.asc]).cursor({ id, title }).first()` returns postId2 — prisma-next cursor is exclusive (keyset-after) vs Prisma inclusive; faithful `.cursor()` port returns a different result
- `test/integration/test/ports/prisma/functional/extended-where.test.ts` › `findFirst with cursor 1 unique (non-PK)` — verifies `.orderBy(p=>p.title.asc()).cursor({ title: 'Hello World 2' }).first()` returns postId2 — prisma-next cursor is exclusive (keyset-after) vs Prisma inclusive; faithful `.cursor()` port returns a different result
- `test/integration/test/ports/prisma/functional/extended-where.test.ts` › `findFirstOrThrow with cursor 1 unique (PK)` — verifies `.orderBy(p=>p.id.asc()).cursor({ id: postId2 }).first()` non-null and id === postId2 — prisma-next cursor is exclusive (keyset-after) vs Prisma inclusive; faithful `.cursor()` port returns a different result
- `test/integration/test/ports/prisma/functional/extended-where.test.ts` › `findFirstOrThrow with cursor 2 uniques (PK & non-PK)` — verifies `.orderBy([id.asc, title.asc]).cursor({ id, title }).first()` non-null and id === postId2 — prisma-next cursor is exclusive (keyset-after) vs Prisma inclusive; faithful `.cursor()` port returns a different result
- `test/integration/test/ports/prisma/functional/extended-where.test.ts` › `findFirstOrThrow with cursor 1 unique (non-PK)` — verifies `.orderBy(p=>p.title.asc()).cursor({ title: 'Hello World 2' }).first()` non-null and id === postId2 — prisma-next cursor is exclusive (keyset-after) vs Prisma inclusive; faithful `.cursor()` port returns a different result
- `test/integration/test/ports/prisma/functional/bytes-upsert.test.ts` › `bytes upsert should work correctly` — verifies second upsert call with same Bytes @unique conflict does not throw — second `upsertByteRow()` fails with `ORM.MUTATION_ROW_MISSING`: `ON CONFLICT DO NOTHING RETURNING *` emits zero rows on conflict, and the follow-up reload via `#buildUpsertConflictCriterion` fails to match the existing row (Uint8Array equality in the shorthand WHERE filter does not round-trip through the reload path)
- `test/integration/test/ports/prisma/functional/issues-11974.test.ts` › `should not throw an error when counting two relation fields using find` — verifies `include(..., rel => rel.count())` over two N:M relations does not throw — N:M count include does not route through the junction; `buildIncludeChildScalarSelect` uses `include.targetColumn` directly on the child table (ignoring `through`), emitting `user.commentId = comment.id`
- `test/integration/test/ports/prisma/functional/issues-12557.test.ts` › `issue 12557` — verifies `_count` on an N:M relation stays correct after deleting a related record — `include('brands', b => b.count())` on a through/N:M relation calls `buildIncludeChildScalarSelect`, which ignores the `through` descriptor and joins directly via `include.targetColumn` (`brand.categoryId`, a non-existent column), producing invalid SQL (same gap as issues-11974)
