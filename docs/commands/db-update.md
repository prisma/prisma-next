## `prisma-next db update`

Reconcile an already adopted (marker-managed) database to the current emitted contract.

### Canonical usage and flags

See the `db update` section in:

- `packages/1-framework/3-tooling/cli/README.md`

### Semantics and migration system context

For marker / ledger semantics and system-level behavior, see:

- `docs/architecture docs/subsystems/7. Migration System.md`
- `docs/architecture docs/Contract-Driven DB Update.md`
