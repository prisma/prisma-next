# Adapters & Targets

**Purpose:** define how Prisma Next supports multiple database targets through a stable Adapter SPI, capability flags, and a conformance suite. It also outlines the "Mongo family" plan, showing how a non-SQL target plugs into the same contract-first architecture and unified Plan model.

## Goals

- Make it straightforward to add a new SQL dialect or a non-SQL target without touching core
- Keep the contract core thin and push target-specific behavior into adapters
- Preserve the unified Plan contract so lanes remain portable and runtime guardrails keep working
- Provide a conformance kit so community adapters can certify behavior and safety

## Principles

- **Thin core, fat targets:** Core defines stable abstractions and validation, adapters implement specifics
- **Capability discovery over feature detection:** Adapters declare what they can do and the runtime adjusts behavior
- **Deterministic lowering:** Given the same contract, AST, and profile, an adapter must render the same wire payload
- **One call → one statement:** Plans map to a single execution unit for predictability and guardrails

⸻

Adapter SPI overview

An adapter implements three concerns
	•	Contract profile
Extends the contract with target-specific storage constructs and codecs, and exposes capability flags
	•	Lowering & compilation
Turns relational AST or target-native AST into a deterministic wire payload (SQL string + params for SQL targets, JSON pipeline for Mongo family)
	•	Driver execution
Executes a Plan and returns typed rows according to registered codecs

Package shape

@prisma/adapter-postgres/
  src/
    profile.ts         # profile id, capabilities, contract extensions
    lower.ts           # AST → SQL lowering
    compile.ts         # QueryAST → { sql, params }
    driver.ts          # execute(sql, params) → rows
    migrate.ts         # DDL op executors for runner
    explain.ts         # EXPLAIN integration for budgets
    index.ts           # adapter factory

Minimal TypeScript SPI

export type CapabilityMap = {
  transactionalDDL: boolean
  savepoints: boolean
  lateral: boolean
  jsonAgg: boolean
  partialIndex: boolean
  deferrableConstraints: boolean
  explainFormat: 'text' | 'json'
  // extensible
}

export interface AdapterProfile {
  id: string                     // e.g. "postgres/default@1"
  target: 'postgres' | 'mysql' | 'sqlite' | 'mongodb'
  capabilities: CapabilityMap
  extendContract?(core: ContractCore): ContractCore // add types, storage nodes, codecs
}

export interface Lowerer {
  target: AdapterProfile['target']
  lower(query: QueryAST, contract: ContractCore): Lowered // deterministic
}

export type Lowered =
  | { kind: 'sql'; sql: string; params: unknown[] }
  | { kind: 'mongo'; pipeline: any[]; collection: string }

export interface Driver {
  connect(cfg: any): Promise<void>
  execute(lowered: Lowered, codecs: CodecRegistry): Promise<any[]> // rows or docs
  explain?(lowered: Lowered): Promise<ExplainResult>
  close(): Promise<void>
}

export interface MigrationOps {
  apply(op: DDLOp, ctx: ApplyContext): Promise<void>
  precheck(op: DDLOp, ctx: ApplyContext): Promise<CheckResult>
  postcheck(op: DDLOp, ctx: ApplyContext): Promise<CheckResult>
}

The runtime wires these pieces into the Plan pipeline and plugin hooks

⸻

Capability flags

Adapters must publish a stable capability set so lanes and plugins can make safe choices

Examples
	•	transactionalDDL the engine supports wrapping DDL in a transaction
	•	savepoints savepoint support for fine-grained rollback
	•	lateral lateral joins are available
	•	jsonAgg aggregate to JSON at the engine
	•	partialIndex partial or filtered indexes supported
	•	deferrableConstraints DEFERRABLE constraints supported
	•	explainFormat preferred EXPLAIN output for budgets and diagnostics

Lanes and plugins should branch on capabilities, not target strings

⸻

SQL adapter specifics

Lowering responsibilities
	•	Quote and escape identifiers deterministically
	•	Normalize aliases and whitespace for stable hashing and golden tests
	•	Render JOINs, GROUP BY, ORDER, LIMIT according to dialect
	•	Emit safe parameter placeholders and parameter order

DDL execution
	•	Provide op executors for the migration runner with txSemantics and compensations
See ADR 037 and ADR 044

EXPLAIN
	•	Return a normalized structure the budget plugin can consume
For JSON explainers, adapters normalize common fields such as total cost or row estimates

⸻

Mongo “family” plan

Mongo is a first-class target with a non-SQL lowering, still producing a unified Plan

Contract shape extensions

The contract core is extended with Mongo storage constructs via the profile
	•	Collections map to storage units with JSON schema hints
	•	Indexes capture compound, unique, partial, and TTL behaviors
	•	Types add objectId, date, decimal128, and nested documents
	•	Capabilities include aggregation stages supported, transactions, and explain formats

Example contract extension snippet

{
  "target": "mongodb",
  "collections": {
    "users": {
      "schema": {
        "properties": {
          "_id": { "type": "objectId" },
          "email": { "type": "string" },
          "active": { "type": "boolean" }
        },
        "required": ["_id", "email"]
      },
      "indexes": [
        { "keys": { "email": 1 }, "unique": true }
      ]
    }
  },
  "capabilities": { "mongo": { "transactions": true, "stages": ["match","lookup","project","limit","sort"] } }
}

Query lanes

Two options, both producing a Plan the runtime understands
	•	Relational DSL lowered to Mongo
A limited mapping for simple projections, filters, and 1:N lookups using $lookup
Suitable for teams that want a cross-target lane
	•	Mongo DSL lane
Target-native builder that constructs an Aggregation Pipeline AST and lowers to { kind: 'mongo', pipeline, collection }

Both lanes stamp the same annotations and participate in the same plugin hooks for lints, budgets, and telemetry

Execution

The Mongo driver executes the pipeline with the official Node driver and decodes results using registered codecs

Migrations

Migration ops are Mongo-specific but follow the same edge model
	•	createCollection, dropCollection, createIndex, dropIndex
	•	addField with updateMany backfills
	•	Schema advisory checks rely on validator updates where supported

⸻

## Adapter Capability Schema and Negotiation v1

Adapters declare supported capabilities through a standardized schema per ADR 065:

### Capability Declaration
```typescript
interface AdapterCapabilities {
  [namespace: string]: {
    [capability: string]: boolean | string | object | array
  }
}

// Example PostgreSQL adapter capabilities
const postgresCapabilities = {
  postgres: {
    lateral: true,
    jsonAgg: true,
    partialIndex: true,
    deferrableConstraints: true,
    savepoints: true,
    transactionalDDL: true,
    explainFormat: 'json'
  },
  pgvector: {
    ivfflat: true,
    hnsw: false,
    vector: { dim: 'any', distance: ['cosine', 'l2', 'inner'] }
  }
}
```

### Capability Negotiation Flow
1. **Adapter Advertisement**: Adapter declares capabilities at connect time
2. **Contract Requirements**: Runtime checks contract capabilities against adapter capabilities
3. **Extension Pack Loading**: Loads installed packs and validates capability requirements
4. **ProfileHash Computation**: Computes profileHash from negotiated capabilities
5. **Early Failure**: Missing required capabilities cause connection failure

### Capability Stability Contract
- **Core capabilities** (e.g., `lateral`, `jsonAgg`) are reserved and immutable
- **Extension capabilities** are prefixed by pack namespace (e.g., `pgvector.ivfflat`)
- **Reserved namespaces** include `prisma`, `core`, `internal`, and adapter names

## Lowering and Portability

**Lowering lives in adapter profiles, lanes remain portable:**

- **Adapter-specific lowering**: SQL generation, type mapping, and capability-specific optimizations
- **Lane portability**: Query DSL, TypedSQL, and ORM lanes work across adapters
- **Capability branching**: Lanes branch on capabilities rather than target strings
- **Extension integration**: Extension functions/operators integrate through adapter capabilities

⸻

Testing & conformance

Community adapters and new targets must pass the conformance kit to claim support levels

Levels
	•	L0 Execute
Connect, compile, execute SELECT with filters, order, limit, simple codecs, Plan hashing stability
	•	L1 Schema
Additive migrations, index creation, contract marker storage, EXPLAIN integration for budgets
	•	L2 Advanced
Transactions or compensation strategy, partial indexes, advisory locks, pre/post checks, phased not-null, backfill orchestration
	•	L3 Extensions
Extension capability gating, function/operator rendering, codec integration, migration operations, guardrails enforcement

See ADR 026 for certification levels

Fixture packs
	•	Contract fixtures canonical contract.json sets per target with golden Plans and outputs
	•	Query fixtures relational and target-native ASTs with expected lowered payloads
	•	Migration fixtures op sequences and expected pre/post outcomes

Golden tests
	•	SQL adapters must pass golden SQL rendering tests with normalized whitespace and aliasing
	•	Mongo adapters must pass golden pipeline rendering tests with normalized stage ordering where deterministic

Budget & lint tests
	•	Adapters must provide EXPLAIN samples that budget evaluators can consume
	•	Lints run over lowered payloads and annotations to ensure rule parity across targets

Error envelope

All adapters must map engine errors into the stable RuntimeError shape from ADR 027

⸻

Adapter lifecycle in runtime
	1.	Negotiation
Runtime loads the adapter profile by id and merges capabilities with the contract profile hash
	2.	Lowering
Lane produces a target-agnostic AST or a target-native AST which the adapter lowers deterministically
	3.	Plan formation
Runtime builds a Plan with body from the lowered payload, plus annotations and hashes
	4.	Hooks
Plugins run beforeCompile, beforeExecute with lints and budgets
	5.	Execute
Driver executes and decodes with the contract’s codecs
	6.	After hooks
Telemetry and budgets record outcomes, errors are normalized

⸻

Adding a new SQL dialect

Checklist
	•	Implement AdapterProfile with capability flags
	•	Provide lower and compile for SQL rendering and parameterization
	•	Implement driver with execute and explain
	•	Add DDL op executors and map txSemantics
	•	Pass L0 and L1 suites, target L2 for production readiness
	•	Provide golden tests for rendering and EXPLAIN normalization

⸻

Adding a new non-SQL family

Checklist
	•	Define contract extensions for storage constructs and types in the profile
	•	Provide a target-native lane or a lowering from the relational DSL subset
	•	Implement a driver with execute and explain analogs
	•	Implement migration ops for the family and pass L1 schema suite
	•	Validate Plan annotations parity so guardrails apply consistently

⸻

Community contribution path
	•	Publish adapters as @org/prisma-adapter-<target> packages
	•	Include adapter-manifest.json with profile id, version, capability summary, and conformance level badges
	•	Run the open conformance kit in CI and attach results to releases
	•	Adopt ADR 016 determinism guarantees and ADR 011 unified Plan contract

⸻

Open questions
	•	How far should the relational DSL lower into Mongo before it becomes misleading for users
	•	Should we ship a small “compat” lane that normalizes pagination across targets
	•	Do we allow adapters to enrich Plans post-lowering with explain-time annotations, or keep explain external only

The adapter and target model outlined here lets us grow the ecosystem without bloating core, keep safety and determinism measurable, and welcome community contributions across SQL and non-SQL families

## ADRs

	•	ADR 065 — Adapter capability schema & negotiation v1
Canonical capability keys, optional features, negotiation flow at connect time, and how lanes/plugins branch on capabilities rather than target strings
	•	ADR 066 — Contract core extension model for targets
How adapters extend the data contract with storage constructs, types, and codecs using namespaced nodes, plus forward/backward-compat rules
	•	ADR 067 — Adapter lowering determinism & golden tests
Determinism requirements for lowering, normalization rules, golden rendering policy per target, and when goldens may change
Complements ADR 016 but focuses on adapter conformance
	•	ADR 068 — Error mapping to RuntimeError
Stable mapping from engine/driver errors to the RuntimeError envelope and codes, including SQLSTATE handling and non-SQL equivalents
Complements ADR 027
	•	ADR 069 — Codec registration precedence & collisions
Precedence between contract codecs, adapter defaults, and lane hints, with conflict detection and safe overrides
Complements ADR 030
	•	ADR 070 — Non-SQL Plan body kinds & execution semantics
Formalizes { kind: 'mongo', pipeline, collection } and other non-SQL bodies, required annotations, and execution guarantees under the unified Plan model
Complements ADR 011
	•	ADR 071 — Migration operation namespaces per target
vendor/op@v naming, versioning, and validation for SQL vs non-SQL op families, including cross-family no-op semantics and risk classes
Complements ADR 041 and ADR 044
	•	ADR 072 — Adapter packaging & manifest
Required metadata (profileId, capabilities, conformance level, versions), how manifests are discovered and verified, and publishing guidelines for community adapters
Complements ADR 026 (conformance kit)
	•	ADR 073 — Capability fallback and gating
What happens when a required capability is missing: degrade, rewrite, or reject; user-visible errors and policy knobs for strict vs permissive modes
	•	ADR 074 — Third-party adapter security & trust policy
Sandboxing expectations, allowed dependencies, telemetry/redaction requirements, and how PPg treats untrusted adapters
	•	ADR 075 — Profile selection & pinning
Rules to select an adapter profile by engine version/features, how profiles are pinned in profileHash, and upgrade pathways without breaking determinism
Complements ADR 004
	•	ADR 076 — EXPLAIN normalization across targets
Minimal normalized explain schema for budgets, mapping from target-specific explain outputs, and adapter obligations for units and fields
Complements ADR 023
	•	ADR 065 — Adapter capability schema & negotiation v1
Canonical capability keys, optional features, negotiation flow at connect time, and how lanes/plugins branch on capabilities rather than target strings
	•	ADR 067 — Adapter lowering determinism & golden tests
Determinism requirements for lowering, normalization rules, golden rendering policy per target, and when goldens may change
Complements ADR 016 but focuses on adapter conformance
