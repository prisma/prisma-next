# Data Contract

**Purpose:** define the "data contract" artifact, how it's authored, what it contains, how it's versioned and verified, and how other subsystems consume it.

## 1. Purpose & Scope

The data contract is the single, verifiable description of an application's data model and its storage layout. It is the system boundary that ties together:

- Application code (queries and runtime verification)
- Database state (contract marker in the DB)
- Migrations (contract‑to‑contract transitions)
- Tooling (preflight, advisors, CI checks)
- Agents (machine‑readable, deterministic inputs and outputs)

**Scope (v1):** relational targets starting with Postgres; models/relations; storage (tables, columns, indexes, FK/PK); type codecs; capability flags; contract hashing; authoring via PSL or a TS builder; deterministic emission to `contract.json` and `.d.ts` types.

**Non‑goals (v1):** data cataloging/governance beyond schema; RBAC/row‑level security; cross‑tenant schema federation; full polymorphic type inference across arbitrary expressions (handled at query level).

## 2. Artifact & Determinism

### 2.1 Artifact

- **contract.json:** canonical JSON describing models, storage, codecs, capabilities, and metadata
- **Types (contract.d.ts):** generated TS types for tables/columns/models and column codecs
- **Hashing:**
  - **coreHash:** hash of the meaningful schema (models, fields, relations, storage layout)
  - **profileHash:** optional hash of physical profile (dialect‑specific features/capabilities that do not change meaning, e.g., index methods, storage parameters)
- **DB Marker:** a small schema in the database stores the current `coreHash`, `profileHash`, marker version, and a ledger of applied migrations/tasks

### 2.2 Deterministic emission

- **Canonicalization:** key ordering (lexicographic), normalized scalars (number/string/boolean), normalized defaults (e.g., `now()`), and stable array ordering where order has semantic meaning
- **Hash:** SHA‑256 (or equivalent) over the canonicalized JSON
- **Emission is deterministic across platforms and toolchains**

## 3. Authoring Modes

Two equivalent sources produce the same artifact:

1. **PSL‑first:** today's schema syntax; parsed and emitted to `contract.json` + `.d.ts`
2. **TS‑first builder:** an explicit API (e.g., `defineContract()`) that declares models, fields, relations, and storage with abstract scalars and facets

**Rules:**

- A repo/package declares one authoritative source (PSL or TS builder). The other form can be generated for reference, not as source of truth
- **Dev workflow:** bundler/dev‑server plugins (Vite/Next/esbuild) auto‑emit `contract.json` and `.d.ts` on import/watch; no visible "generate" step in day‑to‑day
- **CI:** explicit emit step validates determinism and pins artifacts

## 4. Structure & Content

At a high level:

```json
{
  "schemaVersion": "1",
  "targetFamily": "sql",
  "target": "postgres",
  "coreHash": "sha256:…",
  "profileHash": "sha256:…",
  "capabilities": { "postgres": { "jsonb": true, "lateral": true, "jsonAgg": true, "vector": false } },
  "models": {
    "User": {
      "storage": { "table": "user" },
      "fields": {
        "id": { "column": "id", "type": "integer", "pk": true, "nullable": false, "default": { "kind": "autoincrement" } },
        "email": { "column": "email", "type": "string", "unique": true, "nullable": false },
        "active": { "column": "active", "type": "boolean", "nullable": false, "default": { "kind": "literal", "value": "true" } }
      },
      "relations": {
        "posts": { "to": "Post", "cardinality": "1:N", "on": { "parentCols": ["id"], "childCols": ["user_id"] } }
      }
    }
  },
  "storage": {
    "tables": {
      "user": {
        "columns": {
          "id":    { "type": "int4", "nullable": false },
          "email": { "type": "text", "nullable": false },
          "active":{ "type": "bool", "nullable": false }
        },
        "primaryKey": { "columns": ["id"], "name": "user_pkey" },
        "uniques":    [ { "columns": ["email"], "name": "user_email_key" } ],
        "indexes":    [],
        "foreignKeys": []
      },
      "post": {
        "columns": {
          "id":      { "type": "int4", "nullable": false },
          "user_id": { "type": "int4", "nullable": false }
        },
        "primaryKey": { "columns": ["id"], "name": "post_pkey" },
        "foreignKeys": [ { "columns": ["user_id"], "references": { "table": "user", "columns": ["id"] }, "name": "post_user_id_fkey" } ],
        "indexes": [ { "columns": ["user_id"], "name": "post_user_id_idx" } ]
      }
    }
  },
  "codecs": {
    "int4": { "ts": "number" },
    "text": { "ts": "string" },
    "bool": { "ts": "boolean" }
  },
  "meta": { "source": "psl://prisma/schema.prisma" }
}
```

**Notes:**

- **models** map domain entities to storage
- **storage** describes physical layout; in SQL: tables, columns, PK/UK/FK, indexes
- **capabilities** surface target features that affect compilation/lowering (e.g., lateral)
- **codecs** define DB→TS type mapping; extensions can add custom scalars (e.g., vector, geography)
- **meta** is non‑semantic (hash‑excluded) info such as source location

### 4.1 Core entities vs pack extensions

Core defines portable entities in the contract; target- and adapter-specific storage types and logic are provided by adapters or extension packs.

- Core entities in the contract
  - Tables, columns, primary keys, unique constraints, foreign keys, and basic indexes
  - ForeignKey as first-class relation metadata consumable by lanes and ORM helpers
  - Logical enums registry; columns reference enums via `logicalType`
  - Namespaces via `RelationId = { namespace?, name }` (e.g., Postgres schemas)
  - `ext?: Record<string, unknown>` on every entity reserved for packs
  - Capability keys referenced by the contract, not implemented by core

- Provided by adapters or packs (out of core)
  - Physical storage types and codecs (e.g., `int4`→`number`, branded `Geometry`, `Vector`)
  - Index methods and predicates (e.g., GIN/GiST, partial/filtered indexes)
  - Projection features (e.g., DISTINCT ON) and join flavors (lateral/semi/anti)
  - Distribution/shard keys and colocation policies
  - View definitions and materialization mechanics
  - Extension functions/operators and their deterministic lowering rules

- Separation of concerns
  - The contract stays target-agnostic; packs attach namespaced data under `ext` for adapter-aware behavior
  - Capabilities are negotiated at runtime and used by lanes/plugins; adapters/packs own their keys and semantics
  - Verification uses the contract hash; lowering and execution defer to the adapter, with codecs composed from adapters/packs

Examples

Logical enum referenced by a column

```json
{
  "enums": [{ "name": "UserStatus", "values": ["active", "suspended"] }],
  "tables": {
    "public.user": {
      "columns": {
        "status": { "type": { "logical": "UserStatus" }, "nullable": false }
      }
    }
  }
}
```

Partial index via pack extension

```json
{
  "tables": {
    "public.post": {
      "indexes": [{
        "columns": ["published", "created_at"],
        "ext": { "pg.index": { "predicate": "published = true" } }
      }]
    }
  }
}
```

Determinism
- Core fields follow ADR 010 ordering and normalization
- `ext` content participates in profile hash per ADR 004 and ADR 106
- Unknown `ext` namespaces are preserved round-trip but not interpreted by core

## 5. Compatibility Rules

- **Additive changes** (add table/column, nullable field, new index) → compatible; produce new `coreHash`
- **Renames/drops/type changes** → require explicit hints and a migration plan; not auto‑compatible
- **Non‑null column addition** → needs default/backfill task; planner should fail fast without a task
- **Capability changes** (e.g., enabling `jsonAgg`) → affect `profileHash`; do not change meaning
- **Deprecation policy:** contract schema (`schemaVersion`) evolves under semver; breaking changes bump major

## 6. Consumers & Flows

- **Query DSL & TypedSQL:** load `contract.json` + `.d.ts` to expose typed column/model references; embed `coreHash` in each Plan for verification
- **Runtime:** verifies `plan.coreHash` vs active contract; streams results as `AsyncIterable<Row>` with codecs applied per-row for extension types; lints/budgets enforced incrementally during streaming
- **Migrations:** planner diffs two contracts (from → to) to emit schema edges and node tasks; runner asserts DB marker matches `from.coreHash`
- **Preflight:** uses contract to validate referenced objects, policy checks, EXPLAIN expectations
- **PPg:** stores the DB marker; orchestrator and advisors use contract data to plan and analyze changes

## 7. Public APIs (library & CLI)

**Library (@prisma/contract):**

- `load(pathOrJson): Contract`
- `validate(contract: unknown): Contract`
- `hash(contract: Contract): { coreHash: string; profileHash?: string }`
- `diff(a: Contract, b: Contract): ContractDiff` (models, storage, relations, capability deltas)
- `capabilities(contract): CapabilityFlags`
- `introspectRefs(plan): ReferencedObjects` (best‑effort mapping for diagnostics)

**CLI:**

- `prisma-next emit` (PSL/TS → `contract.json` + `.d.ts`)
- `prisma-next verify --db <url>` (assert DB marker hash vs contract)
- `prisma-next diff <from> <to>` (human‑readable and JSON output)

**Dev plugins:**

- Vite/Next/esbuild plugin that auto‑emits on import/watch and surfaces errors inline

## 8. Extensions

Prisma Next's extension system enables domain-specific capabilities through installable packs that extend the data contract with additional types, functions, and storage constructs.

### Extension Encoding in Contract

Extensions are encoded in the contract under `extensions.<namespace>` with a fixed structure:

```json
{
  "extensions": {
    "pgvector": {
      "version": "1.2.0",
      "capabilities": { "ivfflat": true, "hnsw": false },
      "decorations": {
        "columns": [
          {
            "ref": { "kind": "column", "table": "document", "column": "embedding" },
            "payload": { "dim": 1536, "distance": "cosine" }
          }
        ]
      },
      "constructs": {
        "operatorClasses": [
          {
            "name": "pgvector_ivfflat_cosine",
            "family": "ivfflat",
            "distance": "cosine"
          }
        ]
      }
    }
  }
}
```

### PSL and TS Authoring

Both PSL and TS authoring modes support extensions through namespaced syntax:

**PSL Syntax:**
```prisma
extensions {
  pgvector = "1.2.0"
  postgis  = "3.4.1"
}

model Document {
  id        Int     @id @default(autoincrement())
  content   String
  embedding Bytes   @pgvector.column(dim: 1536, distance: cosine)
}

model Place {
  id   Int     @id @default(autoincrement())
  geom Bytes   @postgis.geometry(type: "POINT", srid: 4326)

  @@postgis.gistIndex(fields: [geom])
}
```

**TS Builder Syntax:**
```typescript
contract
  .table('document', t => t
    .column('embedding', types.bytea().ext('pgvector', { dim: 1536, distance: 'cosine' }))
  )
  .extensions({ pgvector: '1.2.0' })
```

### Capability Key Namespace and Stability

Capability keys follow a strict namespace and stability contract:

- **Core capabilities** (e.g., `lateral`, `jsonAgg`) are reserved and immutable
- **Extension capabilities** are prefixed by pack namespace (e.g., `pgvector.ivfflat`)
- **Reserved namespaces** include `prisma`, `core`, `internal`, and adapter names (`postgres`, `mysql`, `sqlite`)

See [Capabilities Reference](../../reference/capabilities.md) for the complete list of canonical capability keys.

### Canonicalization Rules for Extensions

Extension data follows deterministic canonicalization rules per ADR 106:

- **Key ordering**: Maps sorted lexicographically
- **Array ordering**: Preserved unless pack schema marks as sets
- **Reference stability**: Core node references use structured addressing
- **Version pinning**: Extension versions are pinned and immutable per contract

### ProfileHash Computation

The `profileHash` is computed from negotiated capabilities and adapter configuration:

- **Core capabilities**: Adapter-advertised features (e.g., `lateral`, `jsonAgg`)
- **Extension capabilities**: Pack-declared features in use (e.g., `pgvector.ivfflat`)
- **Adapter profile**: Engine version and configuration (e.g., `postgres@15`)

Changes to capability flags or adapter profiles affect `profileHash` but not `coreHash`, enabling capability evolution without contract invalidation.

### Extension Validation

The emitter validates extensions against pack-provided schemas:

- **Namespace presence**: Extension must be pinned in `extensions` block
- **Schema validation**: Attribute arguments validated against pack JSON Schema
- **Capability gating**: Extension usage checked against adapter capabilities
- **Reference resolution**: Decorations must reference existing core nodes

## 8.1 Pack-Owned Top-Level Blocks

In addition to decorators (attributes) on core entities, packs can register entirely new **top-level blocks** that introduce first-class constructs like views, enums, and other domain-specific objects (see **ADR 126** for the formal SPI).

**Top-level blocks** are:
- Declared at the PSL file root (e.g., `pg.view MyActiveUsers { ... }`, `pg.enumType Status { ... }`)
- Owned and emitted by packs, not core
- Each produces a stable `id` computed from content hash and fully-qualified name
- Deterministically serialized to `contract.extensions.<namespace>.<kind>[]`

**Examples:**

```prisma
extensions {
  postgres = "15.0"
}

// Top-level block provided by postgres pack
pg.view active_users {
  schema: "public"
  sql: """
    select id, email from "user" where active = true
  """
  shape: { id: Int, email: String }
}

pg.materializedView daily_stats {
  schema: "analytics"
  sql: """
    select date_trunc('day', created_at) as day, count(*) as signups
    from "user" group by 1
  """
  materialized: true
  refreshPolicy: { mode: "onDemand" }
  shape: { day: DateTime, signups: BigInt }
}

pg.enumType user_status {
  values: ["active", "suspended"]
}
```

**Emission:**

Top-level blocks emit deterministic JSON under `contract.extensions.<namespace>.<kind>[]`:

```json
{
  "extensions": {
    "postgres": {
      "version": "15.0",
      "views": [
        {
          "id": "pg.view:public.active_users@sha256:abc123",
          "name": "active_users",
          "schema": "public",
          "sql": "select id, email from \"user\" where active = true",
          "shape": { "id": "int4", "email": "text" },
          "materialized": false
        }
      ],
      "enumTypes": [
        {
          "id": "pg.enumType:user_status@sha256:def456",
          "name": "user_status",
          "values": ["active", "suspended"]
        }
      ]
    }
  }
}
```

**Validation:**

- Packs provide JSON Schemas and validation functions for their blocks
- Emitter enforces:
  - Block syntax and grammar per pack parser
  - Semantic validity (e.g., view dependencies exist, SQL is non-empty)
  - Capability gating (e.g., materialized views require `postgres.view.materialized` capability)
  - Deterministic canonicalization per ADR 106

**Distinguish from decorators:**

- **Decorators** (e.g., `@pg.type("user_status")`, `@@pg.predicate("active = true")`) attach metadata to existing core entities (columns, indexes, tables)
- **Top-level blocks** (e.g., `pg.view`, `pg.enumType`) introduce new, addressable constructs with their own identity
- Both are pack-owned, but blocks are standalone while decorators augment core nodes

See **ADR 126** for the full PSL top-level block SPI.

## 8.2 Read-Only Sources in the Contract

Pack-owned blocks (such as views) can optionally **project as read-only sources** into a `sources` map at the contract root, making them queryable through the DSL like tables but with read-only enforcement.

**Purpose:**

- Keep view/enum definitions in packs (target-specific)
- Expose views to the query DSL with full type safety
- Signal to runtime and lints that mutations are disallowed
- Maintain a clear origin link back to the pack-owned object

**Contract structure:**

```json
{
  "sources": {
    "public.active_users": {
      "readOnly": true,
      "projection": {
        "id": { "type": "int4", "nullable": false },
        "email": { "type": "text", "nullable": false }
      },
      "origin": {
        "namespace": "postgres",
        "kind": "view",
        "id": "pg.view:public.active_users@sha256:abc123"
      },
      "capabilities": {
        "postgres.view.base": true,
        "postgres.view.materialized": false
      }
    },
    "public.user": {
      "readOnly": false,
      "projection": {
        "id": { "type": "int4", "nullable": false },
        "email": { "type": "text", "nullable": false }
      }
    }
  }
}
```

**Source fields:**

- **`readOnly: boolean`** — If true, mutations (INSERT/UPDATE/DELETE) are blocked by lints and runtime checks
- **`projection`** — Map of column name to `{ type, nullable }` for type inference and DSL exposure
- **`origin`** (optional) — Provenance link for views: `{ namespace, kind, id }` traces back to the pack-owned block
- **`capabilities`** — Feature flags relevant to this source (e.g., whether it's materialized, refreshable, etc.)

**Type generation:**

The `.d.ts` file exposes sources alongside tables:

```typescript
export interface Sources {
  'public.active_users': { id: number; email: string }
  'analytics.daily_stats': { day: Date; signups: bigint }
}

export const t: TablesAndSources<Tables, Sources>
```

Developers query views identically to tables:

```typescript
const plan = sql()
  .from(t['public.active_users'])
  .select({ id: t['public.active_users'].id, email: t['public.active_users'].email })
  .limit(100)
  .build()
```

**Mutation enforcement:**

- **Lints** (build-time): `no-mutation-on-read-only-source` error prevents INSERT/UPDATE/DELETE on read-only sources
- **Runtime** (execution-time): Plan verification checks `readOnly` flag and blocks mutations with a clear error
- Errors are deterministic and actionable for developers and agents

See **ADR 127** for the full design of views as read-only sources, including migrations, materialization, and adapter lowering.

## 8.3 Tagged content in the contract

- Any field contributed by a pack may store an `ext` object with `{ pack, tag, body, bodyHash }` representing canonicalized tagged literal content owned by the pack.
- The core treats `ext` values as opaque, except for canonical diffing using `bodyHash`.
- Views and other read-only sources may include `definition.ext`. Partial indexes store their predicates as `where.ext`. Check constraints store `check.ext`.

## 9. Extensibility (Thin Core, Fat Targets)

**Target adapters register:**

- Scalar mappings (`int4`→`number`, `timestamptz`→`Date`, etc.)
- Capability flags and lowering constraints
- Validation hooks (e.g., "FK requires equality index")

**Extension packs add:**

- New scalars (e.g., vector, geography) + codecs
- Storage constructs (e.g., PostGIS indexes) under capabilities and `storage.indexes.method`
- Function and operator registries for type-safe query building
- Migration operations for extension-specific DDL

**Mongo family (path):**

- `targetFamily: "document"`, `target: "mongo"`
- collections (keys, indexes), document schema hints
- Model‑to‑document mapping rules
- Same hashing and verification story; different lowering and capabilities

**Community contributions:**

- TS‑only package boundaries; stable adapter interfaces; documented JSON schema; conformance tests let external contributors add dialects, scalars, and validation rules

## 9. Performance & Limits

- **Parse/emit budget:** fast enough to run on file save (<100 ms for small contracts, <1 s for large schemas)
- **Hashing:** O(size of JSON); cached in dev plugin
- **Artifact size:** aim to keep `contract.json` compact (no redundant duplication between models and storage)
- **Watch mode:** debounce emissions; avoid rebuild storms

## 10. Security & Privacy

- **contract.json** contains structure only—no data. Safe to commit to source control
- **DB marker/ledger** contain hashes and op metadata (no application data)
- **Diagnostics** redact identifiers as configured for logs; EXPLAIN plans may include table names—guard with env‑based redaction
- **Secrets** for DB verification/preflight are handled by the runtime/CI, not embedded in the contract

## 11. Open Questions & Risks

- **Cross‑platform determinism:** ensure canonicalization produces identical hashes across Node versions and locales
- **Numeric/decimal precision** and default literal normalization—define a single canonical representation
- **Handling collations/encodings** in the contract (likely `profileHash` only)
- **Minimal Mongo v1 surface** that is credible but doesn't overreach
- **Backward‑compatible evolution** of models vs storage sections without duplication

## 12. Test Strategy

- **Schema conformance:** arktype schema validation; property tests for allowed/denied shapes
- **Determinism:** golden tests for PSL/TS → `contract.json` + `coreHash`/`profileHash`
- **Round‑trip:** PSL→contract→PSL (rendered) sanity checks for information preservation (where applicable)
- **Diff tests:** known change sets (add column, add FK, rename with hint) → expected diffs
- **Adapter conformance:** per‑dialect suite validating codecs, capabilities, and validation hooks

## 13. References

- **Subsystems consuming the contract:** Query DSL, TypedSQL, Runtime, Migrations, Preflight, PPg Integration
- **ADRs:**
  - `adr/001-migrations-as-edges.md`
  - `adr/002-plans-are-immutable.md`
  - `adr/003-one-query-one-statement.md`
  - `adr/004-core-vs-profile-hash.md`
  - `adr/005-thin-core-fat-targets.md`
  - `adr/104-psl-extension-namespacing-syntax.md`
  - `adr/105-contract-extension-encoding.md`
  - `adr/106-canonicalization-for-extensions.md`
  - `adr/117-extension-capability-keys.md`
  - `adr/126-psl-top-level-block-spi.md`
  - `adr/127-views-as-extension-owned-read-only-sources.md`
  - `adr/128-read-only-sources-and-extension-owned-ddl.md`
  - `adr/129-template-tagged-literals-for-extensions.md`

## Summary

The data contract is the bedrock artifact of Prisma Next. It is deterministic, verifiable, and consumable by humans, machines, and agents. It enables safer queries, deterministic migrations, and a contract‑aware platform story with PPg—while keeping the core small, target adapters powerful, and the project open to community contribution.
