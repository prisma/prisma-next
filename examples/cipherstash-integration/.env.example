DATABASE_URL=postgres://postgres:postgres@localhost:5432/cipherstash_demo

# CipherStash workspace credentials. Generate via the CipherStash dashboard
# (https://cipherstash.com/signup) and copy the four values from the
# onboarding flow's `.env` snippet.
CS_WORKSPACE_CRN=
CS_CLIENT_ID=
CS_CLIENT_KEY=
CS_CLIENT_ACCESS_KEY=
