{
  "name": "todo-app",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "pnpm exec tsc",
    "dev": "pnpm exec tsc --watch",
    "start": "pnpm exec node dist/main.js",
    "generate": "pnpm exec prisma-next generate schema.psl",
    "dev:contract": "pnpm exec prisma-next dev schema.psl",
    "setup-db": "pnpm exec node dist/setup-db.js",
    "test": "vitest run",
    "test:integration": "vitest run --config vitest.config.ts",
    "lint": "eslint 'src/**/*.ts'",
    "typecheck": "pnpm exec tsc --noEmit && pnpm exec tsc --noEmit --skipLibCheck $(find src -name '*.ts' 2>/dev/null || true)"
  },
  "dependencies": {
    "@prisma/cli": "workspace:*",
    "@prisma/dev": "^0.11.0",
    "@prisma/psl": "workspace:*",
    "@prisma/relational-ir": "workspace:*",
    "@prisma/runtime": "workspace:*",
    "@prisma/schema-emitter": "workspace:*",
    "@prisma/sql": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^24.7.2",
    "typescript": "^5.6.2",
    "vitest": "^2.1.1"
  }
}
