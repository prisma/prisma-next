# prisma-next-cloudflare-worker

End-to-end example for the `@prisma-next/postgres/serverless` facade running on a Cloudflare Worker against a Hyperdrive-fronted Postgres.

> Detailed setup, deploy, and integration-test instructions are added in the next slice of this milestone (M3 task 3.5).
